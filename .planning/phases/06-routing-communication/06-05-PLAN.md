---
phase: 06-routing-communication
plan: 05
type: execute
wave: 4
depends_on:
  - 06-02
  - 06-03
  - 06-04
files_modified: []
autonomous: false
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - COMM-01
  - COMM-02
  - COMM-03
  - COMM-04
  - COMM-05

must_haves:
  truths:
    - "npm run build exits 0 — no TypeScript errors, no missing imports, no broken module resolution"
    - "All 7 Konfiguration tabs are visible and navigable: Öffnungszeiten, Terminarten, Begrüßungstexte, Vertretung, Medikamente, Routing, Kommunikation"
    - "Routing tab: routing rules table renders (empty state OK), VIP numbers section renders (empty state OK)"
    - "Kommunikation tab: all 4 sub-tabs render — Interne Benachrichtigungen, Patientenbenachrichtigungen, Nachrichtenvorlagen, Versandprotokoll"
    - "No Phase 5 tabs broken by Phase 6 additions"
    - "All Phase 6 requirements delivered: ROUTE-01..03, COMM-01..05"
  artifacts:
    - path: "src/components/config/RoutingTab.tsx"
      provides: "All ROUTE-01..03 UI delivered and verified"
    - path: "src/components/config/KommunikationTab.tsx"
      provides: "All COMM-01..05 UI delivered and verified"
  key_links:
    - from: "src/components/config/KonfigurationTabs.tsx"
      to: "RoutingTab + KommunikationTab"
      via: "TabsContent value='routing' + value='kommunikation'"
      pattern: "value=\"routing\"|value=\"kommunikation\""
    - from: "src/app/(dashboard)/konfiguration/page.tsx"
      to: "routing_rules + vip_numbers + comm_rules + message_templates + send_log"
      via: "Promise.all with 14 supabase queries"
      pattern: "\\.from\\('(routing_rules|vip_numbers|comm_rules|message_templates|send_log)'\\)"
---

<objective>
Verify the complete Phase 6 implementation: production build passes, all 7 Konfiguration tabs are functional, and a smoke test checklist confirms end-to-end behavior. No new code is written unless the build reveals errors that must be fixed.

Purpose: Gate that prevents shipping broken code. The build must pass before Phase 6 is declared complete.
Output: npm run build success, human-verified smoke test, Phase 6 COMPLETE.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-routing-communication/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Production build gate + TypeScript verification</name>
  <files></files>
  <read_first>
    - src/components/config/KonfigurationTabs.tsx (verify all 7 tab triggers present, all component imports present)
    - src/app/(dashboard)/konfiguration/page.tsx (verify Promise.all has routing_rules, vip_numbers, comm_rules, message_templates, send_log queries; all 14 queries present)
    - src/lib/types/index.ts (verify Phase 06 section with all 5 Row interfaces and 6 union types)
  </read_first>
  <action>
    Step 1 — TypeScript strict check: run npx tsc --noEmit. If it reports errors, fix them before proceeding. Common issues to check:
    - Missing props on KonfigurationTabs (messageTemplates, sendLog added by Plan 06-04 — must be passed from page.tsx)
    - CommRuleRow field mismatch (template_id is uuid | null — ensure TS types match)
    - SendLogRow fields used in KommunikationTab match the interface
    - RoutingRuleRow condition_value/action_value typed as Record<string,unknown> — ensure no implicit any in JSX renderers (use Boolean(field) not field for unknown shorts)

    Step 2 — Production build: run npm run build. Fix any build errors before declaring success. Likely build issues:
    - Missing 'use client' on components using useState/useTransition
    - 'server-only' imported in a component file (should only be in action files)
    - NEXT_PUBLIC env vars not available at build time (not an issue for this phase — no new env vars)
    - Missing module resolution for new action files (check import paths use @/ alias consistently)

    Step 3 — Grep guard: verify no raw recipient column exposed in UI layer:
    grep -rn "recipient_email\|recipient_phone\|recipient_raw" src/ — must return 0 matches
    grep -n "recipient_masked" src/components/config/KommunikationTab.tsx — must return at least 1 match (the column being rendered)

    Step 4 — Verify all 8 requirement IDs are covered:
    - ROUTE-01: grep "condition_type\|RoutingConditionType" src/lib/actions/routing.ts — present
    - ROUTE-02: grep "action_type\|RoutingActionType" src/lib/actions/routing.ts — present
    - ROUTE-03: grep "vip_numbers\|VipNumberRow" src/components/config/RoutingTab.tsx — present
    - COMM-01: grep "direction.*intern\|intern.*direction" src/lib/actions/communication.ts — present
    - COMM-02: grep "direction.*patient\|patient.*direction" src/lib/actions/communication.ts — present
    - COMM-03: grep "privacy_class\|retry_interval_minutes\|time_window_from" src/lib/actions/communication.ts — all COMM-03 fields present
    - COMM-04: grep "send_log\|recipient_masked" src/components/config/KommunikationTab.tsx — present
    - COMM-05: grep "message_templates\|createTemplateAction" src/components/config/KommunikationTab.tsx — present

    If any grep fails: identify the missing coverage, fix the source file, re-run tsc and build.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - npm run build exits 0 (production build successful)
    - grep -rn "recipient_email\|recipient_phone\|recipient_raw" src/ returns 0 matches
    - grep "recipient_masked" src/components/config/KommunikationTab.tsx returns at least 1 match
    - All 8 requirement grep checks pass
    - KonfigurationTabs.tsx has 7 TabsTrigger elements: oeffnungszeiten, terminarten, begruessung, vertretung, medikamente, routing, kommunikation
  </acceptance_criteria>
  <done>npm run build exits 0. TypeScript compiles clean. DSGVO recipient masking verified. All 8 Phase 6 requirements have implementation coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Smoke test — all 7 Konfiguration tabs functional</name>
  <what-built>Phase 6 complete: 5 DB tables, 20 RLS policies, 13 Server Actions (6 routing + 4 comm + 3 templates), RoutingTab, KommunikationTab with 4 sub-tabs, all wired into the existing /konfiguration page with 2 new top-level tabs.</what-built>
  <how-to-verify>
    Start the dev server: npm run dev
    Log in to the dashboard as a user with operator or ordination_admin role.
    Navigate to /konfiguration.

    1. EXISTING TABS (verify not broken):
       - Click "Öffnungszeiten" — weekly hours grid renders
       - Click "Terminarten" — appointment types table renders
       - Click "Begrüßungstexte" — greeting text editor renders
       - Click "Vertretung" — deputy doctors table renders
       - Click "Medikamente" — medication list renders

    2. ROUTING TAB (?tab=routing):
       a. Click "Routing" tab — URL changes to ?tab=routing
       b. "Routing-Regeln" section visible with empty state "Noch keine Routing-Regeln konfiguriert."
       c. Click "Routing-Regel hinzufügen" — Sheet opens with right width (540px)
       d. Enter Bezeichnung, select Bedingungstyp "Telefonnummer" — phone input appears
       e. Switch Bedingungstyp to "Zeitraum" — phone input hides, weekday checkboxes + time inputs appear
       f. Switch Bedingungstyp to "Modus" — mode Select appears
       g. Select Aktion "An Nummer weiterleiten" — forwarding number input appears
       h. Select Aktion "Eigener Prompt" — prompt textarea appears
       i. Fill all required fields, click "Regel speichern" — rule appears in table
       j. Toggle Active switch on the new rule — optimistic update, no reload
       k. Click Edit (Pencil) — Sheet opens pre-filled with existing rule data
       l. Click Delete (Trash2) — confirmation Dialog appears; confirm → rule removed
       m. VIP/WIP-Nummern section: enter phone number + label, click "Hinzufügen" — row appears in VIP table
       n. Click Delete on VIP row — confirmation Dialog; confirm → VIP row removed

    3. KOMMUNIKATION TAB (?tab=kommunikation):
       a. Click "Kommunikation" tab — URL changes to ?tab=kommunikation
       b. "Interne Benachrichtigungen" sub-tab is default (active)
       c. Empty state "Noch keine internen Benachrichtigungsregeln konfiguriert."
       d. Click "Regel hinzufügen" — Sheet opens with all COMM-03 fields
       e. Select Ereignistyp "Neuer Inbox-Fall", Kanal "E-Mail", enter email address
       f. Set Priorität, time window, max retries, Datenschutzklasse
       g. Save — rule appears in intern table
       h. Toggle active switch — optimistic update
       i. Click "Patientenbenachrichtigungen" sub-tab — patient rules list (empty)
       j. Click "Regel hinzufügen" on patient sub-tab — Sheet opens with patient-only channels (E-Mail, SMS — no Inbox or Telegram in Kanal Select)
       k. Click "Nachrichtenvorlagen" sub-tab — template table (empty state)
       l. Click "Vorlage erstellen" — Sheet opens (wider, 600px) with all fields
       m. Select Kanal "E-Mail" — Betreff field appears
       n. Enter body text with "{{patient_name}}" — Preview updates live showing "Muster Maria"
       o. Click a variable button (e.g. {{appointment_date}}) — variable inserted at cursor in textarea
       p. Save — template appears in template table with truncated preview in Vorschau column
       q. Click "Versandprotokoll" sub-tab — empty state "Noch keine Versandversuche protokolliert." with filter bar visible above table
       r. Filter controls (event type, channel, status, date range) are visible and functional (even if empty)

    4. SECURITY SMOKE TEST (as viewer role if available):
       - Navigate to /konfiguration?tab=routing
       - "Routing-Regel hinzufügen" button should NOT appear (hasEditRight=false for viewer)
       - Table renders in read-only mode (no edit/delete action buttons)

  </how-to-verify>
  <resume-signal>Type "verified" after all smoke tests pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration test | No new attack surface introduced in this plan — verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-V-01 | Information Disclosure | send_log raw recipient exposure in build output | mitigate | grep -rn "recipient_email\|recipient_raw" src/ check in Task 1 — must return 0 matches before build passes |
| T-06-V-02 | Elevation | hasEditRight not enforced in UI layer | mitigate | Smoke test step 4 verifies viewer role sees no add/edit/delete controls — role check at Server Action is the security gate, UI is defense-in-depth |
| T-06-V-03 | Tampering | Phase 5 tab functionality broken by Phase 6 extensions | accept | Smoke test step 1 verifies all 5 Phase 5 tabs still render correctly after KonfigurationTabs and page.tsx were extended |
</threat_model>

<verification>
Phase 6 complete when:
- npm run build exits 0
- npx tsc --noEmit exits 0
- All 7 Konfiguration tabs visible and functional (smoke test step 1-3 pass)
- Viewer role cannot see add/edit/delete controls (smoke test step 4)
- No raw recipient column rendered anywhere in UI (grep confirms)
- All 8 Phase 6 requirement IDs have verified implementation: ROUTE-01..03, COMM-01..05
</verification>

<success_criteria>
- Production build passes (npm run build exits 0)
- All 7 KonfigurationTabs tabs navigable without errors
- Routing tab: rule CRUD functional (create with conditional fields, toggle, edit, delete), VIP numbers functional
- Kommunikation tab: 4 sub-tabs functional (intern rules, patient rules, templates with variable insert + preview, send log with DSGVO masking + filters)
- No regression in Phase 5 tabs
- Human smoke test confirmed
</success_criteria>

<output>
Create .planning/phases/06-routing-communication/06-05-SUMMARY.md when done.
Update ROADMAP.md: Phase 6 marked complete, Plans list updated with 5 plans.
Update STATE.md: current_phase updated, Phase 6 progress marked COMPLETE, ROUTE-01..03 + COMM-01..05 all marked delivered.
</output>
