---
phase: 06-routing-communication
plan: 04
type: execute
wave: 3
depends_on:
  - 06-03
files_modified:
  - src/lib/actions/templates.ts
  - src/components/config/KommunikationTab.tsx
autonomous: true
requirements:
  - COMM-04
  - COMM-05

must_haves:
  truths:
    - "Nachrichtenvorlagen sub-tab is fully functional: user can create, edit, delete message templates with name, channel, language, subject (email), body with variable insert buttons, and live preview"
    - "Versandprotokoll sub-tab is fully functional: read-only paginated table showing send log with masked recipients, event type, channel, template name/version, status badge, error reason, and filter controls"
    - "Template variable insert buttons insert the variable at cursor position in the textarea"
    - "Live preview substitutes example values into the template body on every keystroke"
    - "send_log recipient_masked column is always shown — no unmasking option exists"
    - "Send log filter controls (event type, channel, status, date range) work client-side on the loaded page data"
  artifacts:
    - path: "src/lib/actions/templates.ts"
      provides: "createTemplateAction, updateTemplateAction, deleteTemplateAction"
      exports:
        - "createTemplateAction"
        - "updateTemplateAction"
        - "deleteTemplateAction"
    - path: "src/components/config/KommunikationTab.tsx"
      provides: "Nachrichtenvorlagen sub-tab replaces placeholder with full template CRUD + Sheet; Versandprotokoll sub-tab replaces placeholder with read-only send log table + filters"
  key_links:
    - from: "src/components/config/KommunikationTab.tsx"
      to: "src/lib/actions/templates.ts"
      via: "useActionState / useTransition"
      pattern: "createTemplateAction|updateTemplateAction|deleteTemplateAction"
    - from: "src/app/(dashboard)/konfiguration/page.tsx"
      to: "message_templates + send_log"
      via: "supabase.from('message_templates').select / supabase.from('send_log').select"
      pattern: "\\.from\\('message_templates'\\)|\\.from\\('send_log'\\)"
---

<objective>
Complete the Kommunikation module by replacing the Plan 06-03 placeholders: (1) Nachrichtenvorlagen sub-tab with full template CRUD including variable insert and live preview (COMM-05); (2) Versandprotokoll sub-tab with a read-only paginated send log table with DSGVO-masked recipients and filter controls (COMM-04).

Purpose: Practices can now manage message templates used by comm rules and audit every send attempt with full log visibility. This completes all 8 Phase 6 requirements.
Output: templates.ts (3 Server Actions), updated KommunikationTab.tsx (sub-tabs 3 and 4 implemented), page.tsx extended with message_templates + send_log queries.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-routing-communication/06-UI-SPEC.md
@.planning/phases/06-routing-communication/06-03-SUMMARY.md
</context>

<interfaces>
From src/app/actions/update-inbox-status.ts — canonical Server Action auth sequence (copy exactly):
- 'use server', import 'server-only'
- ALLOWED_ROLES = ['operator', 'ordination_admin'] as const
- Zod parse → await createServerClient() → getUser() → tenantId → role gate → createServiceRoleClient() (sync) → DB op → return state

From src/lib/types/index.ts (Phase 06 section from Plan 06-01):
- MessageTemplateRow: { id, tenant_id, name, channel ('email'|'sms'|'telegram'), language_code, subject (string|null), body, version (number), created_at, updated_at }
- SendLogRow: { id, tenant_id, comm_rule_id (string|null), event_type, channel, recipient_masked, template_name (string|null), template_version (number|null), status (SendLogStatus), error_reason (string|null), sent_at, created_at }
- SendLogStatus = 'pending' | 'delivered' | 'failed'

From src/components/config/KommunikationTab.tsx (written by Plan 06-03 — read current state):
- Props interface: KommunikationTabProps { tenantId, hasEditRight, initialCommRules }
- Extend props with: initialTemplates: MessageTemplateRow[]; initialSendLog: SendLogRow[]
- Replace placeholder content in sub-tab "vorlagen" and sub-tab "protokoll"

From src/app/(dashboard)/konfiguration/page.tsx (may already have routing + comm queries — read current state):
- Add message_templates and send_log queries to Promise.all
- Pass as props to KonfigurationTabs → KommunikationTab

Template variables per UI-SPEC: {{patient_name}}, {{appointment_date}}, {{appointment_type}}, {{practice_name}}, {{callback_phone}}

Example values for preview (hardcoded — never real patient data):
- patient_name → "Muster Maria"
- appointment_date → "Montag, 12. März um 9 Uhr"
- appointment_type → "Allgemeinuntersuchung"
- practice_name → "Ordination Beispiel"
- callback_phone → "+43 1 234 567"

shadcn components installed (no new installs required per UI-SPEC):
- Popover, PopoverContent, PopoverTrigger, Calendar — from Phase 5 (available)
- All Sheet, Table, Badge, Button, Input, Select, Textarea, Tooltip, TooltipContent, TooltipProvider, TooltipTrigger from existing installs

CHANNEL_LABELS constant is already defined in KommunikationTab.tsx (added in Plan 06-03). Reuse it.
INTERNAL_EVENT_LABELS and PATIENT_EVENT_LABELS are also already defined — combine for send log event display.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions for message templates</name>
  <files>src/lib/actions/templates.ts</files>
  <read_first>
    - src/app/actions/update-inbox-status.ts (full file — canonical auth sequence)
    - src/lib/actions/routing.ts (same file structure pattern to follow)
    - src/lib/types/index.ts (MessageTemplateRow — confirm column names, version field)
  </read_first>
  <behavior>
    - createTemplateAction: receives { name, channel, language_code, subject?, body } — Zod validates; inserts one message_templates row with version=1; returns { success: true, id: string } or { error: string }
    - updateTemplateAction: receives { id, name, channel, language_code, subject?, body } — Zod validates; updates row with .eq('id').eq('tenant_id'); increments version by 1 (version = version + 1 via SQL or fetch+increment); returns state
    - deleteTemplateAction: receives { id } — checks if any active comm_rules reference this template_id; if yes, deactivates those rules (sets active=false) before deleting the template; returns { success: true, deactivated_rules: number } or error
  </behavior>
  <action>
    Create src/lib/actions/templates.ts with 'use server' directive and import 'server-only'.

    File-level constant: const ALLOWED_ROLES = ['operator', 'ordination_admin'] as const

    Export state interfaces:
    - TemplateActionState: { success?: boolean; error?: string; id?: string; deactivated_rules?: number }

    Zod schemas:
    - templateBaseSchema: z.object({ name: z.string().min(1, 'Dieses Feld ist erforderlich.'), channel: z.enum(['email','sms','telegram']), language_code: z.enum(['de','en']).default('de'), subject: z.string().optional().nullable(), body: z.string().min(1, 'Dieses Feld ist erforderlich.') }).refine(d => d.channel !== 'email' || (d.subject && d.subject.length > 0), { message: 'Betreff ist für E-Mail-Vorlagen erforderlich.', path: ['subject'] })
    - createTemplateSchema: templateBaseSchema (no id)
    - updateTemplateSchema: templateBaseSchema.extend({ id: z.string().uuid() })
    - deleteTemplateSchema: z.object({ id: z.string().uuid() })

    createTemplateAction: auth sequence → serviceClient.from('message_templates').insert({ ...parsed.data, tenant_id: tenantId, version: 1 }).select('id').single() → return { success: true, id: data.id }

    updateTemplateAction: auth sequence → first fetch current version with serviceClient.from('message_templates').select('version').eq('id').eq('tenant_id').single() → then update with { ...parsed.data, tenant_id: tenantId, version: currentVersion + 1, updated_at: new Date().toISOString() } using both eq filters.

    deleteTemplateAction: auth sequence → 
    1. serviceClient.from('comm_rules').update({ active: false, updated_at: now }).eq('template_id', id).eq('tenant_id', tenantId) → count deactivated rows from result
    2. serviceClient.from('message_templates').delete().eq('id', id).eq('tenant_id', tenantId)
    3. Return { success: true, deactivated_rules: count }

    All error paths: console.error('[actionName] DB error:', dbError)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function" src/lib/actions/templates.ts outputs 3
    - grep "createTemplateAction\|updateTemplateAction\|deleteTemplateAction" src/lib/actions/templates.ts — all 3 present
    - grep "\.eq('tenant_id'" src/lib/actions/templates.ts — every delete/update has tenant_id filter
    - grep "createServiceRoleClient()" src/lib/actions/templates.ts has no await (sync)
    - grep "await createServerClient()" src/lib/actions/templates.ts confirms await
    - grep "server-only" src/lib/actions/templates.ts — import present
    - grep "comm_rules.*active.*false\|deactivat" src/lib/actions/templates.ts — deleteTemplateAction deactivates dependent rules
    - grep "version.*\+.*1\|version.*increment" src/lib/actions/templates.ts — updateTemplateAction increments version
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>3 Server Actions: createTemplateAction (version=1), updateTemplateAction (version+1), deleteTemplateAction (deactivates dependent comm_rules before delete). TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Nachrichtenvorlagen + Versandprotokoll sub-tabs in KommunikationTab, extend page.tsx</name>
  <files>
    src/components/config/KommunikationTab.tsx,
    src/app/(dashboard)/konfiguration/page.tsx,
    src/components/config/KonfigurationTabs.tsx
  </files>
  <read_first>
    - src/components/config/KommunikationTab.tsx (full current file from Plan 06-03 — understand existing state, constants, shared imports; replace only placeholder divs in sub-tabs "vorlagen" and "protokoll")
    - src/app/(dashboard)/konfiguration/page.tsx (full current file — extend Promise.all with message_templates + send_log queries)
    - src/components/config/KonfigurationTabs.tsx (full current file — extend props with initialTemplates + initialSendLog)
    - src/lib/actions/templates.ts (just written — verify exports before importing)
    - src/lib/types/index.ts (MessageTemplateRow, SendLogRow, SendLogStatus)
    - .planning/phases/06-routing-communication/06-UI-SPEC.md sub-tab 3 (Nachrichtenvorlagen) and sub-tab 4 (Versandprotokoll) sections — full interaction contract
  </read_first>
  <action>
    STEP A — Update src/components/config/KommunikationTab.tsx:

    Extend KommunikationTabProps with:
    - initialTemplates: MessageTemplateRow[]
    - initialSendLog: SendLogRow[]

    Add template state variables:
    - templates: MessageTemplateRow[] (initialized from initialTemplates)
    - templateSheetOpen: boolean
    - editingTemplate: MessageTemplateRow | null
    - templateDeleteTarget: MessageTemplateRow | null
    - templateBodyRef: React.RefObject<HTMLTextAreaElement> (for cursor-position variable insertion)

    Add send log state variables:
    - sendLog: SendLogRow[] (initialized from initialSendLog)
    - slFilterEvent: string ('all')
    - slFilterChannel: string ('all')
    - slFilterStatus: string ('all')
    - slFilterFrom: Date | undefined
    - slFilterTo: Date | undefined
    - slPage: number (1) — for "Ältere laden" cursor pagination (25 rows per page)

    Import from templates.ts: createTemplateAction, updateTemplateAction, deleteTemplateAction

    SUB-TAB "vorlagen" — Nachrichtenvorlagen (COMM-05):
    Replace placeholder div with full implementation.

    "Vorlage erstellen" Button (variant="outline" size="sm" icon Plus) — only if hasEditRight.
    Table columns: Name | Kanal | Sprache | Vorschau | Aktionen
    - Name: text-sm — template.name
    - Kanal: Badge variant="outline" with channel icon (Mail for email, MessageCircle for sms, MessageSquare for telegram) + CHANNEL_LABELS label
    - Sprache: Badge variant="outline" — template.language_code (e.g. "de")
    - Vorschau: text-sm text-muted-foreground — template.body truncated to 60 chars with "..." if longer; full text in Tooltip (TooltipProvider > Tooltip > TooltipTrigger + TooltipContent)
    - Aktionen: Pencil + Trash2 ghost icon buttons (only if hasEditRight)
    Empty state: "Noch keine Nachrichtenvorlagen erstellt. Erstellen Sie Ihre erste Vorlage."

    NachrichtenvorlageSheet (inline):
    SheetContent className="w-[600px] sm:max-w-[600px]" (wider per UI-SPEC)
    Form with useActionState — createTemplateAction or updateTemplateAction

    Fields:
    - Vorlagenname: Input placeholder "Terminbestätigung Deutsch" required
    - Kanal Select: email|sms|telegram with German labels
    - Subject (conditional — only when channel='email'): Input placeholder "Ihre Terminbestätigung — {{practice_name}}"
    - Sprache Select: "Deutsch (de)"|"Englisch (en)" → de|en
    - Nachrichtentext: Textarea min-h-48 ref={templateBodyRef} — full template body

    Variable help block (bg-muted rounded-md p-3 mt-2):
    - Header: "Verfügbare Variablen — Klicken zum Einfügen" (text-xs font-semibold text-muted-foreground mb-2)
    - 5 Button elements (variant="outline" size="sm" className="font-mono text-xs h-7"):
      - {{patient_name}} aria-label="Variable {{patient_name}} einfügen"
      - {{appointment_date}} aria-label="Variable {{appointment_date}} einfügen"
      - {{appointment_type}} aria-label="Variable {{appointment_type}} einfügen"
      - {{practice_name}} aria-label="Variable {{practice_name}} einfügen"
      - {{callback_phone}} aria-label="Variable {{callback_phone}} einfügen"
    - Click handler: insert variable at cursor position in textarea using templateBodyRef.current selection positions:
      const textarea = templateBodyRef.current
      if (!textarea) return
      const start = textarea.selectionStart
      const end = textarea.selectionEnd
      const newValue = bodyValue.slice(0, start) + variable + bodyValue.slice(end)
      setBodyValue(newValue)
      Then set cursor after insertion: requestAnimationFrame(() => { textarea.focus(); textarea.setSelectionRange(start + variable.length, start + variable.length) })

    Preview section (below textarea):
    - Label: "Vorschau (mit Beispieldaten)" (text-xs font-semibold uppercase tracking-wide text-muted-foreground)
    - Preview box (bg-muted/50 border rounded-md p-3 text-sm text-muted-foreground):
      live renders bodyValue with example substitutions applied via string replace:
      EXAMPLE_VALUES: Record<string, string> = { '{{patient_name}}': 'Muster Maria', '{{appointment_date}}': 'Montag, 12. März um 9 Uhr', '{{appointment_type}}': 'Allgemeinuntersuchung', '{{practice_name}}': 'Ordination Beispiel', '{{callback_phone}}': '+43 1 234 567' }
      Preview computed: Object.entries(EXAMPLE_VALUES).reduce((text, [key, val]) => text.replaceAll(key, val), bodyValue)
    - Update trigger: re-renders on bodyValue state change (already reactive)

    CTA: "Vorlage speichern" with Loader2 when isPending
    On state.success: update templates array (add or replace); setTemplateSheetOpen(false)

    Delete template Dialog:
    - Title "Vorlage entfernen"
    - Body "Möchten Sie die Vorlage '{templateDeleteTarget?.name}' entfernen? Kommunikationsregeln, die diese Vorlage verwenden, werden deaktiviert."
    - Confirm destructive "Entfernen"
    - Cancel outline "Abbrechen"
    - On confirm: call deleteTemplateAction({id}) in startTransition; on success: remove from templates; if result.deactivated_rules > 0: show brief success message mentioning deactivated rules count

    SUB-TAB "protokoll" — Versandprotokoll (COMM-04):
    Replace placeholder div with full implementation.

    Filter bar (horizontal flex row, gap-2):
    - Select slFilterEvent: "Alle Ereignisse" + all event types from combined INTERNAL_EVENT_LABELS + PATIENT_EVENT_LABELS
    - Select slFilterChannel: "Alle Kanäle" + all CommChannel values with German labels
    - Select slFilterStatus: "Alle Status" | "Zugestellt" (delivered) | "Ausstehend" (pending) | "Fehlgeschlagen" (failed)
    - Von date: Popover + Calendar (mode="single") — sets slFilterFrom
    - Bis date: Popover + Calendar (mode="single") — sets slFilterTo
    Filters auto-apply on change (no explicit Apply button per UI-SPEC).

    Filtered log computation: useMemo derived from sendLog filtered by all active filters client-side.
    Paginated display: filteredLog.slice(0, slPage * 25) — "Ältere laden" Button below table increments slPage.

    Table aria-label="Versandprotokoll"
    Columns: Zeitpunkt | Ereignis | Kanal | Empfänger | Vorlage | Status | Fehler
    - Zeitpunkt: new Intl.DateTimeFormat('de-AT', { day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' }).format(new Date(log.sent_at))
    - Ereignis: INTERNAL_EVENT_LABELS[log.event_type] ?? PATIENT_EVENT_LABELS[log.event_type] ?? log.event_type
    - Kanal: channel icon (aria-hidden) + label text
    - Empfänger: <span className="font-mono text-sm text-muted-foreground" aria-label="Maskierte Empfängeradresse">{log.recipient_masked}</span>
    - Vorlage: text-sm text-muted-foreground — "{log.template_name} v{log.template_version}" or "—" if null
    - Status: Badge with semantic colors: delivered → bg-green-100 text-green-600; pending → bg-yellow-100 text-yellow-600; failed → bg-red-100 text-red-600; labels: Zugestellt|Ausstehend|Fehlgeschlagen
    - Fehler: text-xs text-muted-foreground — log.error_reason or "—"
    Empty state: "Noch keine Versandversuche protokolliert."
    No edit controls on this sub-tab (read-only per UI-SPEC).

    STEP B — Extend src/app/(dashboard)/konfiguration/page.tsx:
    Read current file state, then add:
    - MessageTemplateRow, SendLogRow to imports from @/lib/types
    - In Promise.all: add supabase.from('message_templates').select('*').eq('tenant_id', tenantId).order('name') and supabase.from('send_log').select('*').eq('tenant_id', tenantId).order('created_at', { ascending: false }).limit(100)
    - Destructure: { data: messageTemplatesRaw }, { data: sendLogRaw }
    - Pass to KonfigurationTabs → flows to KommunikationTab via updated KonfigurationTabs props

    STEP C — Extend src/components/config/KonfigurationTabs.tsx props:
    Read current file state, then:
    - Add MessageTemplateRow, SendLogRow to imports from @/lib/types
    - Extend KonfigurationTabsProps with: messageTemplates: MessageTemplateRow[]; sendLog: SendLogRow[]
    - Add destructuring and pass to KommunikationTab: initialTemplates={messageTemplates} initialSendLog={sendLog}
    - Update KonfigurationTabs call in page.tsx to include messageTemplates and sendLog props
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "createTemplateAction\|updateTemplateAction\|deleteTemplateAction" src/components/config/KommunikationTab.tsx — all 3 template actions imported and used
    - grep "templateBodyRef\|selectionStart\|setSelectionRange" src/components/config/KommunikationTab.tsx — cursor-position variable insertion implemented
    - grep "replaceAll\|EXAMPLE_VALUES\|Muster Maria" src/components/config/KommunikationTab.tsx — live preview with example substitution present
    - grep "aria-label=\"Versandprotokoll\"" src/components/config/KommunikationTab.tsx — send log table has aria-label
    - grep "aria-label=\"Maskierte Empfängeradresse\"" src/components/config/KommunikationTab.tsx — masked recipient cell has aria-label
    - grep "recipient_masked" src/components/config/KommunikationTab.tsx — only masked column used (no raw recipient)
    - grep "message_templates\|send_log" src/app/(dashboard)/konfiguration/page.tsx — both queries in Promise.all
    - grep "slFilterEvent\|slFilterChannel\|slFilterStatus" src/components/config/KommunikationTab.tsx — filter state variables present
    - grep "Ältere laden" src/components/config/KommunikationTab.tsx — pagination button present
    - grep "deactivated_rules\|Kommunikationsregeln.*deaktiviert" src/components/config/KommunikationTab.tsx — deleteTemplate deactivation feedback present
    - grep "role=\"alert\"" src/components/config/KommunikationTab.tsx — error paragraphs accessible
    - grep "aria-label.*Variable.*einfügen" src/components/config/KommunikationTab.tsx — variable insert buttons have aria-label
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Nachrichtenvorlagen sub-tab: full template CRUD with variable insert at cursor + live preview. Versandprotokoll sub-tab: read-only table with DSGVO-masked recipients + client-side filters + pagination. page.tsx extended with message_templates + send_log queries. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NachrichtenvorlageSheet (browser) → Server Actions | Template name, body, channel inputs are untrusted |
| Versandprotokoll (browser) | Read-only display — no write surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-T-01 | Spoofing | createTemplateAction, updateTemplateAction | mitigate | tenant_id from user.app_metadata only — never from formData (STATE.md C2) |
| T-06-T-02 | Tampering | deleteTemplateAction | mitigate | .eq('id').eq('tenant_id') both filters — service-role bypasses RLS |
| T-06-T-03 | Elevation | Template mutations without role gate | mitigate | All 3 Server Actions check ara_role IN ('operator','ordination_admin') |
| T-06-T-04 | Information Disclosure | Template preview uses real patient data | mitigate | Preview substitutes only hardcoded EXAMPLE_VALUES (Muster Maria, Ordination Beispiel etc.) — no DB fetch, no real patient data ever used |
| T-06-T-05 | Information Disclosure | send_log recipient shown unmasked | mitigate | Versandprotokoll reads only recipient_masked column from DB (no raw column exists in schema). No permission toggle for unmasking — DSGVO hard constraint. UI aria-label="Maskierte Empfängeradresse" on cell. |
| T-06-T-06 | Tampering | Template delete without deactivating dependent comm_rules | mitigate | deleteTemplateAction first deactivates all active comm_rules referencing the template_id, then deletes — no orphaned active rules |
| T-06-T-07 | Tampering | Email template without subject | mitigate | Zod .refine() in templateBaseSchema: channel='email' requires subject non-empty |
</threat_model>

<verification>
After this plan executes:
- Visit /konfiguration?tab=kommunikation, click "Nachrichtenvorlagen" sub-tab: template table (empty state), "Vorlage erstellen" button visible
- Click "Vorlage erstellen": Sheet opens with all fields, variable buttons, preview
- Click "Versandprotokoll" sub-tab: empty state "Noch keine Versandversuche protokolliert." with filter bar above
- npx tsc --noEmit exits 0
- grep -c "export async function" src/lib/actions/templates.ts = 3
- No mention of ElevenLabs/n8n in KommunikationTab.tsx
</verification>

<success_criteria>
- 3 Server Actions in templates.ts: createTemplateAction, updateTemplateAction (version+1), deleteTemplateAction (deactivates dependent comm_rules)
- Nachrichtenvorlagen sub-tab: template list, NachrichtenvorlageSheet (all fields per COMM-05), variable insert at cursor, live preview with EXAMPLE_VALUES
- Versandprotokoll sub-tab: read-only table, recipient_masked only (no unmask option), German datetime, status badges, filter controls, "Ältere laden" pagination
- DSGVO: recipient_masked is the only column read — enforced in DB schema (Plan 06-01) and in this component
- TypeScript strict: no errors
</success_criteria>

<output>
Create .planning/phases/06-routing-communication/06-04-SUMMARY.md when done.
</output>
