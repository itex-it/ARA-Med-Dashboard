---
phase: 07-statistics-user-mgmt
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/app/(dashboard)/statistiken/page.tsx
  - src/components/statistics/KpiCard.tsx
  - src/components/statistics/DailyVolumeChart.tsx
  - src/components/statistics/IntentsTable.tsx
  - src/components/statistics/SavedTimeCard.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05

must_haves:
  truths:
    - "User can view call volume with total/resolved/forwarded breakdown for last 7/30/90 days"
    - "User can view average call duration in seconds and formatted as 'X Min Y Sek'"
    - "User can view auto-resolution rate (%) and forwarding rate (%)"
    - "User can view count of open tasks, booked appointments, and prescription requests"
    - "User can view top-8 intents by frequency as a ranked table"
    - "User can view estimated saved phone time (resolved calls × 5 min) as 'ca. X Std. Y Min.'"
    - "Time period filter (7 Tage / 30 Tage / 90 Tage) works via URL searchParams — no JavaScript required"
    - "All queries use service-role client with explicit .eq('tenant_id', tenantId) filter"
  artifacts:
    - path: "src/app/(dashboard)/statistiken/page.tsx"
      provides: "Full Server Component: fetches all stats data, renders KPI grid + volume chart + intents table + saved time"
      min_lines: 100
    - path: "src/components/statistics/KpiCard.tsx"
      provides: "Reusable KPI card with label, value, optional sub-value"
    - path: "src/components/statistics/DailyVolumeChart.tsx"
      provides: "CSS-based bar chart for daily call volume (no recharts dependency)"
    - path: "src/components/statistics/IntentsTable.tsx"
      provides: "Ranked table of top intents with count and percentage bar"
    - path: "src/components/statistics/SavedTimeCard.tsx"
      provides: "Saved time card with formula display and formatted total"
  key_links:
    - from: "src/app/(dashboard)/statistiken/page.tsx"
      to: "call_log + call_actions + inbox_items tables"
      via: "service-role client queries with tenant_id filter"
      pattern: "\\.from\\('call_log'\\)|\\.from\\('call_actions'\\)|\\.from\\('inbox_items'\\)"
    - from: "src/app/(dashboard)/statistiken/page.tsx"
      to: "src/components/statistics/*.tsx"
      via: "import and render in page"
      pattern: "KpiCard|DailyVolumeChart|IntentsTable|SavedTimeCard"
---

<objective>
Build the full Statistics page for ARA-Med. Practice management can view call KPIs, saved-time estimates, top intents, and outcome breakdowns — all scoped to their tenant.

Purpose: STAT-01..05 requirements delivered via a single Server Component page that runs SQL aggregations at request time. No new DB tables — all data from existing call_log, call_actions, inbox_items. No recharts dependency — CSS bar charts only.
Output: statistiken/page.tsx (full) + 4 statistics components.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key analogs:
- src/app/(dashboard)/konfiguration/page.tsx (Server Component with Promise.all queries, service-role client pattern, props to client components)
- src/app/(dashboard)/inbox/page.tsx (Server Component auth sequence)
- src/lib/supabase/server.ts (createServerClient, createServiceRoleClient — sync, no await)
- src/lib/types/index.ts Phase 07 section (StatsSummary, DailyCallVolume, IntentFrequency types just added in 07-01)

Existing tables queried (read migration schemas for column names):
- call_log: id, tenant_id, status ('completed'|'forwarded'|'failed'|'abandoned'|'active'), duration_seconds, intent_main, created_at
- call_actions: id, tenant_id, call_log_id, action_type ('appointment_booked'|'prescription_ordered'|...), created_at
- inbox_items: id, tenant_id, case_type, status ('open'|'in_progress'|'resolved'|'archived'), created_at
</context>

<interfaces>
Auth sequence (identical to all other pages):
  const supabase = await createServerClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/auth/login')
  const tenantId = (user.app_metadata?.tenant_id as string) ?? ''
  const serviceClient = createServiceRoleClient()  // sync, no await

searchParams for time filter:
  type: { tage?: string }
  Default: 30 days
  Parse: const days = parseInt(searchParams.tage ?? '30', 10); clamp to [7, 90]
  startDate: new Date(Date.now() - days * 24 * 60 * 60 * 1000).toISOString()

Query 1 — Call log batch (all within period):
  serviceClient.from('call_log')
    .select('created_at, status, duration_seconds, intent_main')
    .eq('tenant_id', tenantId)
    .gte('created_at', startDate)
    .order('created_at', { ascending: true })

Query 2 — Booked appointments count:
  serviceClient.from('call_actions')
    .select('id', { count: 'exact', head: true })
    .eq('tenant_id', tenantId)
    .eq('action_type', 'appointment_booked')
    .gte('created_at', startDate)

Query 3 — Prescription requests count:
  serviceClient.from('call_actions')
    .select('id', { count: 'exact', head: true })
    .eq('tenant_id', tenantId)
    .eq('action_type', 'prescription_ordered')
    .gte('created_at', startDate)

Query 4 — Open tasks count:
  serviceClient.from('inbox_items')
    .select('id', { count: 'exact', head: true })
    .eq('tenant_id', tenantId)
    .in('status', ['open', 'in_progress'])

Query 5 — Emergency cases (last 30 days always, not period-filtered):
  serviceClient.from('inbox_items')
    .select('id', { count: 'exact', head: true })
    .eq('tenant_id', tenantId)
    .eq('case_type', 'emergency')
    .gte('created_at', thirtyDaysAgo)

Run all 5 in Promise.all for performance.

JavaScript aggregations from Query 1 result:
  totalCalls = callLog.length
  resolvedCalls = callLog.filter(r => r.status === 'completed')
  forwardedCalls = callLog.filter(r => r.status === 'forwarded')
  failedCalls = callLog.filter(r => r.status === 'failed' || r.status === 'abandoned')
  avgDurationSeconds = mean of duration_seconds where status = 'completed' (exclude nulls and non-completed)
  resolutionRate = resolvedCalls.length / totalCalls (0 if totalCalls === 0)
  forwardingRate = forwardedCalls.length / totalCalls (0 if totalCalls === 0)
  estimatedSavedMinutes = resolvedCalls.length × 5  (5 min per resolved call — conservative estimate)
  dailyVolume: group callLog by date string (created_at.split('T')[0])
  topIntents: count by intent_main, sort desc, take top 8, exclude null intent_main

Saved time display helper:
  const hours = Math.floor(estimatedSavedMinutes / 60)
  const mins = estimatedSavedMinutes % 60
  → "ca. {hours} Std. {mins} Min." or "ca. {mins} Min." if hours === 0

KpiCard props:
  label: string
  value: string | number
  sub?: string
  description?: string

DailyVolumeChart props:
  data: DailyCallVolume[]
  maxCount: number (max of data[].total for bar scaling)

IntentsTable props:
  intents: IntentFrequency[]
  total: number (for percentage calculation)

SavedTimeCard props:
  estimatedSavedMinutes: number
  resolvedCount: number
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create statistics component files</name>
  <files>
    src/components/statistics/KpiCard.tsx,
    src/components/statistics/DailyVolumeChart.tsx,
    src/components/statistics/IntentsTable.tsx,
    src/components/statistics/SavedTimeCard.tsx
  </files>
  <read_first>
    - src/lib/types/index.ts (read Phase 07 section for StatsSummary, DailyCallVolume, IntentFrequency types)
    - src/components/ui/card.tsx (Card, CardContent, CardHeader, CardTitle imports)
    - src/components/ui/badge.tsx (Badge for status labels)
  </read_first>
  <action>
    Create src/components/statistics/KpiCard.tsx:
    - 'use client' NOT needed — pure display, no state
    - Props: { label: string; value: string | number; sub?: string; description?: string }
    - Render a Card with CardHeader (CardTitle = label) and CardContent showing value large, sub small
    - No 'use client' directive — Server Component compatible

    Create src/components/statistics/DailyVolumeChart.tsx:
    - 'use client' NOT needed — pure display, no interactivity
    - Props: { data: DailyCallVolume[]; label: string }
    - Renders a simple CSS bar chart: for each day, a div with a bar whose width% = (day.total / max) × 100
    - Show last 14 days max (slice the data array) so bars are readable
    - Bar: green for resolved portion, gray for total. Use Tailwind absolute positioning or flex approach
    - Below each bar: the date formatted as "DD.MM." (no year to save space)
    - Display max count label on right side
    - Simple, no animation, no external lib

    Create src/components/statistics/IntentsTable.tsx:
    - Props: { intents: IntentFrequency[]; total: number }
    - Render a simple table: rank, intent name, count, percentage (count/total×100).toFixed(1)%
    - Each row has a thin percentage bar using Tailwind inline style width
    - Show "Kein Intent erkannt" for null/empty intent strings
    - No 'use client' needed

    Create src/components/statistics/SavedTimeCard.tsx:
    - Props: { estimatedSavedMinutes: number; resolvedCount: number }
    - Display the saved time in "ca. X Std. Y Min." format
    - Show the formula: "{resolvedCount} erledigte Gespräche × 5 Min. Zeitersparnis"
    - Render as a shadcn Card
    - No 'use client' needed
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 component files exist and export a default function
    - grep "'use client'" src/components/statistics/KpiCard.tsx outputs nothing (Server Component)
    - DailyVolumeChart uses Tailwind classes for bars, no recharts or chart library import
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>4 statistics components created. All are Server Component compatible (no 'use client'). CSS-only bar charts.</done>
</task>

<task type="auto">
  <name>Task 2: Build full statistiken/page.tsx</name>
  <files>src/app/(dashboard)/statistiken/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/statistiken/page.tsx (the stub from 07-01 — overwrite it)
    - src/lib/supabase/server.ts (createServerClient, createServiceRoleClient exports)
    - src/lib/types/index.ts Phase 07 section (StatsSummary, DailyCallVolume, IntentFrequency)
    - src/components/statistics/KpiCard.tsx (just created — verify import path)
  </read_first>
  <action>
    Overwrite src/app/(dashboard)/statistiken/page.tsx with the full Server Component:

    Imports:
    - redirect from 'next/navigation'
    - createServerClient from '@/lib/supabase/server'
    - createServiceRoleClient from '@/lib/supabase/server'
    - KpiCard, DailyVolumeChart, IntentsTable, SavedTimeCard from '@/components/statistics/*'
    - DailyCallVolume, IntentFrequency from '@/lib/types'

    Props: { searchParams: Promise<{ tage?: string }> } — Next.js 16 async searchParams

    Auth sequence:
      const supabase = await createServerClient()
      const { data: { user } } = await supabase.auth.getUser()
      if (!user) redirect('/auth/login')
      const tenantId = (user.app_metadata?.tenant_id as string) ?? ''
      const serviceClient = createServiceRoleClient()

    Time filter:
      const params = await searchParams
      const days = Math.min(90, Math.max(7, parseInt(params.tage ?? '30', 10)))
      const startDate = new Date(Date.now() - days * 24 * 60 * 60 * 1000).toISOString()
      const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString()

    Fetch all data in Promise.all:
    const [callLogResult, apptCountResult, rxCountResult, openTasksResult, emergencyResult] = await Promise.all([
      serviceClient.from('call_log').select('created_at, status, duration_seconds, intent_main').eq('tenant_id', tenantId).gte('created_at', startDate).order('created_at', { ascending: true }),
      serviceClient.from('call_actions').select('id', { count: 'exact', head: true }).eq('tenant_id', tenantId).eq('action_type', 'appointment_booked').gte('created_at', startDate),
      serviceClient.from('call_actions').select('id', { count: 'exact', head: true }).eq('tenant_id', tenantId).eq('action_type', 'prescription_ordered').gte('created_at', startDate),
      serviceClient.from('inbox_items').select('id', { count: 'exact', head: true }).eq('tenant_id', tenantId).in('status', ['open', 'in_progress']),
      serviceClient.from('inbox_items').select('id', { count: 'exact', head: true }).eq('tenant_id', tenantId).eq('case_type', 'emergency').gte('created_at', thirtyDaysAgo),
    ])

    JavaScript aggregations (from callLogResult.data ?? []):
      const callLog = callLogResult.data ?? []
      const totalCalls = callLog.length
      const resolvedCalls = callLog.filter(r => r.status === 'completed')
      const forwardedCalls = callLog.filter(r => r.status === 'forwarded')
      const completedWithDuration = resolvedCalls.filter(r => r.duration_seconds !== null)
      const avgDurationSeconds = completedWithDuration.length > 0
        ? Math.round(completedWithDuration.reduce((sum, r) => sum + (r.duration_seconds ?? 0), 0) / completedWithDuration.length)
        : 0
      const resolutionRate = totalCalls > 0 ? Math.round((resolvedCalls.length / totalCalls) * 100) : 0
      const forwardingRate = totalCalls > 0 ? Math.round((forwardedCalls.length / totalCalls) * 100) : 0
      const estimatedSavedMinutes = resolvedCalls.length * 5

      // Daily volume
      const byDay: Record<string, { total: number; resolved: number; forwarded: number }> = {}
      for (const row of callLog) {
        const date = row.created_at.split('T')[0]
        if (!byDay[date]) byDay[date] = { total: 0, resolved: 0, forwarded: 0 }
        byDay[date].total++
        if (row.status === 'completed') byDay[date].resolved++
        if (row.status === 'forwarded') byDay[date].forwarded++
      }
      const dailyVolume: DailyCallVolume[] = Object.entries(byDay)
        .map(([date, counts]) => ({ date, ...counts }))
        .sort((a, b) => a.date.localeCompare(b.date))

      // Top intents
      const intentCounts: Record<string, number> = {}
      for (const row of callLog) {
        const intent = row.intent_main ?? ''
        intentCounts[intent] = (intentCounts[intent] ?? 0) + 1
      }
      const topIntents: IntentFrequency[] = Object.entries(intentCounts)
        .sort((a, b) => b[1] - a[1])
        .slice(0, 8)
        .map(([intent, count]) => ({ intent: intent || 'Kein Intent erkannt', count }))

    Formatted helpers:
      const avgDurationLabel = avgDurationSeconds > 0
        ? `${Math.floor(avgDurationSeconds / 60)} Min. ${avgDurationSeconds % 60} Sek.`
        : '—'

    Render:
      <div className="space-y-6">
        <div className="flex items-center justify-between">
          <h1 className="text-xl font-semibold text-gray-900">Statistiken</h1>
          <div className="flex gap-2 text-sm">
            {[7, 30, 90].map(d => (
              <a key={d} href={`/statistiken?tage=${d}`}
                className={`rounded px-3 py-1 ${days === d ? 'bg-gray-900 text-white' : 'bg-white text-gray-700 border hover:bg-gray-50'}`}
              >
                {d} Tage
              </a>
            ))}
          </div>
        </div>

        {/* KPI grid — 4 columns */}
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
          <KpiCard label="Anrufe gesamt" value={totalCalls} sub={`${days} Tage`} />
          <KpiCard label="Ø Gesprächsdauer" value={avgDurationLabel} />
          <KpiCard label="Lösungsquote" value={`${resolutionRate}%`} sub={`${resolvedCalls.length} erledigt`} />
          <KpiCard label="Weiterleitungsquote" value={`${forwardingRate}%`} sub={`${forwardedCalls.length} weitergeleitet`} />
        </div>

        {/* Second row */}
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">
          <KpiCard label="Offene Aufgaben" value={openTasksResult.count ?? 0} />
          <KpiCard label="Gebuchte Termine" value={apptCountResult.count ?? 0} sub={`${days} Tage`} />
          <KpiCard label="Rezeptanfragen" value={rxCountResult.count ?? 0} sub={`${days} Tage`} />
          <KpiCard label="Notfallfälle" value={emergencyResult.count ?? 0} sub="letzte 30 Tage" />
        </div>

        {/* Volume chart */}
        <DailyVolumeChart data={dailyVolume} label={`Anrufvolumen — letzte ${days} Tage`} />

        {/* Bottom row: intents + saved time */}
        <div className="grid gap-4 lg:grid-cols-2">
          <IntentsTable intents={topIntents} total={totalCalls} />
          <SavedTimeCard estimatedSavedMinutes={estimatedSavedMinutes} resolvedCount={resolvedCalls.length} />
        </div>
      </div>
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "Promise.all" src/app/(dashboard)/statistiken/page.tsx — parallel queries used
    - grep "call_log\|call_actions\|inbox_items" src/app/(dashboard)/statistiken/page.tsx — all 3 tables queried
    - grep "tenantId" src/app/(dashboard)/statistiken/page.tsx — tenant isolation verified in queries
    - grep "KpiCard\|DailyVolumeChart\|IntentsTable\|SavedTimeCard" src/app/(dashboard)/statistiken/page.tsx — all 4 components used
    - grep "searchParams" src/app/(dashboard)/statistiken/page.tsx — time filter wired
    - File is longer than 80 lines
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Full statistiken/page.tsx built. All 5 STAT requirements delivered: call volume, duration, resolution/forwarding rates, task/appointment/prescription counts, top intents, saved time estimate.</done>
</task>

<task type="auto">
  <name>Task 3: Git commit statistics module</name>
  <files>all statistics files</files>
  <action>
    Stage and commit all statistics files:
    - src/app/(dashboard)/statistiken/page.tsx
    - src/components/statistics/KpiCard.tsx
    - src/components/statistics/DailyVolumeChart.tsx
    - src/components/statistics/IntentsTable.tsx
    - src/components/statistics/SavedTimeCard.tsx

    Commit message: "feat(phase-07): statistics module — STAT-01..05 KPIs, volume chart, top intents, saved time"
  </action>
  <acceptance_criteria>
    - git status shows clean working tree
    - git log --oneline -1 confirms commit
  </acceptance_criteria>
  <done>Statistics module committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → statistiken/page.tsx | tenantId from JWT app_metadata — not from URL params |
| statistiken page → Supabase | Service-role client + explicit .eq('tenant_id', tenantId) on every query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | Statistics queries return cross-tenant data | mitigate | Every query has .eq('tenant_id', tenantId) where tenantId comes from JWT app_metadata, not URL or request body (C2 pitfall prevention) |
| T-07-03 | Information Disclosure | Statistics page exposed to viewers without auth | mitigate | Auth check at top of page with redirect('/auth/login') — no role gate needed, all authenticated users can view statistics (STAT-01..05 are read-only aggregates) |
| T-07-SC | Tampering | npm installs | accept | No new packages — CSS bar charts instead of recharts |
</threat_model>

<verification>
After all tasks:
- grep -c "serviceClient.from" src/app/(dashboard)/statistiken/page.tsx outputs 5 (one per table/query)
- grep "tenantId" src/app/(dashboard)/statistiken/page.tsx — appears in all .eq() calls
- All 4 component files in src/components/statistics/ exist
- npx tsc --noEmit exits 0
- STAT requirements covered:
  STAT-01: totalCalls + avgDurationSeconds KPI cards ✓
  STAT-02: resolutionRate + forwardingRate KPI cards ✓
  STAT-03: openTasksCount + bookedAppointments + prescriptionRequests KPI cards ✓
  STAT-04: topIntents table + emergencyCases KPI card ✓
  STAT-05: estimatedSavedMinutes SavedTimeCard ✓
</verification>

<success_criteria>
- /statistiken page loads for any authenticated user and shows real data from their tenant
- 8 KPI cards visible: Anrufe gesamt, Ø Gesprächsdauer, Lösungsquote, Weiterleitungsquote, Offene Aufgaben, Gebuchte Termine, Rezeptanfragen, Notfallfälle
- Daily volume chart renders (CSS bars, no external chart library)
- Top-8 intents ranked table shows frequency and percentage
- Saved time estimate shown with formula explanation
- Time period filter (7/30/90 Tage) changes via URL searchParams
- TypeScript strict compiles without errors
</success_criteria>

<output>
Create .planning/phases/07-statistics-user-mgmt/07-02-SUMMARY.md when done.
</output>
