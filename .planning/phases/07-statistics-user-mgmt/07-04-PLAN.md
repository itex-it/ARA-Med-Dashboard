---
phase: 07-statistics-user-mgmt
plan: 04
type: execute
wave: 3
depends_on:
  - 07-02
  - 07-03
files_modified: []
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - RBAC-01
  - RBAC-02
  - RBAC-03
  - RBAC-04
  - RBAC-05
  - RBAC-06

must_haves:
  truths:
    - "TypeScript strict build passes with 0 errors (npx tsc --noEmit)"
    - "GitHub Actions CI build succeeds and Docker image pushed to ghcr.io/itex-it/ara-med-dashboard:latest"
    - "Portainer redeployed with new image (all 3 env vars preserved in PUT payload)"
    - "https://app.aramed.at/statistiken loads and shows data for the test tenant"
    - "https://app.aramed.at/benutzer loads and shows the user list for operator/ordination_admin"
    - "No RBAC leakage: navigating to /benutzer as assistant/viewer redirects to /dashboard"
  artifacts:
    - path: ".planning/phases/07-statistics-user-mgmt/07-04-SUMMARY.md"
      provides: "Build gate results, requirement coverage table, deployment confirmation"
  key_links:
    - from: "07-04-SUMMARY.md"
      to: "GitHub Actions run"
      via: "CI build URL and result"
      pattern: "github.com/itex-it/ARA-Med-Dashboard/actions"
---

<objective>
Validate the complete Phase 7 implementation: TypeScript check, CI build, Portainer redeploy, and smoke test of both new pages (/statistiken and /benutzer) against the live deployment.

Purpose: Gate that confirms STAT-01..05 and RBAC-01..06 are delivered end-to-end on the production deployment. Portainer redeploy MUST include all 3 env vars in the PUT payload to avoid wiping them.
Output: Phase 7 is COMPLETE. ROADMAP.md updated.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.json

Infrastructure:
- Portainer URL: https://194.242.35.77:9443
- Portainer API Key: ptr_YA6VBAMtrqc5r4z0w4W0LH8MyvtqhJIhyim5nQgphJo=
- Stack ID: 97, Endpoint ID: 3
- CRITICAL: Portainer PUT /api/stacks/97?endpointId=3 MUST include all 3 Env vars — sending empty Env array wipes them (Phase 6 incident, HANDOFF.json decision)
- Env vars required: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
- Read .env.local for the values if needed (service role key must not appear in plan output)

- Dashboard URL: https://app.aramed.at
- Test tenant login: winter@itex.at (tenant_id: 31ff4da7-7434-4f41-a551-c2a6075348f5)
- GitHub repo: github.com/itex-it/ARA-Med-Dashboard

Key Phase 6 pattern for Portainer redeploy:
  PUT https://194.242.35.77:9443/api/stacks/97?endpointId=3
  Headers: X-API-Key, Content-Type: application/json
  Body: { "pullImage": true, "prune": false, "Env": [{"name":"NEXT_PUBLIC_SUPABASE_URL","value":"..."},{"name":"NEXT_PUBLIC_SUPABASE_ANON_KEY","value":"..."},{"name":"SUPABASE_SERVICE_ROLE_KEY","value":"..."}] }
</context>

<interfaces>
Build gate checks (run in order):

1. TypeScript check:
   npx tsc --noEmit
   → Must exit 0

2. CI trigger:
   git push origin main (if not already pushed)
   Monitor GitHub Actions: GET https://api.github.com/repos/itex-it/ARA-Med-Dashboard/actions/runs?per_page=1
   Wait for conclusion === 'success'

3. Portainer redeploy:
   Read current env vars from .env.local (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY)
   PUT https://194.242.35.77:9443/api/stacks/97?endpointId=3 with pullImage:true AND full Env array
   Verify container is Up and running: GET /api/endpoints/3/docker/containers/{id}/json — check State.Running === true

4. Smoke test /statistiken:
   GET https://app.aramed.at/statistiken — should return 200 or redirect to login (not 500)
   Page must render KPI cards (verify HTML contains "Statistiken")

5. Smoke test /benutzer:
   GET https://app.aramed.at/benutzer — should return 200 or redirect to login (not 500)

6. RBAC smoke test:
   Verify /benutzer page server-side role gate is in place:
   grep "redirect('/dashboard')" src/app/(dashboard)/benutzer/page.tsx
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript strict check</name>
  <files>—</files>
  <read_first>none</read_first>
  <action>
    Run: npx tsc --noEmit
    If errors exist: read each error, fix the source file, re-run until 0 errors.
    Common patterns to check:
    - Import paths: @/lib/permissions.ts must resolve (file created in 07-01)
    - Async searchParams in statistiken/page.tsx: must be Promise<{tage?: string}> in Next.js 16
    - auth.admin.listUsers() response shape: check .data?.users type compatibility with TenantUserDisplay mapping
    - PERMISSION_LEVEL_ORDER used in InviteUserSheet must import from src/lib/permissions.ts
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0 with no error output
  </acceptance_criteria>
  <done>TypeScript strict check passed. 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Push to GitHub and wait for CI build</name>
  <files>—</files>
  <read_first>none</read_first>
  <action>
    1. Verify all Phase 7 commits are on main branch: git log --oneline -5
    2. Push: git push origin main
    3. Poll GitHub Actions API until the latest run completes:
       GET https://api.github.com/repos/itex-it/ARA-Med-Dashboard/actions/runs?per_page=1
       Check: runs[0].conclusion === 'success'
       If conclusion === 'failure': read the job logs, identify the failing step, fix it, re-push.
    4. Confirm Docker image pushed: runs[0].name should include the build-and-push step
  </action>
  <verify>
    <automated>curl -s "https://api.github.com/repos/itex-it/ARA-Med-Dashboard/actions/runs?per_page=1" | python3 -c "import sys,json; r=json.load(sys.stdin)['workflow_runs'][0]; print(r['conclusion'], r['status'])"</automated>
  </verify>
  <acceptance_criteria>
    - GitHub Actions latest run: conclusion === 'success'
    - Docker image ghcr.io/itex-it/ara-med-dashboard:latest updated (check run timestamp)
  </acceptance_criteria>
  <done>GitHub Actions CI build successful. Docker image pushed.</done>
</task>

<task type="auto">
  <name>Task 3: Portainer redeploy with full env var preservation</name>
  <files>—</files>
  <read_first>
    - .env.local (read for NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY values)
    - .planning/HANDOFF.json (confirm Portainer PUT must include Env array — critical decision from Phase 6)
  </read_first>
  <action>
    CRITICAL: The Portainer PUT payload MUST include all 3 Env vars. Sending empty Env array wipes all env vars from the stack (Phase 6 incident).

    1. Read the 3 env var values from .env.local
    2. Execute Portainer PUT via curl (PowerShell Invoke-RestMethod or Bash curl):
       PUT https://194.242.35.77:9443/api/stacks/97?endpointId=3
       Body: { "pullImage": true, "prune": false, "Env": [ {"name":"NEXT_PUBLIC_SUPABASE_URL","value":"<value>"}, {"name":"NEXT_PUBLIC_SUPABASE_ANON_KEY","value":"<value>"}, {"name":"SUPABASE_SERVICE_ROLE_KEY","value":"<value>"} ] }
       Skip TLS verification (self-signed cert): curl -k or Invoke-RestMethod -SkipCertificateCheck

    3. Wait ~60 seconds for container to pull and restart

    4. Verify container state:
       GET https://194.242.35.77:9443/api/endpoints/3/docker/containers/json?all=0
       Find the ara-med container, confirm State = 'running'

    5. Verify no env var wipe:
       If container starts but crashes with "Your project's URL and Key are required" → env vars were wiped → re-run PUT with Env array
  </action>
  <verify>
    <automated>curl -sk -H "X-API-Key: ptr_YA6VBAMtrqc5r4z0w4W0LH8MyvtqhJIhyim5nQgphJo=" "https://194.242.35.77:9443/api/endpoints/3/docker/containers/json?all=0" | python3 -c "import sys,json; cs=[c for c in json.load(sys.stdin) if 'ara-med' in str(c.get('Names',''))]; print(cs[0]['State'] if cs else 'NOT FOUND')"</automated>
  </verify>
  <acceptance_criteria>
    - Container state: 'running'
    - Container started AFTER the CI build completed (timestamp check)
    - No crash restart loop (check container status is stable after 30s)
  </acceptance_criteria>
  <done>Portainer redeployed. Container running with new Phase 7 image. All 3 env vars preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Smoke test both new pages</name>
  <files>—</files>
  <read_first>none</read_first>
  <action>
    1. Check /statistiken page reachable:
       curl -sk -o /dev/null -w "%{http_code}" https://app.aramed.at/statistiken
       Accept: 200 (cached/logged-in) or 307/302 (redirect to login) — NOT 500

    2. Check /benutzer page reachable:
       curl -sk -o /dev/null -w "%{http_code}" https://app.aramed.at/benutzer
       Accept: 200 or 307/302 — NOT 500

    3. RBAC server-side check (code-level, not runtime):
       grep "redirect('/dashboard')" src/app/(dashboard)/benutzer/page.tsx
       Must return 1 match — confirms role gate is in place

    4. Nav links check:
       grep "Statistiken" src/app/(dashboard)/layout.tsx — must exist
       grep "Benutzer" src/app/(dashboard)/layout.tsx — must exist with role condition

    5. Verify no recharts or external chart library was added:
       grep -r "recharts\|chart.js\|d3\|victory" package.json — must return nothing

    Record all results in 07-04-SUMMARY.md.
  </action>
  <verify>
    <automated>grep "redirect('/dashboard')" src/app/(dashboard)/benutzer/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - /statistiken returns 200 or redirect (not 500)
    - /benutzer returns 200 or redirect (not 500)
    - RBAC role gate confirmed in benutzer/page.tsx
    - No external chart library in package.json
    - Nav links for Statistiken and Benutzer present in layout.tsx
  </acceptance_criteria>
  <done>Smoke test passed. Both pages reachable. RBAC role gate in place. No external chart dependency added.</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md and HANDOFF.json</name>
  <files>
    .planning/ROADMAP.md,
    .planning/HANDOFF.json
  </files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 7 section — mark as complete, check all plan checkboxes)
    - .planning/HANDOFF.json (update for Phase 8 handoff)
  </read_first>
  <action>
    Update .planning/ROADMAP.md:
    - Phase 7 status line: change "[ ] **Phase 7: Statistics & User Management**" to "[x] **Phase 7: Statistics & User Management**" — add "(completed 2026-05-24)"
    - Plans section for Phase 7: replace "**Plans:** TBD" with:
      **Plans:** 4 plans
      Plans:
      - [x] 07-01-PLAN.md — Foundation: Phase 07 types, permissions utility, nav links, stub pages
      - [x] 07-02-PLAN.md — Statistics module: KPIs, volume chart, top intents, saved time (STAT-01..05)
      - [x] 07-03-PLAN.md — User management: invite, edit, deactivate with delegation ceiling (RBAC-01..06)
      - [x] 07-04-PLAN.md — Build gate + smoke test: CI build + both pages verified live
    - Progress table: Phase 7 row: "4/4 | COMPLETE | 2026-05-24"

    Update .planning/HANDOFF.json:
    - phase: "08"
    - phase_name: "Audit Log & System Polish"
    - phase_dir: ".planning/phases/08-audit-log-polish"
    - status: "not_started"
    - completed_tasks: [] (reset)
    - remaining_tasks: [{"id": 1, "name": "Plan Phase 8 via /gsd-plan-phase 8", "status": "not_started"}]
    - context_notes: "Phase 7 complete. Statistics (/statistiken) and User Management (/benutzer) live. All STAT-01..05 and RBAC-01..06 delivered. Phase 8: Audit Log & System Polish — 3 requirements (AUDIT-01..03). Last phase of MVP."
    - decisions: preserve existing decisions, add: {"decision": "Statistics use CSS-only bars — no recharts", "rationale": "No external chart dependency for MVP; CSS bar charts via Tailwind are sufficient for call volume visualization", "phase": "07"}, {"decision": "New users created via auth.admin.createUser with email_confirm:true", "rationale": "SMTP not configured on self-hosted Supabase; createUser bypasses email invite, admin shares tempPassword once", "phase": "07"}
    - next_action: "Start new session and run /gsd-plan-phase 8"
  </action>
  <acceptance_criteria>
    - grep "\[x\].*Phase 7" .planning/ROADMAP.md — Phase 7 marked complete
    - grep "07-01-PLAN\|07-02-PLAN\|07-03-PLAN\|07-04-PLAN" .planning/ROADMAP.md — all 4 plans listed
    - cat .planning/HANDOFF.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['phase'], d['status'])" outputs "08 not_started"
  </acceptance_criteria>
  <done>ROADMAP.md updated (Phase 7 complete, 4 plans checked). HANDOFF.json updated for Phase 8.</done>
</task>

<task type="auto">
  <name>Task 6: Final git commit and create summary</name>
  <files>
    .planning/ROADMAP.md,
    .planning/HANDOFF.json,
    .planning/phases/07-statistics-user-mgmt/07-04-SUMMARY.md
  </files>
  <action>
    Create .planning/phases/07-statistics-user-mgmt/07-04-SUMMARY.md with:
    - Status: COMPLETE, date 2026-05-24
    - Build gate results table (TypeScript check, CI build, Portainer, smoke tests)
    - Requirement coverage table (STAT-01..05, RBAC-01..06 — all ✓ DELIVERED)
    - Notes on no-SMTP approach, CSS charts, delegation ceiling

    Commit all remaining changes:
    - .planning/ROADMAP.md
    - .planning/HANDOFF.json
    - .planning/phases/07-statistics-user-mgmt/07-04-SUMMARY.md

    Commit message: "chore(phase-07): complete — STAT-01..05 + RBAC-01..06 delivered, HANDOFF updated for Phase 8"
  </action>
  <acceptance_criteria>
    - git status shows clean working tree
    - All 07-0{1..4}-SUMMARY.md files exist in .planning/phases/07-statistics-user-mgmt/
  </acceptance_criteria>
  <done>Phase 7 complete. All SUMMARY files present. HANDOFF.json ready for Phase 8.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/CD | GitHub Actions builds on ubuntu-latest — unaffected by Windows Turbopack null-byte issue |
| Portainer | Service account API key required for redeploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | Portainer PUT wipes env vars | mitigate | Always pass complete Env array in PUT body — documented as Phase 6 decision in HANDOFF.json |
| T-07-12 | Denial of Service | Container crash from missing env vars | mitigate | Verify container running state after redeploy via Portainer API before declaring smoke test passed |
</threat_model>

<verification>
After all tasks — final Phase 7 checklist:
STAT-01: Anrufvolumen + Ø Dauer KPI cards on /statistiken ✓
STAT-02: Lösungsquote + Weiterleitungsquote KPI cards ✓
STAT-03: Offene Aufgaben + Termine + Rezeptanfragen KPI cards ✓
STAT-04: Top-8 Intents ranked table + Notfallfälle count ✓
STAT-05: Eingesparte Zeit = resolved × 5 Min. SavedTimeCard ✓
RBAC-01: Operator can invite ordination_admin, assistant, viewer ✓
RBAC-02: Ordination_admin can invite assistant, viewer only ✓
RBAC-03: Role displayed per user in BenutzerTable ✓
RBAC-04: 20-module permission matrix in InviteUserSheet/EditUserSheet ✓
RBAC-05: applyDelegationCeiling server-side + disabled UI inputs ✓
RBAC-06: All actions server-side; /benutzer redirects non-admin roles ✓
</verification>

<success_criteria>
- TypeScript strict: 0 errors
- CI build: success, image pushed
- Portainer: container running with Phase 7 image, env vars intact
- /statistiken live and serving real data for test tenant
- /benutzer live and accessible to operator/ordination_admin
- ROADMAP.md Phase 7 marked [x] complete
- HANDOFF.json ready for Phase 8
</success_criteria>

<output>
Create .planning/phases/07-statistics-user-mgmt/07-04-SUMMARY.md when done.
</output>
