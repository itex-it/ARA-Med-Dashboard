---
phase: 07-statistics-user-mgmt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/index.ts
  - src/lib/permissions.ts
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/statistiken/page.tsx
  - src/app/(dashboard)/benutzer/page.tsx
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - STAT-04
  - STAT-05
  - RBAC-01
  - RBAC-02
  - RBAC-03
  - RBAC-04
  - RBAC-05
  - RBAC-06

must_haves:
  truths:
    - "src/lib/types/index.ts has Phase 07 section with PermissionLevel, ModuleCategory, ModulePermissions, TenantUserDisplay types"
    - "src/lib/permissions.ts exports MODULE_CATEGORIES (20 entries), PERMISSION_LEVELS, DEFAULT_PERMISSIONS by role, canGrantPermission(), applyDelegationCeiling()"
    - "Dashboard layout.tsx has Statistiken and Benutzer nav links (Benutzer conditionally shown for operator/ordination_admin)"
    - "src/app/(dashboard)/statistiken/page.tsx exists as a valid Server Component with loading skeleton"
    - "src/app/(dashboard)/benutzer/page.tsx exists as a valid Server Component with loading skeleton"
  artifacts:
    - path: "src/lib/types/index.ts"
      provides: "Phase 07 type block: PermissionLevel union, ModuleCategory union, ModulePermissions record, TenantUserDisplay interface"
      contains: "Phase 07"
    - path: "src/lib/permissions.ts"
      provides: "MODULE_CATEGORIES array, PERMISSION_LEVELS array, DEFAULT_PERMISSIONS, canGrantPermission, applyDelegationCeiling"
      contains: "export const MODULE_CATEGORIES"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Statistiken and Benutzer nav links"
      contains: "Statistiken"
    - path: "src/app/(dashboard)/statistiken/page.tsx"
      provides: "Stub Server Component page — no TypeScript errors"
      contains: "export default"
    - path: "src/app/(dashboard)/benutzer/page.tsx"
      provides: "Stub Server Component page — no TypeScript errors"
      contains: "export default"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/app/(dashboard)/statistiken/page.tsx"
      via: "href='/statistiken' nav link"
      pattern: "href=\"/statistiken\""
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/app/(dashboard)/benutzer/page.tsx"
      via: "href='/benutzer' nav link (role-gated)"
      pattern: "href=\"/benutzer\""
    - from: "src/lib/permissions.ts"
      to: "src/lib/types/index.ts"
      via: "ModuleCategory and PermissionLevel types used in permissions.ts"
      pattern: "ModuleCategory|PermissionLevel"
---

<objective>
Create the shared foundation for Phase 7: TypeScript types for the permission system, the permissions utility module, navigation links for Statistiken and Benutzer, and stub pages for both new routes.

Purpose: Plans 07-02 (statistics) and 07-03 (user management) both import from src/lib/types/index.ts and src/lib/permissions.ts, and both write to their respective page files. This plan lays those foundations so 07-02 and 07-03 can run in parallel without conflicts.
Output: 2 new files (permissions.ts, 2 stub pages), 2 updated files (types/index.ts, layout.tsx).
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key analogs:
- src/lib/types/index.ts lines 270-352 (Phase 06 section header pattern — replicate for Phase 07)
- src/app/(dashboard)/layout.tsx (current nav — add Statistiken and Benutzer links)
- src/app/(dashboard)/konfiguration/page.tsx (Server Component page pattern — use as stub template)
- supabase/migrations/20260522000002_user_tenant_roles.sql (existing schema: role CHECK constraint values, permissions JSONB column)
</context>

<interfaces>
Phase 07 type additions for src/lib/types/index.ts:

MODULE_CATEGORIES (20 items, kebab-free DB-safe keys):
  'dashboard' | 'telefonate' | 'audio' | 'transkripte' | 'patientendaten' |
  'inbox' | 'termine' | 'rezepte' | 'routing' | 'kommunikation' |
  'kommunikation_templates' | 'oeffnungszeiten' | 'vertretung' | 'prompts' |
  'training' | 'statistiken' | 'kosten' | 'benutzerverwaltung' |
  'system_settings' | 'audit_log'

PERMISSION_LEVELS: 'none' | 'view' | 'edit' | 'manage' | 'admin'

TenantUserDisplay (combined auth.users + user_tenant_roles for rendering):
  id: string (user_id from user_tenant_roles)
  email: string (from auth.users)
  role: AraRole
  permissions: Partial<ModulePermissions>
  active: boolean
  created_at: string
  last_sign_in_at: string | null

Existing AraRole (already in types/index.ts — do NOT redefine):
  'operator' | 'ordination_admin' | 'assistant' | 'viewer'

permissions.ts exports:
  MODULE_CATEGORIES — readonly tuple (use 'as const')
  PERMISSION_LEVELS — readonly tuple (use 'as const')
  DEFAULT_PERMISSIONS — Record<AraRole, ModulePermissions> with per-role defaults
  PERMISSION_LEVEL_ORDER — Record<PermissionLevel, number> for comparison
  canGrantPermission(grantorLevel, targetLevel) — returns boolean
  applyDelegationCeiling(grantorPerms, targetPerms) — returns ModulePermissions capped at grantor ceiling
  getDefaultPermissions(role) — returns ModulePermissions for a given AraRole

Default permissions by role (used in invite form auto-fill):
  operator: all 'admin'
  ordination_admin: most 'manage', kosten='view', system_settings='none', audit_log='view'
  assistant: inbox='edit', dashboard/telefonate/audio/transkripte/patientendaten/termine/rezepte/kommunikation/statistiken='view', everything else='none'
  viewer: dashboard/telefonate/inbox/statistiken='view', everything else='none'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 07 types to src/lib/types/index.ts</name>
  <files>src/lib/types/index.ts</files>
  <read_first>
    - src/lib/types/index.ts (read full file — find end of Phase 06 section at line ~352, append after)
  </read_first>
  <action>
    Append a new Phase 07 section after the last Phase 06 type (SendLogRow). Do NOT modify any existing types.

    Section header:
    // ============================================================
    // Phase 07: Statistics & User Management Types
    // ============================================================

    Union types:
    export type PermissionLevel = 'none' | 'view' | 'edit' | 'manage' | 'admin'

    export type ModuleCategory =
      | 'dashboard' | 'telefonate' | 'audio' | 'transkripte' | 'patientendaten'
      | 'inbox' | 'termine' | 'rezepte' | 'routing' | 'kommunikation'
      | 'kommunikation_templates' | 'oeffnungszeiten' | 'vertretung' | 'prompts'
      | 'training' | 'statistiken' | 'kosten' | 'benutzerverwaltung'
      | 'system_settings' | 'audit_log'

    Record type:
    export type ModulePermissions = Record<ModuleCategory, PermissionLevel>

    Interface (combined auth user + user_tenant_roles for display):
    export interface TenantUserDisplay {
      id: string
      email: string
      role: AraRole
      permissions: Partial<ModulePermissions>
      active: boolean
      created_at: string
      last_sign_in_at: string | null
    }

    Stats summary type (for the statistics page):
    export interface StatsSummary {
      totalCalls: number
      avgDurationSeconds: number
      resolvedCount: number
      forwardedCount: number
      failedCount: number
      resolutionRate: number
      forwardingRate: number
      openTasksCount: number
      bookedAppointments: number
      prescriptionRequests: number
      emergencyCases: number
      estimatedSavedMinutes: number
    }

    Daily volume type:
    export interface DailyCallVolume {
      date: string
      total: number
      resolved: number
      forwarded: number
    }

    Intent frequency type:
    export interface IntentFrequency {
      intent: string
      count: number
    }
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "Phase 07" src/lib/types/index.ts confirms section header added
    - grep "PermissionLevel\|ModuleCategory\|ModulePermissions\|TenantUserDisplay\|StatsSummary\|DailyCallVolume\|IntentFrequency" src/lib/types/index.ts — all 7 new types present
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - Existing types (AraRole, SendLogRow, etc.) unchanged
  </acceptance_criteria>
  <done>Phase 07 type block appended to src/lib/types/index.ts. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/lib/permissions.ts</name>
  <files>src/lib/permissions.ts</files>
  <read_first>
    - src/lib/types/index.ts (just updated — import ModuleCategory, PermissionLevel, ModulePermissions, AraRole from here)
    - supabase/migrations/20260522000002_user_tenant_roles.sql (role values and permissions JSONB comment)
  </read_first>
  <action>
    Create src/lib/permissions.ts as a shared module (no 'server-only' — constants needed on client for permission matrix UI):

    Import types from '@/lib/types'

    Export MODULE_CATEGORIES as a readonly const tuple (all 20 categories).
    Export MODULE_CATEGORY_LABELS: Record<ModuleCategory, string> with German display names:
      dashboard: 'Dashboard-Übersicht',
      telefonate: 'Telefonate',
      audio: 'Audio',
      transkripte: 'Transkripte',
      patientendaten: 'Patientendaten',
      inbox: 'Inbox/Aufgaben',
      termine: 'Termine',
      rezepte: 'Rezepte',
      routing: 'Routing',
      kommunikation: 'Kommunikation',
      kommunikation_templates: 'Kommunikations-Templates',
      oeffnungszeiten: 'Öffnungszeiten',
      vertretung: 'Vertretung',
      prompts: 'Prompts/Texte',
      training: 'Training/FAQ',
      statistiken: 'Statistiken',
      kosten: 'Kosten',
      benutzerverwaltung: 'Benutzerverwaltung',
      system_settings: 'System-Settings',
      audit_log: 'Audit-Log',

    Export PERMISSION_LEVELS as a readonly const tuple: ['none', 'view', 'edit', 'manage', 'admin']
    Export PERMISSION_LEVEL_LABELS: Record<PermissionLevel, string> = { none: 'Kein', view: 'Lesen', edit: 'Bearbeiten', manage: 'Verwalten', admin: 'Admin' }
    Export PERMISSION_LEVEL_ORDER: Record<PermissionLevel, number> = { none: 0, view: 1, edit: 2, manage: 3, admin: 4 }

    Export ROLE_HIERARCHY: Record<AraRole, number> = { viewer: 0, assistant: 1, ordination_admin: 2, operator: 3 }
    Export ROLE_LABELS: Record<AraRole, string> = { operator: 'Operator', ordination_admin: 'Arzt/Admin', assistant: 'Ordinationsassistenz', viewer: 'Viewer' }

    Export DEFAULT_PERMISSIONS: Record<AraRole, ModulePermissions>:
    - operator: all 20 modules = 'admin'
    - ordination_admin: { dashboard: 'manage', telefonate: 'manage', audio: 'manage', transkripte: 'manage', patientendaten: 'manage', inbox: 'manage', termine: 'manage', rezepte: 'manage', routing: 'manage', kommunikation: 'manage', kommunikation_templates: 'manage', oeffnungszeiten: 'manage', vertretung: 'manage', prompts: 'manage', training: 'manage', statistiken: 'manage', kosten: 'view', benutzerverwaltung: 'manage', system_settings: 'none', audit_log: 'view' }
    - assistant: { dashboard: 'view', telefonate: 'view', audio: 'view', transkripte: 'view', patientendaten: 'view', inbox: 'edit', termine: 'view', rezepte: 'view', routing: 'none', kommunikation: 'view', kommunikation_templates: 'none', oeffnungszeiten: 'view', vertretung: 'none', prompts: 'none', training: 'none', statistiken: 'view', kosten: 'none', benutzerverwaltung: 'none', system_settings: 'none', audit_log: 'none' }
    - viewer: { dashboard: 'view', telefonate: 'view', audio: 'none', transkripte: 'none', patientendaten: 'none', inbox: 'view', termine: 'none', rezepte: 'none', routing: 'none', kommunikation: 'none', kommunikation_templates: 'none', oeffnungszeiten: 'none', vertretung: 'none', prompts: 'none', training: 'none', statistiken: 'view', kosten: 'none', benutzerverwaltung: 'none', system_settings: 'none', audit_log: 'none' }

    Export functions:
    - canGrantPermission(grantorLevel: PermissionLevel, targetLevel: PermissionLevel): boolean
      → PERMISSION_LEVEL_ORDER[grantorLevel] >= PERMISSION_LEVEL_ORDER[targetLevel]

    - canGrantRole(grantorRole: AraRole, targetRole: AraRole): boolean
      → ROLE_HIERARCHY[grantorRole] > ROLE_HIERARCHY[targetRole]
      → operator can grant any lower role; ordination_admin can only grant assistant/viewer

    - applyDelegationCeiling(grantorPermissions: ModulePermissions, targetPermissions: Partial<ModulePermissions>): ModulePermissions
      → For each of 20 MODULE_CATEGORIES: cap targetPermissions[cat] to grantorPermissions[cat]
      → Returns complete ModulePermissions with every category set

    - getDefaultPermissions(role: AraRole): ModulePermissions
      → Returns DEFAULT_PERMISSIONS[role]
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export const MODULE_CATEGORIES" src/lib/permissions.ts outputs 1
    - grep -c "export function" src/lib/permissions.ts outputs at least 4 (canGrantPermission, canGrantRole, applyDelegationCeiling, getDefaultPermissions)
    - MODULE_CATEGORIES has exactly 20 entries: grep -c "'" src/lib/permissions.ts in the MODULE_CATEGORIES array shows 20 strings
    - DEFAULT_PERMISSIONS covers all 4 roles (operator, ordination_admin, assistant, viewer)
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>src/lib/permissions.ts created with MODULE_CATEGORIES (20), PERMISSION_LEVELS, DEFAULT_PERMISSIONS (4 roles), and 4 exported functions.</done>
</task>

<task type="auto">
  <name>Task 3: Add Statistiken and Benutzer nav links to dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (read full file — understand current nav structure and user/role extraction)
  </read_first>
  <action>
    Update src/app/(dashboard)/layout.tsx:

    1. Extract araRole from user: const araRole = (user.app_metadata?.ara_role as string | undefined) ?? ''
       (araRole is already derivable from the same user.app_metadata block — add this line after tenantId extraction)

    2. Add Statistiken link AFTER Inbox and BEFORE Konfiguration in the nav:
       <a href="/statistiken" className="rounded px-2 py-1.5 text-gray-700 hover:bg-gray-100">
         Statistiken
       </a>

    3. Add Benutzer link BEFORE Einstellungen, conditionally rendered for operator/ordination_admin:
       {['operator', 'ordination_admin'].includes(araRole) && (
         <a href="/benutzer" className="rounded px-2 py-1.5 text-gray-700 hover:bg-gray-100">
           Benutzer
         </a>
       )}

    Final nav order: Übersicht → Telefonate → Inbox → Statistiken → Konfiguration → Benutzer (conditional) → Einstellungen
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "Statistiken" src/app/(dashboard)/layout.tsx — link present
    - grep "Benutzer" src/app/(dashboard)/layout.tsx — link present with role condition
    - grep "ordination_admin" src/app/(dashboard)/layout.tsx confirms role gate on Benutzer link
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Statistiken and Benutzer nav links added to dashboard layout.</done>
</task>

<task type="auto">
  <name>Task 4: Create stub page stubs for /statistiken and /benutzer</name>
  <files>
    src/app/(dashboard)/statistiken/page.tsx,
    src/app/(dashboard)/benutzer/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/inbox/page.tsx (Server Component page pattern: createServerClient, getUser, redirect on no user)
  </read_first>
  <action>
    Create src/app/(dashboard)/statistiken/page.tsx:
    - Server Component (no 'use client')
    - Import createServerClient, redirect
    - Get user → redirect to /auth/login if not authenticated
    - Return a page with heading "Statistiken" and a placeholder Card containing "Statistiken werden geladen…"
    - Export default async function StatistikenPage

    Create src/app/(dashboard)/benutzer/page.tsx:
    - Server Component (no 'use client')
    - Import createServerClient, redirect
    - Get user → redirect to /auth/login if not authenticated
    - Extract araRole; if not operator/ordination_admin, redirect to /dashboard
    - Return a page with heading "Benutzerverwaltung" and a placeholder Card containing "Benutzer werden geladen…"
    - Export default async function BenutzerPage

    Both pages must compile without TypeScript errors. They are stubs — Plans 07-02 and 07-03 will overwrite with full implementations.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and export a default async function
    - /statistiken stub has no role gate (accessible to all authenticated users)
    - /benutzer stub redirects non-admin roles to /dashboard
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Stub pages created for /statistiken and /benutzer. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 5: Git commit foundation</name>
  <files>all modified and created files</files>
  <action>
    Stage and commit all changes:
    - src/lib/types/index.ts (Phase 07 types appended)
    - src/lib/permissions.ts (new — permissions utility module)
    - src/app/(dashboard)/layout.tsx (Statistiken + Benutzer nav links)
    - src/app/(dashboard)/statistiken/page.tsx (stub)
    - src/app/(dashboard)/benutzer/page.tsx (stub)

    Commit message: "feat(phase-07): foundation — Phase 07 types, permissions utility, nav links, stub pages"
  </action>
  <acceptance_criteria>
    - git status shows clean working tree after commit
    - git log --oneline -1 confirms the commit exists
  </acceptance_criteria>
  <done>All 5 files committed. Foundation for Phase 7 in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Layout | araRole read from server-side user.app_metadata — not from request params |
| Browser → /benutzer | Server-side redirect for non-admin roles — client cannot bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation | /benutzer page access by assistant/viewer | mitigate | Server-side redirect in page.tsx — araRole from JWT app_metadata, not from URL |
| T-07-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
After all tasks:
- grep "Phase 07" src/lib/types/index.ts — section header present
- grep "export const MODULE_CATEGORIES" src/lib/permissions.ts — utility exported
- grep "Statistiken" src/app/(dashboard)/layout.tsx — nav link present
- grep "Benutzer" src/app/(dashboard)/layout.tsx — nav link with role gate present
- npx tsc --noEmit exits 0
- Both stub pages exist and export valid default async functions
</verification>

<success_criteria>
- Phase 07 type block added to src/lib/types/index.ts (PermissionLevel, ModuleCategory, ModulePermissions, TenantUserDisplay, StatsSummary, DailyCallVolume, IntentFrequency)
- src/lib/permissions.ts created with 20 MODULE_CATEGORIES, DEFAULT_PERMISSIONS for all 4 roles, and 4 utility functions
- Dashboard nav has Statistiken (all users) and Benutzer (operator/ordination_admin only) links
- /statistiken and /benutzer stubs exist and compile without TypeScript errors
</success_criteria>

<output>
Create .planning/phases/07-statistics-user-mgmt/07-01-SUMMARY.md when done.
</output>
