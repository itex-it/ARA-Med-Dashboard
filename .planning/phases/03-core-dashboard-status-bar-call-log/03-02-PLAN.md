---
phase: 03-core-dashboard-status-bar-call-log
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/ui/badge.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/table.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/select.tsx
  - src/components/ui/switch.tsx
  - src/app/(dashboard)/layout.tsx
  - src/components/status/StatusBar.tsx
  - src/app/actions/toggle-ara-med.ts
  - src/lib/hooks/useTenantStatus.ts
autonomous: true
requirements: [STATUS-01, STATUS-02, STATUS-03, STATUS-04, STATUS-05]
must_haves:
  truths:
    - "On every dashboard page the status bar shows ARA-MED state, practice state, active mode, active-call count, and open-task count"
    - "All five segments update live without a page refresh via Supabase Realtime on the tenants table"
    - "A user with operator or ordination_admin role can flip the ARA-MED switch and the ara_status change takes effect immediately"
    - "Users without the toggle role see the status indicator but no switch"
  artifacts:
    - path: "src/components/status/StatusBar.tsx"
      provides: "Full status-bar Client Component with 6 segments"
      min_lines: 60
    - path: "src/app/actions/toggle-ara-med.ts"
      provides: "Server Action to flip tenants.ara_status"
      exports: ["toggleAraMedAction"]
    - path: "src/lib/hooks/useTenantStatus.ts"
      provides: "Realtime hook for tenants status columns"
      exports: ["useTenantStatus"]
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/status/StatusBar.tsx"
      via: "render StatusBar with tenantId + canToggle props"
      pattern: "StatusBar"
    - from: "src/components/status/StatusBar.tsx"
      to: "src/app/actions/toggle-ara-med.ts"
      via: "Server Action call on switch toggle"
      pattern: "toggleAraMedAction"
    - from: "src/lib/hooks/useTenantStatus.ts"
      to: "tenants"
      via: "useRealtimeChannel on tenants table"
      pattern: "table: 'tenants'"
---

<objective>
Install the shadcn components Phase 3 needs and complete the status bar: a Client Component `StatusBar` showing all five status segments live, plus a Server Action to toggle ARA-MED on/off.

Purpose: This is the first vertical slice of Phase 3. After this plan a practice staff member sees the live ARA-MED state on every page and can pause/resume the Voice AI with one click — STATUS-01..05 fully delivered.
Output: 7 shadcn UI files, `StatusBar.tsx`, `toggle-ara-med.ts` Server Action, `useTenantStatus.ts` hook, and an updated `layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-core-dashboard-status-bar-call-log/03-UI-SPEC.md

<interfaces>
<!-- Patterns the executor must follow — extracted from the codebase. No exploration needed. -->

From src/app/(dashboard)/layout.tsx — the status-bar shell to EXTEND (not replace):
- layout.tsx is a Server Component; gets `user` via `supabase.auth.getUser()`; `tenantId = user.app_metadata?.tenant_id`
- Status-bar shell: `<div className="flex items-center border-b px-6 py-2">` currently containing only `<OpenTaskCounter tenantId={tenantId} />`
- ara_role is at `user.app_metadata?.ara_role`

From src/lib/hooks/useRealtimeChannel.ts — the Realtime hook to reuse:
- `useRealtimeChannel<T extends { [key: string]: unknown }>({ table, tenantId, events, onChange })`
- Channel scoped `${table}:${tenantId}`, filter `tenant_id=eq.${tenantId}`, single `event:'*'`, removeChannel cleanup
- NOTE: tenants rows key on `id`, not `tenant_id`. The hook filters on `tenant_id`. For the tenants table, query/subscribe filtered on `id` — see Task 3 action for the workaround.

From src/components/status/OpenTaskCounter.tsx — existing segment, keep as-is, mount inside StatusBar.

From src/app/(dashboard)/settings/actions.ts — Server Action pattern (toggle-ara-med.ts must mirror this):
- 'use server' directive at top
- auth via `createServerClient()` then `auth.getUser()` — never getSession()
- tenant_id and ara_role from `user.app_metadata` — NEVER from formData/args
- role gate: allowedRoles = ['operator','ordination_admin']
- DB write via `createServiceRoleClient()` (tenants has no RLS)
- returns a typed state object, never void

From src/lib/types/index.ts (after 03-01): AraStatus, PracticeStatus, ActiveMode, TenantRow (now with ara_status/practice_status/active_mode).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components and tenant status Realtime hook</name>
  <files>src/components/ui/badge.tsx, src/components/ui/sheet.tsx, src/components/ui/table.tsx, src/components/ui/separator.tsx, src/components/ui/textarea.tsx, src/components/ui/select.tsx, src/components/ui/switch.tsx, src/lib/hooks/useTenantStatus.ts</files>
  <read_first>
    - src/lib/hooks/useOpenTaskCount.ts (hook + initial-query pattern to mirror)
    - src/lib/hooks/useRealtimeChannel.ts (Realtime hook contract)
    - components.json (shadcn config — new-york, neutral, lucide)
  </read_first>
  <action>
    Run `npx shadcn@latest add badge sheet table separator textarea select switch` from the project root. Accept overwrite=no for any already-present file. Verify the 7 files land in `src/components/ui/`.
    Then create `src/lib/hooks/useTenantStatus.ts` — a `'use client'` hook `useTenantStatus(tenantId: string)` that returns `{ status: { ara_status, practice_status, active_mode } | null, loading: boolean }`.
    Initial fetch: `createClient().from('tenants').select('ara_status, practice_status, active_mode').eq('id', tenantId).single()` — set state, set loading false.
    Live updates: the tenants table is keyed on `id` not `tenant_id`, so do NOT use `useRealtimeChannel` (it filters on tenant_id). Instead subscribe directly: `createClient().channel('tenants:' + tenantId).on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'tenants', filter: 'id=eq.' + tenantId }, handler)` then `.subscribe()`. In the handler, set status from `payload.new`. Clean up with `supabase.removeChannel(channel)` in the useEffect return (C4). Guard on empty tenantId. No `any` — type the payload as `RealtimePostgresChangesPayload<TenantRow>`.
  </action>
  <verify>
    <automated>ls src/components/ui/badge.tsx src/components/ui/sheet.tsx src/components/ui/table.tsx src/components/ui/separator.tsx src/components/ui/textarea.tsx src/components/ui/select.tsx src/components/ui/switch.tsx src/lib/hooks/useTenantStatus.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 7 shadcn component files exist in src/components/ui/
    - useTenantStatus.ts exists, starts with `'use client'`, exports `useTenantStatus`
    - The hook subscribes filtered on `id=eq.` (not tenant_id) and calls `removeChannel` in cleanup
    - `npx tsc --noEmit` exits 0; no `any`
  </acceptance_criteria>
  <done>shadcn components installed; useTenantStatus delivers live tenant status with proper channel cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: ARA-MED toggle Server Action</name>
  <files>src/app/actions/toggle-ara-med.ts</files>
  <read_first>
    - src/app/(dashboard)/settings/actions.ts (Server Action security pattern — auth, role gate, service-role write)
    - src/lib/supabase/server.ts (createServerClient + createServiceRoleClient)
    - src/lib/types/index.ts (AraStatus type from 03-01)
  </read_first>
  <action>
    Create `src/app/actions/toggle-ara-med.ts` with `'use server'` at the top.
    Export `interface ToggleAraMedState { success?: boolean; error?: string; ara_status?: AraStatus }`.
    Export async function `toggleAraMedAction(nextStatus: AraStatus): Promise<ToggleAraMedState>`.
    Validate `nextStatus` against the allowed set `['active','paused','error']` — reject anything else with `{ error: 'Ungültiger Status.' }`.
    Auth: `createServerClient()` then `auth.getUser()`. If no user → `{ error: 'Nicht authentifiziert.' }`.
    Read `tenant_id` and `ara_role` from `user.app_metadata` — NEVER from the argument. If no tenant_id → `{ error: 'Kein Tenant zugewiesen.' }`.
    Role gate: allowedRoles `['operator','ordination_admin']`; if `ara_role` not in set → `{ error: 'Unzureichende Berechtigung.' }`.
    Write via `createServiceRoleClient().from('tenants').update({ ara_status: nextStatus }).eq('id', tenantId)`. On DB error log `console.error` and return `{ error: 'Aktion fehlgeschlagen. Bitte erneut versuchen.' }` (matches UI-SPEC toggle error toast copy).
    On success return `{ success: true, ara_status: nextStatus }`.
  </action>
  <verify>
    <automated>grep -q "'use server'" src/app/actions/toggle-ara-med.ts && grep -q "toggleAraMedAction" src/app/actions/toggle-ara-med.ts && grep -q "app_metadata" src/app/actions/toggle-ara-med.ts && grep -q "createServiceRoleClient" src/app/actions/toggle-ara-med.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use server'`
    - `tenant_id` and `ara_role` are read from `user.app_metadata`, not from the function argument (grep confirms no `tenant_id` taken from args)
    - Role gate restricts to operator/ordination_admin
    - DB write uses `createServiceRoleClient()` and `.eq('id', tenantId)`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>toggleAraMedAction flips tenants.ara_status server-side with auth + role enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: StatusBar component and layout wiring</name>
  <files>src/components/status/StatusBar.tsx, src/app/(dashboard)/layout.tsx</files>
  <read_first>
    - src/app/(dashboard)/layout.tsx (the file being modified — read fully)
    - src/components/status/OpenTaskCounter.tsx (segment to mount inside StatusBar)
    - .planning/phases/03-core-dashboard-status-bar-call-log/03-UI-SPEC.md (Status Bar Design Contract — section, exact copy strings, colors)
  </read_first>
  <action>
    Create `src/components/status/StatusBar.tsx` as a `'use client'` component `StatusBar({ tenantId, canToggle }: { tenantId: string; canToggle: boolean })`.
    Use `useTenantStatus(tenantId)` for live ara_status/practice_status/active_mode.
    Render a horizontal flex row of six segments, each separated by `<Separator orientation="vertical" className="h-4" />` (shadcn separator):
    1. ARA-MED Status — 8px dot + label. Map ara_status: 'active' → green dot with `animate-pulse` + "ARA-MED aktiv" (text-green-600); 'paused' → static yellow dot + "ARA-MED pausiert" (text-yellow-600); 'error' → red dot + "ARA-MED gestört" (text-destructive). Use `text-sm font-medium`.
    2. Praxis-Status — label only, `text-sm`. Map practice_status: 'open' → "Praxis geöffnet" (text-green-600); 'closed' → "Praxis geschlossen" (text-muted-foreground); 'special' → "Sondermodus" (text-yellow-600).
    3. Aktiver Modus — `text-xs text-muted-foreground`. Map active_mode: normal→"Normalbetrieb", vacation→"Urlaubsmodus", deputy→"Vertretungsmodus", overload→"Überlastung".
    4. Aktive Gespräche — `text-xs text-muted-foreground` label "Aktive Gespräche" + a count badge. For this MVP slice the active-call count source is `call_log` rows with `status='active'`: fetch initial count via `createClient().from('call_log').select('id',{count:'exact',head:true}).eq('tenant_id',tenantId).eq('status','active')` inside a small inline `useState`+`useEffect`, and subscribe via `useRealtimeChannel<CallLogRow & {[k:string]:unknown}>` on table `call_log` adjusting the count on INSERT/UPDATE/DELETE based on whether status crosses in/out of 'active' (mirror useOpenTaskCount's delta logic). Badge: count>0 → `bg-primary text-primary-foreground`, else `bg-muted text-muted-foreground`, both `text-xs font-semibold rounded-full px-2 py-0.5`.
    5. `<OpenTaskCounter tenantId={tenantId} />` — mount existing component unchanged.
    6. ARA-MED Toggle — right-aligned via `ml-auto`. Render only when `canToggle` is true. Use shadcn `<Switch>` with label "ARA-MED" left of it. `checked` = ara_status==='active'. On change: use React 19 `useOptimistic` to flip the displayed status immediately, then call `toggleAraMedAction(checked ? 'paused' : 'active')`. While the action is pending show Lucide `Loader2` with `animate-spin` and set Switch `disabled`. On error result, revert optimistic state (the Realtime UPDATE will also correct it).
    During `loading` from useTenantStatus, render muted placeholder dashes — never crash on null status.
    Then edit `src/app/(dashboard)/layout.tsx`: keep it a Server Component. Compute `canToggle = ['operator','ordination_admin'].includes(user.app_metadata?.ara_role)`. Replace the inner content of the `<div className="flex items-center border-b px-6 py-2">` — remove the direct `<OpenTaskCounter>` (it now lives inside StatusBar) and render `<StatusBar tenantId={tenantId} canToggle={canToggle} />`. Update the import.
  </action>
  <verify>
    <automated>grep -q "StatusBar" src/app/\(dashboard\)/layout.tsx && grep -q "useTenantStatus" src/components/status/StatusBar.tsx && grep -q "toggleAraMedAction" src/components/status/StatusBar.tsx && grep -q "OpenTaskCounter" src/components/status/StatusBar.tsx && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - StatusBar.tsx starts with `'use client'` and renders 6 segments with vertical Separators
    - All status copy strings match the UI-SPEC Copywriting Contract exactly (German, "ARA-MED" branding, no "ElevenLabs"/"n8n")
    - Toggle Switch renders only when `canToggle` is true; uses useOptimistic + Loader2 spinner
    - layout.tsx stays a Server Component, passes `tenantId` and `canToggle`, and OpenTaskCounter is no longer rendered directly in layout.tsx
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>The status bar shows all 5 live segments on every page; permitted users can toggle ARA-MED in one click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → toggleAraMedAction | Untrusted client requests an ARA-MED state change |
| browser → tenants Realtime channel | Client subscribes to tenant status updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | toggleAraMedAction | mitigate | tenant_id + ara_role read from JWT app_metadata; role gate restricts write to operator/ordination_admin (C2) |
| T-03-06 | Tampering | nextStatus argument | mitigate | nextStatus validated against the allowed enum before any DB write |
| T-03-07 | Information Disclosure | tenants Realtime channel | mitigate | channel + postgres_changes filter scoped to `id=eq.{tenantId}`; client cannot subscribe to other tenants (C3) |
| T-03-08 | Denial of Service | tenants channel not cleaned up | mitigate | useEffect return calls removeChannel — no leaked subscriptions (C4) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` succeeds
- Status bar renders 6 segments; toggle hidden for viewer/assistant roles
- All copy strings match UI-SPEC; no provider brand names present
</verification>

<success_criteria>
- STATUS-01..04: status bar shows ARA-MED state, practice state, active mode, active-call count, open-task count — all live
- STATUS-05: operator/ordination_admin can toggle ARA-MED; change persists via Server Action and reflects via Realtime
</success_criteria>

<output>
Create `.planning/phases/03-core-dashboard-status-bar-call-log/03-02-SUMMARY.md` when done.
</output>
