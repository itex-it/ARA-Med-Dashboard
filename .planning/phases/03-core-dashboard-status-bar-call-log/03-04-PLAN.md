---
phase: 03-core-dashboard-status-bar-call-log
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/app/api/calls/[id]/audio/route.ts
  - src/app/actions/call-feedback.ts
  - src/components/calls/CallDetailSheet.tsx
  - src/components/calls/CallLogTable.tsx
  - src/app/(dashboard)/telefonate/page.tsx
autonomous: true
requirements: [CALL-03, CALL-04, CALL-05, CALL-06, CALL-07, CALL-08, CALL-09, CALL-10]
must_haves:
  truths:
    - "Clicking a call row opens a slide-in detail sheet showing summary, contact data, executed actions, audio, transcript, and AI decision path"
    - "The audio player loads its source from a 15-minute presigned URL fetched only when the sheet opens"
    - "A user can save an internal note, correct the intent category, and set a feedback label — all persisted to call_log"
    - "Executed MEDSTAR actions are listed with their API response status (Erfolg/Fehler/Ausstehend)"
  artifacts:
    - path: "src/app/api/calls/[id]/audio/route.ts"
      provides: "Presigned audio URL endpoint (15-min expiry, tenant-checked)"
      exports: ["GET"]
    - path: "src/app/actions/call-feedback.ts"
      provides: "Server Action for note, intent correction, feedback label"
      exports: ["saveCallFeedbackAction"]
    - path: "src/components/calls/CallDetailSheet.tsx"
      provides: "Call detail slide-over panel"
      min_lines: 90
  key_links:
    - from: "src/components/calls/CallLogTable.tsx"
      to: "src/components/calls/CallDetailSheet.tsx"
      via: "onRowClick opens sheet with selected call id"
      pattern: "CallDetailSheet"
    - from: "src/components/calls/CallDetailSheet.tsx"
      to: "/api/calls/[id]/audio"
      via: "fetch on sheet open"
      pattern: "api/calls"
    - from: "src/components/calls/CallDetailSheet.tsx"
      to: "src/app/actions/call-feedback.ts"
      via: "Server Action on note/feedback submit"
      pattern: "saveCallFeedbackAction"
---

<objective>
Build the call detail Sheet: a slide-in panel opened from any call row showing the AI summary, contact data, executed MEDSTAR actions, audio recording, transcript, AI decision path, plus note / intent-correction / feedback inputs.

Purpose: Completes the Phase 3 call log — staff can fully review and annotate any call. Vertical slice: detail sheet + audio API route + feedback Server Action, wired into the existing CallLogTable. Delivers CALL-03..10.
Output: audio URL API route, `saveCallFeedbackAction` Server Action, `CallDetailSheet` component, `onRowClick` wiring in `CallLogTable`, and permission-prop pass-through in `/telefonate/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/03-core-dashboard-status-bar-call-log/03-UI-SPEC.md

<interfaces>
<!-- Patterns the executor must follow. No codebase exploration needed. -->

From src/app/api/internal/events/route.ts pattern + src/lib/supabase/server.ts — API route + service-role pattern:
- API route: `export async function GET(request: Request, { params }: { params: Promise<{ id: string }> })` — Next.js 16 async params
- `createServerClient()` for the authenticated user check; `createServiceRoleClient()` for privileged reads
- Service-Role client: `import 'server-only'`-guarded in server.ts — never reaches the browser

From src/app/(dashboard)/settings/actions.ts — Server Action security pattern:
- 'use server'; auth via createServerClient().auth.getUser(); tenant_id + ara_role from user.app_metadata
- service-role write; typed non-void return state

From src/lib/types/index.ts (after 03-01): CallLogRow (incl. feedback_label, internal_note, intent_corrected, summary_structured, transcript_text, audio_url), CallActionRow, FeedbackLabel, CallActionType, MedstarActionStatus.

From src/components/calls/CallLogTable.tsx (created in 03-03) — already accepts an optional `onRowClick?: (id: string) => void` prop and the rows are clickable. It currently takes only `{ tenantId }`.

shadcn `sheet`, `textarea`, `select`, `button`, `badge` are all installed by 03-02 (a `depends_on` of this plan) — import them directly; do NOT run `npx shadcn add`.

STATE.md C7 decision: audio presigned URL generated on-demand via API route, 15-min expiry, never at page load. Supabase Storage signed URL: `serviceClient.storage.from(bucket).createSignedUrl(path, 900)` (900s = 15min).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audio presigned-URL API route</name>
  <files>src/app/api/calls/[id]/audio/route.ts</files>
  <read_first>
    - src/app/api/internal/events/route.ts (API route structure, error handling)
    - src/lib/supabase/server.ts (createServerClient + createServiceRoleClient)
    - .planning/STATE.md (C7 decision — audio URL on-demand, 15-min expiry)
  </read_first>
  <action>
    Create `src/app/api/calls/[id]/audio/route.ts` with `export async function GET(request: Request, { params }: { params: Promise<{ id: string }> })`.
    Await `params` to get the call `id`.
    Auth: `createServerClient()` → `auth.getUser()`. If no user, return `new Response(null, { status: 401 })`.
    Read `tenant_id` from `user.app_metadata` — never from the request. If absent, return 401.
    Use `createServiceRoleClient()` to read the call row: `.from('call_log').select('audio_url, tenant_id').eq('id', id).single()`.
    Tenant check (defense-in-depth — RLS would also block, but service-role bypasses RLS): if the row's `tenant_id` !== the JWT tenant_id, return `new Response(null, { status: 403 })`.
    If the row has no `audio_url` (null), return `new Response(null, { status: 404 })`.
    Generate a signed URL: treat `audio_url` as a storage object path within a `call-recordings` bucket — `serviceClient.storage.from('call-recordings').createSignedUrl(audioPath, 900)`. If `audio_url` is already a full https URL (legacy/external), skip signing and use it directly. On signing error return 404.
    Return `Response.json({ url: signedUrl })`. Add a doc comment: never cache, never expose at page-load (C7).
  </action>
  <verify>
    <automated>grep -q "export async function GET" src/app/api/calls/\[id\]/audio/route.ts && grep -q "createSignedUrl" src/app/api/calls/\[id\]/audio/route.ts && grep -q "app_metadata" src/app/api/calls/\[id\]/audio/route.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GET handler awaits Next.js 16 async `params` for the call id
    - Unauthenticated → 401; cross-tenant call id → 403; missing audio_url → 404
    - tenant_id read from JWT app_metadata, compared against the row's tenant_id
    - Signed URL created with a 900-second (15-minute) expiry
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GET /api/calls/[id]/audio returns a tenant-checked 15-minute presigned audio URL.</done>
</task>

<task type="auto">
  <name>Task 2: Call feedback / note / intent-correction Server Action</name>
  <files>src/app/actions/call-feedback.ts</files>
  <read_first>
    - src/app/(dashboard)/settings/actions.ts (Server Action security + Zod pattern)
    - src/app/actions/toggle-ara-med.ts (Phase 3 Server Action shape from 03-02)
    - src/lib/types/index.ts (FeedbackLabel, CallLogRow)
  </read_first>
  <action>
    Create `src/app/actions/call-feedback.ts` with `'use server'`.
    Export `interface CallFeedbackState { success?: boolean; error?: string }`.
    Export async `saveCallFeedbackAction(input: { callId: string; internalNote?: string; intentCorrected?: string; feedbackLabel?: FeedbackLabel | null }): Promise<CallFeedbackState>`.
    Validate `input` with a Zod v4 schema: `callId` a uuid; `internalNote` optional string max 4000; `intentCorrected` optional string max 200; `feedbackLabel` optional enum `['correct','incorrect','needs_training']` or null. On parse failure return `{ error: 'Ungültige Eingabe.' }`.
    Auth: `createServerClient().auth.getUser()`. No user → `{ error: 'Nicht authentifiziert.' }`. Read `tenant_id` from `user.app_metadata`; if absent → `{ error: 'Kein Tenant zugewiesen.' }`.
    Write via `createServiceRoleClient().from('call_log').update({ ... }).eq('id', callId).eq('tenant_id', tenantId)` — include the explicit `.eq('tenant_id', tenantId)` so a forged callId from another tenant matches zero rows (defense-in-depth alongside RLS). Only include fields that were provided in `input` in the update object (build it conditionally so an unset field is not overwritten with null).
    On DB error: `console.error` and return `{ error: 'Notiz konnte nicht gespeichert werden.' }` (matches UI-SPEC note save error copy). On success `{ success: true }`.
  </action>
  <verify>
    <automated>grep -q "'use server'" src/app/actions/call-feedback.ts && grep -q "saveCallFeedbackAction" src/app/actions/call-feedback.ts && grep -q "app_metadata" src/app/actions/call-feedback.ts && grep -q "eq('tenant_id'" src/app/actions/call-feedback.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File starts with `'use server'`; exports `saveCallFeedbackAction`
    - Input validated with Zod; feedbackLabel restricted to the 3 valid values
    - tenant_id read from JWT; update is filtered by both `id` and `tenant_id`
    - Unset input fields are not written (no accidental null overwrite)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>saveCallFeedbackAction persists note, intent correction, and feedback label with tenant-scoped enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: CallDetailSheet component and CallLogTable wiring</name>
  <files>src/components/calls/CallDetailSheet.tsx, src/components/calls/CallLogTable.tsx, src/app/(dashboard)/telefonate/page.tsx</files>
  <read_first>
    - .planning/phases/03-core-dashboard-status-bar-call-log/03-UI-SPEC.md (Call Detail Sheet Design Contract — all 8 sections, Copywriting Contract, DSGVO/Permission Gates)
    - src/components/calls/CallLogTable.tsx (the file being modified — read fully; it has onRowClick)
    - src/lib/types/index.ts (CallLogRow, CallActionRow, FeedbackLabel, MedstarActionStatus)
  </read_first>
  <action>
    Create `src/components/calls/CallDetailSheet.tsx` — `'use client'` component `CallDetailSheet({ call, open, onOpenChange, canSeeAudio, canSeeTranscript, canSeeDetail }: { call: CallLogRow | null; open: boolean; onOpenChange: (o: boolean) => void; canSeeAudio: boolean; canSeeTranscript: boolean; canSeeDetail: boolean })`.
    Render shadcn `<Sheet open={open} onOpenChange={onOpenChange}>` with `<SheetContent side="right" className="w-[480px] sm:max-w-[480px] overflow-y-auto">`.
    Header: `<SheetTitle>Gesprächsdetails</SheetTitle>` (text-xl font-semibold) + subtitle line with `formatCallTime(call.created_at)` and `maskPhone(call.phone_hash)` (text-sm text-muted-foreground).
    Sections top-to-bottom, separated by `gap-8` vertical spacing, each with a `text-xs font-semibold uppercase tracking-wide text-muted-foreground` heading:
    1. "Zusammenfassung" (CALL-07): `call.summary_short` as `<p className="text-sm">`; render `call.summary_structured` (if present) as a labeled list — Intent, Aktionen, Offene Punkte, Rückruf nötig — reading keys `intent_main`, `actions_done`, `open_items`, `callback_needed` from the jsonb object (treat unknown shape safely; it is `Record<string, unknown> | null`).
    2. "Kontaktdaten" (CALL-03): always show `maskPhone(call.phone_hash)`. Since call_log stores only phone_hash there is no raw number to reveal; when `canSeeDetail` is true also render the full `phone_hash` value as a monospace string (the strongest identifier available). Add a code comment that a future raw-number column would be revealed here gated on `canSeeDetail`.
    3. "Ausgeführte Aktionen" (CALL-04, CALL-10): when the sheet opens, fetch `call_actions` for this call — `createClient().from('call_actions').select('*').eq('call_log_id', call.id).order('created_at')`. Render each as a row: a Lucide icon by `action_type`, a German label, and a status chip from `medstar_status` — "Erfolg" (green), "Fehler" (red), "Ausstehend" (muted). German action labels: appointment_booked→"Termin gebucht", appointment_cancelled→"Termin storniert", appointment_rescheduled→"Termin verschoben", prescription_ordered→"Rezept bestellt", message_created→"Nachricht erstellt", forwarding→"Weiterleitung", emergency_notice→"Notfallhinweis".
    4. "Gesprächsaufzeichnung" (CALL-05): render only if `canSeeAudio` AND `call.audio_url` is not null. On sheet open, `fetch('/api/calls/' + call.id + '/audio')`, read `{ url }`, then render `<audio controls src={url} className="w-full" />`. While fetching show a skeleton bar `<div className="h-10 w-full rounded bg-muted animate-pulse" />`.
    5. "Transkript" (CALL-06): render only if `canSeeTranscript`. Collapsible — a button toggling visibility, label "Transkript anzeigen" / "Transkript ausblenden"; content `<pre className="text-xs whitespace-pre-wrap font-mono bg-muted p-4 rounded-md">{call.transcript_text}</pre>`. Collapsed by default.
    6. "KI-Entscheidungsweg" (CALL-10): render only if `canSeeDetail`. Collapsible, collapsed by default; render `call.summary_structured` (the structured object) as read-only labeled steps / formatted JSON.
    7. "Interne Notiz" (CALL-08): `<Textarea defaultValue={call.internal_note ?? ''} placeholder="Interne Notiz hinzufügen..." rows={3} className="text-sm" />` + an Intent-Korrektur `<Select>` (label "Intent-Korrektur (optional)") whose options are a small fixed intent list plus the current `intent_main`. A `<Button size="sm">Notiz speichern</Button>` calls `saveCallFeedbackAction({ callId: call.id, internalNote, intentCorrected })`. On error show the German error string from the action.
    8. "Feedback" (CALL-09): three `<Button variant="outline" size="sm">` side by side — "Korrekt", "Falsch erkannt", "Training nötig". The currently-selected one (matching `call.feedback_label`) uses `variant="default"`. Clicking one calls `saveCallFeedbackAction({ callId: call.id, feedbackLabel })` with the mapped value (`correct`/`incorrect`/`needs_training`).
    When `call` is null, render the Sheet with no body.
    Then edit `src/components/calls/CallLogTable.tsx`: extend its props to `{ tenantId: string; canSeeAudio?: boolean; canSeeTranscript?: boolean; canSeeDetail?: boolean }` (each defaulting to `true`). Add local state `selectedId` (string|null) and `sheetOpen` (boolean); pass `onRowClick={(id) => { setSelectedId(id); setSheetOpen(true) }}` into the existing row click handler; derive the selected `CallLogRow` from the `calls` array by `selectedId`; render `<CallDetailSheet call={selectedCall} open={sheetOpen} onOpenChange={setSheetOpen} canSeeAudio={canSeeAudio} canSeeTranscript={canSeeTranscript} canSeeDetail={canSeeDetail} />`.
    Then edit `src/app/(dashboard)/telefonate/page.tsx`: pass `canSeeAudio`, `canSeeTranscript`, `canSeeDetail` to `<CallLogTable>` — for MVP pass all three as `true` with a code comment that Phase 7 RBAC will compute the real per-user values server-side from `user_tenant_roles.permissions`.
  </action>
  <verify>
    <automated>grep -q "CallDetailSheet" src/components/calls/CallLogTable.tsx && grep -q "saveCallFeedbackAction" src/components/calls/CallDetailSheet.tsx && grep -q "Gesprächsdetails" src/components/calls/CallDetailSheet.tsx && grep -q "api/calls" src/components/calls/CallDetailSheet.tsx && grep -q "canSeeAudio" src/app/\(dashboard\)/telefonate/page.tsx && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CallDetailSheet renders all 8 UI-SPEC sections with the exact German section headings and copy
    - Audio section fetches the presigned URL on open and renders `<audio controls>`; hidden when no audio or no permission
    - Transcript and KI-Entscheidungsweg are collapsible, collapsed by default, permission-gated
    - Note save and the 3 feedback buttons call `saveCallFeedbackAction`
    - CallLogTable opens the sheet on row click with the selected call; /telefonate passes the 3 permission props
    - `npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Clicking a call opens a full detail sheet — summary, contact, actions, audio, transcript, decision path, note, feedback — all wired and persisting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/calls/[id]/audio | Client requests a recording for an arbitrary call id |
| browser → saveCallFeedbackAction | Client submits note/feedback for an arbitrary call id |
| browser → call_actions query | Client reads executed-action rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | /api/calls/[id]/audio with forged id | mitigate | route reads tenant_id from JWT, compares to the call row's tenant_id, returns 403 on mismatch before signing |
| T-03-14 | Information Disclosure | audio presigned URL longevity | mitigate | signed URL TTL fixed at 900s; generated on sheet open only, never at page load (C7) |
| T-03-15 | Tampering | saveCallFeedbackAction with cross-tenant callId | mitigate | update filtered by both `id` and JWT `tenant_id` — forged id matches zero rows; RLS UPDATE policy is the backstop |
| T-03-16 | Information Disclosure | transcript / audio / decision path exposure | mitigate | CallDetailSheet permission-gates audio/transcript/decision sections; props flow from the server-rendered page (full RBAC enforced server-side in Phase 7) |
| T-03-17 | Information Disclosure | call_actions cross-tenant read | mitigate | call_actions RLS SELECT policy (03-01) + `.eq('call_log_id', call.id)` where the call itself is tenant-scoped |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0; `npm run build` succeeds
- Detail sheet shows all 8 sections; permission-gated sections hide correctly
- Audio URL endpoint enforces 401/403/404; signed URL TTL is 900s
- All copy German; no provider brand names
</verification>

<success_criteria>
- CALL-03: contact data section (masked + detail-gated identifier)
- CALL-04/CALL-10: executed MEDSTAR actions with API response status + KI decision path
- CALL-05: audio playback via 15-minute presigned URL fetched on demand
- CALL-06: transcript view, permission-gated
- CALL-07: AI summary (short + structured)
- CALL-08/CALL-09: note, intent correction, and feedback label persist via Server Action
</success_criteria>

<output>
Create `.planning/phases/03-core-dashboard-status-bar-call-log/03-04-SUMMARY.md` when done.
</output>
