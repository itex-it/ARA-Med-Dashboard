---
phase: 05-configuration
plan: 06
type: execute
wave: 3
depends_on:
  - 05-02
  - 05-03
  - 05-04
  - 05-05
files_modified:
  - src/app/actions/medications.ts
  - src/components/config/MedikamenteTab.tsx
  - src/components/config/KonfigurationTabs.tsx
  - src/app/(dashboard)/konfiguration/page.tsx
autonomous: false
requirements:
  - MED-01
  - HOURS-01
  - HOURS-02
  - HOURS-03
  - APPT-01
  - APPT-02
  - APPT-03
  - APPT-04
  - APPT-05
  - TEXT-01
  - TEXT-02
  - TEXT-03
  - TEXT-04
  - DEPUTY-01
  - DEPUTY-02
  - DEPUTY-03
  - DEPUTY-04

must_haves:
  truths:
    - "The /konfiguration page is accessible from the dashboard and renders all 5 module tabs (Öffnungszeiten, Terminarten, Begrüßungstexte, Vertretung, Medikamente)"
    - "Tab state is reflected in the URL search param ?tab= and survives page refresh"
    - "The Medikamente tab allows adding, editing (active toggle), and deleting medications with PZN validation (7 digits)"
    - "All medication data is fetched server-side and passed as props — no client-side DB fetch"
    - "A user can navigate to /konfiguration and all 5 configuration tabs render without a broken page or TypeScript error"
  artifacts:
    - path: "src/app/actions/medications.ts"
      provides: "addMedicationAction, toggleMedicationActiveAction, deleteMedicationAction"
      exports:
        - "addMedicationAction"
        - "toggleMedicationActiveAction"
        - "deleteMedicationAction"
    - path: "src/components/config/MedikamenteTab.tsx"
      provides: "Client Component with medication list table, inline add form, active toggle, delete dialog"
      min_lines: 150
    - path: "src/components/config/KonfigurationTabs.tsx"
      provides: "Client Component wrapper — shadcn Tabs with 5 TabsTriggers and 5 TabsContents, URL sync via router.replace"
      min_lines: 60
    - path: "src/app/(dashboard)/konfiguration/page.tsx"
      provides: "Server Component page: fetches all config data, passes as props to KonfigurationTabs client component"
      min_lines: 80
  key_links:
    - from: "src/app/(dashboard)/konfiguration/page.tsx"
      to: "opening_hours + special_days + deputy_periods + appointment_types + appointment_type_synonyms + greeting_texts + faq_entries + deputy_doctors + medications"
      via: "createServerClient() Supabase queries"
      pattern: "supabase\\.from\\('(opening_hours|appointment_types|greeting_texts|deputy_doctors|medications)"
    - from: "src/app/(dashboard)/konfiguration/page.tsx"
      to: "src/components/config/KonfigurationTabs.tsx"
      via: "JSX props"
      pattern: "openingHours|appointmentTypes|greetingTexts|deputyDoctors|medications"
    - from: "src/components/config/KonfigurationTabs.tsx"
      to: "OeffnungszeitenTab + TerminartenTab + BegruesungsTexteTab + VertretungTab + MedikamenteTab"
      via: "TabsContent renders each tab component with correct prop subset"
      pattern: "OeffnungszeitenTab|TerminartenTab|BegruesungsTexteTab|VertretungTab|MedikamenteTab"
    - from: "src/components/config/MedikamenteTab.tsx"
      to: "src/app/actions/medications.ts"
      via: "useActionState + useTransition"
      pattern: "addMedicationAction|toggleMedicationActiveAction|deleteMedicationAction"
---

<objective>
Complete Phase 5 by delivering the Medications module (MED-01) and wiring all 5 configuration tabs into the /konfiguration Server Component page. This plan is the integration gate — it connects the tab components from plans 02-05, adds the Medikamente tab, and verifies the full phase builds cleanly.

Purpose: The /konfiguration page is the user-facing entry point for all Phase 5 features. Without this plan, the tab components exist but are unreachable.
Output: medications.ts (3 Server Actions), MedikamenteTab.tsx (Client Component), KonfigurationTabs.tsx (tab container Client Component), konfiguration/page.tsx (Server Component that wires all 5 tabs), build gate checkpoint.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-configuration/05-RESEARCH.md
@.planning/phases/05-configuration/05-PATTERNS.md
@.planning/phases/05-configuration/05-UI-SPEC.md
@.planning/phases/05-configuration/05-01-SUMMARY.md
@.planning/phases/05-configuration/05-02-SUMMARY.md
@.planning/phases/05-configuration/05-03-SUMMARY.md
@.planning/phases/05-configuration/05-04-SUMMARY.md
@.planning/phases/05-configuration/05-05-SUMMARY.md
</context>

<interfaces>
From src/app/(dashboard)/inbox/page.tsx — Server Component page pattern with searchParams + auth + service-role fetch + prop passing:
- export default async function Page({ searchParams }: { searchParams: Promise&lt;{ tab?: string }&gt; })
- const params = await searchParams; const activeTab = params.tab ?? 'oeffnungszeiten'
- const supabase = await createServerClient()
- const { data: { user } } = await supabase.auth.getUser()
- const tenantId = user?.app_metadata?.tenant_id ?? ''
- Fetch config tables: supabase.from('opening_hours').select('*').eq('tenant_id', tenantId).order('weekday')
- Error boundary: if (!user) redirect('/auth/login')

From src/components/status/StatusBar.tsx — useOptimistic + useTransition for medication active toggle

From src/lib/types/index.ts (Phase 05 section added by Plan 01):
- MedicationRow: { id, tenant_id, pzn, name, phonetic, active, note, created_at, updated_at }

From src/app/actions/update-inbox-status.ts — canonical auth sequence

Tab components created by plans 02-05 (import paths):
- OeffnungszeitenTab from @/components/config/OeffnungszeitenTab
- TerminartenTab from @/components/config/TerminartenTab
- BegruesungsTexteTab from @/components/config/BegruesungsTexteTab
- VertretungTab from @/components/config/VertretungTab
- MedikamenteTab from @/components/config/MedikamenteTab (created in this plan)

From .planning/phases/05-configuration/05-UI-SPEC.md:
- Tab values: 'oeffnungszeiten', 'terminarten', 'begruessung', 'vertretung', 'medikamente'
- Page title: "Konfiguration" (text-xl font-semibold)
- Tab navigation: Tabs defaultValue from URL param ?tab=
- Module 5: Medication table columns (PZN font-mono, Name, Aussprache italic, Aktiv Switch, Notiz truncated, Aktionen)
- Add form inline below table, not Sheet
- Delete dialog: "Medikament entfernen" with interpolated name and PZN

RESEARCH.md pitfall 7: Layout.tsx must have Konfiguration nav link (added in Plan 01).
RESEARCH.md pitfall 5: appointment_types fetched server-side — page fetches, not TerminartenTab.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Medications Server Actions (MED-01)</name>
  <files>src/app/actions/medications.ts</files>
  <read_first>
    - src/app/actions/update-inbox-status.ts (full file — canonical auth + role gate + service-role)
    - src/app/actions/save-inbox-note.ts (direct action call pattern without prevState formData)
    - .planning/phases/05-configuration/05-RESEARCH.md section medications table schema + Pitfall 6 (synonym race condition note — not applicable here, but same pattern)
    - .planning/phases/05-configuration/05-PATTERNS.md section medications.ts (lines 430-461)
    - src/lib/types/index.ts (MedicationRow)
  </read_first>
  <behavior>
    - addMedicationAction({ pzn, name, phonetic?, note?, active }): Zod validates pzn with /^\d{7}$/ (7-digit PZN), name min(1); inserts row; returns { success, error }
    - toggleMedicationActiveAction({ id, active }): Zod validates id uuid, active boolean; updates is_active (active column) with both eq filters; returns state
    - deleteMedicationAction({ id }): Zod validates id uuid; deletes with both id and tenant_id filters; returns state
  </behavior>
  <action>
    Create 'src/app/actions/medications.ts' with 'use server' directive.

    const ALLOWED_ROLES = ['operator', 'ordination_admin']

    Action state interfaces:
    - export interface MedicationActionState { success?: boolean; error?: string }

    Zod schemas:
    - addMedicationSchema: z.object({ pzn: z.string().regex(/^\d{7}$/, 'Bitte geben Sie eine gültige 7-stellige PZN ein.'), name: z.string().min(1, 'Dieses Feld ist erforderlich.'), phonetic: z.string().optional(), note: z.string().optional(), active: z.boolean().default(true) })
    - toggleActiveSchema: z.object({ id: z.string().uuid(), active: z.boolean() })
    - deleteMedicationSchema: z.object({ id: z.string().uuid() })

    addMedicationAction(prevState: MedicationActionState, formData: FormData): Extract pzn, name, phonetic, note, active from formData. Note: active from formData is a checkbox — 'on' maps to true, absent maps to true (default). Zod validate. serviceClient.from('medications').insert({ tenant_id: tenantId, pzn: parsed.data.pzn, name: parsed.data.name, phonetic: parsed.data.phonetic ?? null, note: parsed.data.note ?? null, active: parsed.data.active }). Return state. This uses the useActionState prevState + formData signature so the add form can bind it directly.

    toggleMedicationActiveAction(data: { id: string; active: boolean }): Full auth sequence (Zod + auth + role gate). serviceClient.from('medications').update({ active: parsed.data.active, updated_at: new Date().toISOString() }).eq('id', parsed.data.id).eq('tenant_id', tenantId). Both eq filters mandatory.

    deleteMedicationAction(data: { id: string }): Full auth sequence. serviceClient.from('medications').delete().eq('id', parsed.data.id).eq('tenant_id', tenantId). Both eq filters mandatory.

    All error paths: console.error('[actionName] DB error:', dbError).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function" src/app/actions/medications.ts outputs 3
    - grep "regex.*\\\\d\{7\}\|\\\\d{7}" src/app/actions/medications.ts — PZN 7-digit validation present
    - grep "\.eq\('tenant_id', tenantId\)" src/app/actions/medications.ts — every delete and update has tenant_id filter
    - grep "createServiceRoleClient()" src/app/actions/medications.ts has no 'await' on same line
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>3 Server Actions: addMedicationAction (prevState+formData, PZN validation), toggleMedicationActiveAction (both eq filters), deleteMedicationAction (both eq filters). TypeScript strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: MedikamenteTab Client Component (MED-01)</name>
  <files>src/components/config/MedikamenteTab.tsx</files>
  <read_first>
    - src/components/inbox/InboxTable.tsx (full file — Table + empty state + loading skeleton pattern)
    - src/components/status/StatusBar.tsx (lines 22-33 in PATTERNS.md — useOptimistic + useTransition for Switch toggle)
    - src/app/(dashboard)/settings/SettingsForm.tsx (useActionState + form pattern for add medication form)
    - .planning/phases/05-configuration/05-UI-SPEC.md Module 5 (medication table columns, add form spec, delete dialog)
    - .planning/phases/05-configuration/05-PATTERNS.md section MedikamenteTab.tsx (lines 719-768)
    - src/app/actions/medications.ts (just written — import these 3 functions)
    - src/lib/types/index.ts (MedicationRow)
  </read_first>
  <action>
    Create src/components/config/MedikamenteTab.tsx as Client Component ('use client').

    Props: MedikamenteTabProps { tenantId: string; hasEditRight: boolean; initialMedications: MedicationRow[] }

    Local state: medications (MedicationRow[] from initialMedications), showAddForm (boolean), editId (string|null — for future inline edit; for MVP set null always after add), deleteTarget (MedicationRow|null), [isPending, startTransition] = useTransition()

    useActionState for add form: const [addState, addFormAction, isAddPending] = useActionState(addMedicationAction, {})

    Main table (shadcn Table):
    Columns (per UI-SPEC Module 5): PZN (120px), Medikament (flexible), Aussprache (flexible, italic muted), Aktiv (80px Switch), Notiz (flexible truncated), Aktionen (80px — Pencil + Trash2)

    For each medication row:
    - PZN: font-mono text-sm
    - Medikament: text-sm
    - Aussprache: text-sm text-muted-foreground italic — show phonetic or "—"
    - Aktiv: useOptimistic + useTransition per row for instant toggle; call toggleMedicationActiveAction({ id: med.id, active: newValue }) in startTransition; update local medications array on success
    - Notiz: text-sm text-muted-foreground, line-clamp-1 (truncated to 1 line); Tooltip showing full note on hover if note exists
    - Aktionen: Pencil icon (variant="ghost" size="icon") — for MVP, clicking opens add form pre-populated (or just set showAddForm true); Trash2 (variant="ghost" size="icon") → set deleteTarget

    Row state: inactive rows have className "opacity-60"

    Empty state (full colSpan=6 row): "Noch keine Medikamente in der Liste. Fügen Sie das erste Medikament hinzu."

    Add button: Button variant="outline" size="sm" icon Plus "Medikament hinzufügen" — sets showAddForm=true

    Inline add form (below table when showAddForm=true):
    - div className="mt-4 rounded-lg border p-4 space-y-4 bg-white"
    - Section label: "Neues Medikament" (text-xs font-semibold uppercase tracking-wide text-muted-foreground)
    - Form bound to addFormAction: &lt;form action={addFormAction} className="space-y-3"&gt;
    - PZN: Input name="pzn" pattern="[0-9]{7}" placeholder="1234567" required
    - Name: Input name="name" placeholder="Metformin 500 mg" required
    - Aussprache: Input name="phonetic" placeholder="Met-for-min" + help text "Geben Sie die Aussprache für ARA-MED Voice ein, z. B. 'Ra-mi-pril' für Ramipril." (text-xs text-muted-foreground)
    - Aktiv: Switch name hidden + client state active (boolean default true) — since Switch cannot be a real form input, use hidden input with value of active state
    - Notiz: Textarea name="note" min-h-16 placeholder "Nur für Typ-2-Diabetes-Patienten"
    - Submit: Button type="submit" size="sm" disabled={isAddPending} with Loader2 during isAddPending — label "Medikament speichern"
    - Cancel: Button type="button" variant="outline" size="sm" "Abbrechen" onClick={() => setShowAddForm(false)}
    - Error: {addState.error && &lt;p role="alert" className="text-xs text-destructive mt-1"&gt;{addState.error}&lt;/p&gt;}
    - On success (addState.success): hide form via useEffect watching addState.success; reload medications by adding returned data — for MVP, trigger a router.refresh() or just show success message and let page re-render

    Delete dialog: Dialog with title "Medikament entfernen", body "Möchten Sie [deleteTarget?.name] ([deleteTarget?.pzn]) aus der Medikamentenliste entfernen?", confirm Button variant="destructive" "Entfernen", cancel "Abbrechen". On confirm: call deleteMedicationAction({ id: deleteTarget.id }) in startTransition; remove from local medications; close dialog.

    Import list: Switch, Table, TableBody, TableCell, TableHead, TableHeader, TableRow, Button, Input, Textarea, Tooltip, TooltipContent, TooltipProvider, TooltipTrigger, Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, Badge from @/components/ui/*; Loader2, Plus, Pencil, Trash2 from lucide-react; useActionState, useOptimistic, useTransition, useState, useEffect from react; addMedicationAction, toggleMedicationActiveAction, deleteMedicationAction from @/app/actions/medications; MedicationRow from @/lib/types; useRouter from next/navigation
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "'use client'" src/components/config/MedikamenteTab.tsx outputs 1
    - grep "addMedicationAction\|toggleMedicationActiveAction\|deleteMedicationAction" src/components/config/MedikamenteTab.tsx — all 3 imported and used
    - grep "font-mono" src/components/config/MedikamenteTab.tsx — PZN displayed with monospace font
    - grep "Noch keine Medikamente\|Medikament entfernen" src/components/config/MedikamenteTab.tsx — empty state and dialog copy match UI-SPEC
    - grep "role=\"alert\"" src/components/config/MedikamenteTab.tsx — error paragraph present
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>MedikamenteTab: table with PZN monospace, phonetic italic, active toggle with useOptimistic, truncated note with Tooltip, inline add form, delete dialog. TypeScript strict clean.</done>
</task>

<task type="auto">
  <name>Task 3: KonfigurationTabs + /konfiguration page (integration wire-up)</name>
  <files>
    src/components/config/KonfigurationTabs.tsx,
    src/app/(dashboard)/konfiguration/page.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/inbox/page.tsx (full file — Server Component searchParams + auth + prop-passing pattern)
    - src/app/(dashboard)/layout.tsx (confirm Konfiguration nav link added by Plan 01)
    - components created in plans 02-05: check their props interfaces by reading the first 30 lines of each (OeffnungszeitenTab, TerminartenTab, BegruesungsTexteTab, VertretungTab)
    - src/components/config/MedikamenteTab.tsx (just written — check props interface)
    - .planning/phases/05-configuration/05-UI-SPEC.md tab values and labels section
  </read_first>
  <action>
    PART A — Create src/components/config/KonfigurationTabs.tsx as Client Component ('use client').

    This is the tab container. It receives all config data as props from the Server Component page and renders the top-level shadcn Tabs with 5 TabsTriggers and 5 TabsContents.

    Props interface: KonfigurationTabsProps — receives initialTab (string), tenantId (string), hasEditRight (boolean), and all data arrays: openingHours (OpeningHoursRow[]), specialDays (SpecialDayRow[]), deputyPeriods (DeputyPeriodRow[]), appointmentTypes (AppointmentTypeRow[]), synonyms (AppointmentTypeSynonymRow[]), greetingTexts (GreetingTextRow[]), faqEntries (FaqEntryRow[]), deputyDoctors (DeputyDoctorRow[]), medications (MedicationRow[]).

    Local state: activeTab (string, initialized from initialTab). URL sync: on tab change, call router.replace('/konfiguration?tab='+value) so the URL reflects the active tab. Use useRouter from next/navigation.

    Tab values and labels per UI-SPEC: 'oeffnungszeiten'→'Öffnungszeiten', 'terminarten'→'Terminarten', 'begruessung'→'Begrüßungstexte', 'vertretung'→'Vertretung', 'medikamente'→'Medikamente'

    Each TabsContent renders the appropriate tab component with the correct subset of props:
    - oeffnungszeiten: OeffnungszeitenTab with openingHours, specialDays, deputyPeriods, tenantId, hasEditRight
    - terminarten: TerminartenTab with appointmentTypes, synonyms (as initialSynonyms), tenantId, hasEditRight
    - begruessung: BegruesungsTexteTab with greetingTexts, faqEntries, tenantId, hasEditRight
    - vertretung: VertretungTab with deputyDoctors, tenantId, hasEditRight
    - medikamente: MedikamenteTab with medications (as initialMedications), tenantId, hasEditRight

    Import list: Tabs, TabsContent, TabsList, TabsTrigger from @/components/ui/tabs; all 5 tab components from @/components/config/*; all relevant Row types from @/lib/types; useRouter from next/navigation; useState from react.

    PART B — Create src/app/(dashboard)/konfiguration/page.tsx as Server Component.

    This is the main entry point for all Phase 5 features. It:
    1. Reads ?tab search param (default: 'oeffnungszeiten')
    2. Authenticates user (redirect to /auth/login if not authenticated)
    3. Extracts tenantId from JWT app_metadata
    4. Determines hasEditRight (araRole in ['operator','ordination_admin'])
    5. Fetches all 9 config tables server-side using createServerClient() (anon key with RLS)
    6. Renders KonfigurationTabs passing all data as props

    Page function signature: export default async function KonfigurationPage({ searchParams }: { searchParams: Promise&lt;{ tab?: string }&gt; })

    Auth: await createServerClient() → getUser() → if no user: redirect('/auth/login')
    tenantId: user.app_metadata?.tenant_id ?? ''
    araRole: user.app_metadata?.ara_role as string | undefined ?? ''
    hasEditRight: ['operator', 'ordination_admin'].includes(araRole)

    Data fetches (all use createServerClient() — RLS handles tenant isolation; no createServiceRoleClient() for reads):
    - openingHours: from 'opening_hours' .select('*') .eq('tenant_id', tenantId) .order('weekday')
    - specialDays: from 'special_days' .select('*') .eq('tenant_id', tenantId) .order('date')
    - deputyPeriods: from 'deputy_periods' .select('*') .eq('tenant_id', tenantId) .order('start_date')
    - appointmentTypes: from 'appointment_types' .select('*') .eq('tenant_id', tenantId) .order('display_name')
    - synonyms: from 'appointment_type_synonyms' .select('*') .eq('tenant_id', tenantId)
    - greetingTexts: from 'greeting_texts' .select('*') .eq('tenant_id', tenantId)
    - faqEntries: from 'faq_entries' .select('*') .eq('tenant_id', tenantId) .order('sort_order')
    - deputyDoctors: from 'deputy_doctors' .select('*') .eq('tenant_id', tenantId) .order('created_at', { ascending: false })
    - medications: from 'medications' .select('*') .eq('tenant_id', tenantId) .order('name')

    Page title: &lt;h1 className="text-xl font-semibold mb-6"&gt;Konfiguration&lt;/h1&gt;

    Return: &lt;KonfigurationTabs initialTab={activeTab} tenantId={tenantId} hasEditRight={hasEditRight} openingHours={openingHours ?? []} specialDays={specialDays ?? []} deputyPeriods={deputyPeriods ?? []} appointmentTypes={appointmentTypes ?? []} synonyms={synonyms ?? []} greetingTexts={greetingTexts ?? []} faqEntries={faqEntries ?? []} deputyDoctors={deputyDoctors ?? []} medications={medications ?? []} /&gt;
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ls src/app/(dashboard)/konfiguration/page.tsx — file exists
    - grep "createServerClient\|getUser\|tenantId" src/app/(dashboard)/konfiguration/page.tsx — auth + tenant pattern
    - grep "opening_hours.*appointment_types.*greeting_texts\|medications" src/app/(dashboard)/konfiguration/page.tsx — all tables fetched (can grep for subset)
    - grep "KonfigurationTabs" src/app/(dashboard)/konfiguration/page.tsx — wrapper component rendered
    - ls src/components/config/KonfigurationTabs.tsx — file exists
    - grep "'use client'" src/components/config/KonfigurationTabs.tsx outputs 1
    - grep "oeffnungszeiten.*terminarten\|begruessung.*vertretung\|medikamente" src/components/config/KonfigurationTabs.tsx — all 5 tab values present
    - grep "router.replace\|useRouter" src/components/config/KonfigurationTabs.tsx — URL sync on tab change
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>KonfigurationTabs.tsx (Client Component routing all 5 tabs with URL sync) and konfiguration/page.tsx (Server Component fetching all 9 tables) created. TypeScript strict clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 5 build gate + navigation smoke test</name>
  <what-built>All Phase 5 plans complete: 9 DB tables, 36 RLS policies, 7 seeded appointment types, 5 tab components, 5 sets of Server Actions, /konfiguration page wiring all tabs together.</what-built>
  <how-to-verify>
    1. Run TypeScript check: npm run typecheck — must exit 0 with no errors
    2. Run lint: npm run lint — must exit 0 (or show only pre-existing warnings, no new errors)
    3. Start development server: npm run dev
    4. Log in to the dashboard
    5. Confirm "Konfiguration" appears in the sidebar navigation
    6. Click "Konfiguration" — the /konfiguration page loads
    7. Verify 5 tabs are visible: Öffnungszeiten, Terminarten, Begrüßungstexte, Vertretung, Medikamente
    8. Click each tab — verify the tab activates and the URL updates to ?tab=... (e.g. /konfiguration?tab=terminarten)
    9. Refresh the page with ?tab=medikamente in the URL — confirm the Medikamente tab is active on reload
    10. On Begrüßungstexte tab: verify the EU AI Act lock block appears showing "Sie sprechen mit einem KI-gestützten Telefonsystem." with a lock icon and cannot be edited
    11. On Öffnungszeiten tab: verify the 7-day weekday grid renders (Mo through So)
    12. On Terminarten tab: verify the 7 seeded appointment types are visible (ALLG, BLUT, KIND, HAUS, REZEPT, UEBERW, IMPF as display names: Allgemeinuntersuchung, Blutabnahme, etc.)

    Expected: All tabs load without JavaScript console errors. EU AI Act block is visually locked. No broken layout on any tab.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any issues found (tab not loading, TypeScript errors, EU AI Act block missing, broken tab).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MedikamenteTab (browser) → Server Actions | Medication form inputs including PZN are untrusted |
| Browser → /konfiguration page | Server Component — auth enforced before any data fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-M-01 | Tampering | PZN format injection | mitigate | Zod z.string().regex(/^\d{7}$/) in addMedicationAction rejects non-7-digit values |
| T-05-M-02 | Tampering | Cross-tenant medication delete/toggle via forged UUID | mitigate | toggleMedicationActiveAction and deleteMedicationAction: .eq('id', id).eq('tenant_id', tenantId) — both filters mandatory |
| T-05-M-03 | Elevation | /konfiguration page accessible without auth | mitigate | Page Server Component: if (!user) redirect('/auth/login') — no config data fetched without authentication |
| T-05-M-04 | Information Disclosure | Config data fetched client-side (MEDSTAR types, medications) | mitigate | konfiguration/page.tsx fetches all data server-side with RLS-enforced createServerClient(); no client fetch in any tab component |
| T-05-M-05 | Spoofing | tenant_id from URL param or form body in page | mitigate | tenantId always from user.app_metadata.tenant_id (JWT) — URL param ?tab= only controls UI state, not data scoping |
</threat_model>

<verification>
Phase 5 complete verification:
- npm run typecheck exits 0
- npm run lint exits 0 (no new errors)
- /konfiguration page loads with 5 tabs
- ?tab= URL param controls active tab and survives refresh
- Begrüßungstexte tab shows EU AI Act lock block (role=region, aria-label, disclosure text verbatim)
- Öffnungszeiten tab shows 7-day weekday grid
- Terminarten tab shows 7 seeded appointment type rows
- Medikamente tab shows empty state text + add form
- Vertretung tab shows deputy list (empty if none) + add button
</verification>

<success_criteria>
- 3 Medication Server Actions: addMedicationAction (prevState+formData, /^\d{7}$/ PZN validation), toggleMedicationActiveAction (both eq filters), deleteMedicationAction (both eq filters)
- MedikamenteTab: table with PZN monospace, phonetic italic, active toggle with useOptimistic, truncated note with Tooltip, inline add form, delete dialog
- KonfigurationTabs.tsx: 5 tabs with URL sync, routes each tab to correct component with correct prop subset
- konfiguration/page.tsx: Server Component, fetches all 9 config tables, passes to KonfigurationTabs
- npm run typecheck passes with no errors
- Human smoke test: all 5 tabs visible, EU AI Act block locked, URL sync works, 7 seeded appointment types visible in Terminarten tab
</success_criteria>

<output>
Create .planning/phases/05-configuration/05-06-SUMMARY.md when done.
</output>
