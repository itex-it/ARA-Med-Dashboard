---
phase: 05-configuration
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/actions/appointment-types.ts
  - src/components/config/TerminartenTab.tsx
autonomous: true
requirements:
  - APPT-01
  - APPT-02
  - APPT-03
  - APPT-04
  - APPT-05

must_haves:
  truths:
    - "Appointment types are displayed from the appointment_types table (fetched server-side, passed as props — never client-side fetch)"
    - "A user can toggle is_visible, is_voice_bookable, is_internal_only, and pid_zero_allowed per appointment type with optimistic UI"
    - "is_voice_bookable and is_internal_only are mutually exclusive — the Server Action enforces this via Zod .refine() on the full flag object, not via single-flag schema"
    - "A user can edit synonyms for each appointment type (inline textarea, comma-separated, delete-then-insert replace pattern)"
    - "A user can set a default appointment type for unclear complaints (APPT-04)"
    - "The PID=0 rule is configurable per appointment type with a tooltip explaining the Führerschein rule (APPT-05)"
  artifacts:
    - path: "src/app/actions/appointment-types.ts"
      provides: "updateAppointmentTypeFlagsAction, saveAppointmentSynonymsAction, setDefaultAppointmentTypeAction"
      exports:
        - "updateAppointmentTypeFlagsAction"
        - "saveAppointmentSynonymsAction"
        - "setDefaultAppointmentTypeAction"
    - path: "src/components/config/TerminartenTab.tsx"
      provides: "Client Component with appointment types table, flag toggles, synonym editor, default selector"
      min_lines: 150
  key_links:
    - from: "src/components/config/TerminartenTab.tsx"
      to: "src/app/actions/appointment-types.ts"
      via: "useOptimistic + useTransition + direct action calls"
      pattern: "updateAppointmentTypeFlagsAction|saveAppointmentSynonymsAction|setDefaultAppointmentTypeAction"
    - from: "src/app/actions/appointment-types.ts"
      to: "appointment_types + appointment_type_synonyms"
      via: "serviceClient.from(...) with tenant_id filter"
      pattern: "\\.from\\('(appointment_types|appointment_type_synonyms)'\\)"
    - from: "src/app/actions/appointment-types.ts"
      to: "Zod updateFlagsSchema"
      via: ".refine(d => !(d.is_voice_bookable && d.is_internal_only))"
      pattern: "refine.*is_voice_bookable.*is_internal_only"
---

<objective>
Build the Terminarten configuration module end-to-end: Server Actions for per-row flag updates (all 4 flags in one call with mutual exclusivity enforcement), synonym replace, and default type setting — plus the Client Component tab with optimistic switches, inline synonym editor, and default type selector.

Purpose: APPT-01..05 give the practice full control over which appointment types ARA-Med Voice can book, what natural language triggers them, and which type is the fallback for unclear complaints.
Output: appointment-types.ts (3 Server Actions), TerminartenTab.tsx (Client Component).
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-configuration/05-RESEARCH.md
@.planning/phases/05-configuration/05-PATTERNS.md
@.planning/phases/05-configuration/05-UI-SPEC.md
</context>

<interfaces>
From src/app/actions/update-inbox-status.ts — canonical auth sequence (copy verbatim):
- ALLOWED_ROLES const, Zod parse first, await createServerClient(), getUser(), tenantId from app_metadata, role gate, createServiceRoleClient() (sync)

From src/components/status/StatusBar.tsx — useOptimistic + useTransition Switch toggle pattern:
- const [optimisticValue, setOptimistic] = useOptimistic(serverValue)
- const [isPending, startTransition] = useTransition()
- startTransition(async () => { setOptimistic(newValue); await serverAction(...) })
- {isPending ? &lt;Loader2 animate-spin&gt; : &lt;Switch checked={optimisticValue} onCheckedChange={handleToggle} disabled={isPending}&gt;}

From src/components/inbox/InboxTable.tsx — Table with sticky header, empty state, loading skeleton pattern (lines 59-95, 96-166)

From src/lib/types/index.ts (Phase 05 section added by Plan 01):
- AppointmentTypeRow: { id, tenant_id, appointment_type_code, display_name, is_visible, is_voice_bookable, is_internal_only, is_default, pid_zero_allowed, created_at, updated_at }
- AppointmentTypeSynonymRow: { id, tenant_id, appointment_type_code, synonym, language_code, created_at }

RESEARCH.md patterns for this module:
- Pattern 4: Default-one row — two sequential service-role calls (clear all, then set one) — acceptable for single-user admin config
- Pattern 5: Optimistic switch — useOptimistic + useTransition
- APPT-03 synonyms: delete-then-insert replace per (tenant_id, appointment_type_code) — not per synonym ID
- Pitfall 3: Mutual exclusivity Zod refine required — enforced on FULL FLAG OBJECT, not on per-flag single-column schema
- Pitfall 5: MEDSTAR types fetched SERVER-SIDE, passed as props — never useEffect + client fetch
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions for appointment type configuration</name>
  <files>src/app/actions/appointment-types.ts</files>
  <read_first>
    - src/app/actions/update-inbox-status.ts (full file — canonical 5-step auth + role gate + service-role pattern)
    - src/app/actions/save-inbox-note.ts (direct action call pattern without prevState)
    - .planning/phases/05-configuration/05-RESEARCH.md patterns 3 and 4 (synonym replace, default-one)
    - .planning/phases/05-configuration/05-PATTERNS.md section appointment-types.ts (lines 298-347)
    - src/lib/types/index.ts (AppointmentTypeRow fields for all 4 boolean flag columns)
  </read_first>
  <behavior>
    - updateAppointmentTypeFlagsAction({ id, visible, is_voice_bookable, is_internal_only, pid_zero_allowed }): receives ALL 4 flags together; Zod schema has .refine(d => !(d.is_voice_bookable && d.is_internal_only), { message: 'Ein Typ kann nicht gleichzeitig KI-buchbar und intern sein.' }); on validation pass, UPDATE appointment_types SET is_visible=..., is_voice_bookable=..., is_internal_only=..., pid_zero_allowed=..., updated_at=now() WHERE id=... AND tenant_id=...; returns { success, error }
    - saveAppointmentSynonymsAction({ appointmentTypeCode, synonyms }): synonyms is string[] from splitting textarea by comma; delete all existing synonyms for (tenant_id, appointmentTypeCode), then insert new ones; if synonyms empty, delete clears them all
    - setDefaultAppointmentTypeAction({ id }): two sequential service-role calls: (1) UPDATE appointment_types SET is_default=false WHERE tenant_id=tenantId, then (2) UPDATE appointment_types SET is_default=true WHERE id=id AND tenant_id=tenantId
  </behavior>
  <action>
    Create 'src/app/actions/appointment-types.ts' with 'use server' directive.

    const ALLOWED_ROLES = ['operator', 'ordination_admin']

    Action state interfaces:
    - export interface AppointmentTypeFlagsState { success?: boolean; error?: string }
    - export interface AppointmentSynonymsState { success?: boolean; error?: string }
    - export interface DefaultAppointmentTypeState { success?: boolean; error?: string }

    Zod schemas:
    - updateFlagsSchema: z.object({ id: z.string().uuid(), visible: z.boolean(), is_voice_bookable: z.boolean(), is_internal_only: z.boolean(), pid_zero_allowed: z.boolean() }).refine(d => !(d.is_voice_bookable && d.is_internal_only), { message: 'Ein Typ kann nicht gleichzeitig KI-buchbar und intern sein.', path: ['is_internal_only'] })
    - saveSynonymsSchema: z.object({ appointmentTypeCode: z.string().min(1), synonyms: z.array(z.string().min(1)).max(50) })
    - setDefaultSchema: z.object({ id: z.string().uuid() })

    updateAppointmentTypeFlagsAction(data: { id: string; visible: boolean; is_voice_bookable: boolean; is_internal_only: boolean; pid_zero_allowed: boolean }): Full 5-step auth sequence. Zod parse with updateFlagsSchema (includes .refine mutual exclusivity check — if validation fails, return { error: parsed.error.issues[0].message }). On success: serviceClient.from('appointment_types').update({ is_visible: parsed.data.visible, is_voice_bookable: parsed.data.is_voice_bookable, is_internal_only: parsed.data.is_internal_only, pid_zero_allowed: parsed.data.pid_zero_allowed, updated_at: new Date().toISOString() }).eq('id', parsed.data.id).eq('tenant_id', tenantId). Both .eq filters mandatory.

    saveAppointmentSynonymsAction(data: { appointmentTypeCode: string; synonyms: string[] }): Full auth sequence. Step 1: serviceClient.from('appointment_type_synonyms').delete().eq('tenant_id', tenantId).eq('appointment_type_code', data.appointmentTypeCode). If deleteError: console.error + return error. Step 2: if synonyms.length > 0, insert rows [{tenant_id, appointment_type_code, synonym, language_code: 'de'}]. Return { success: true } or error.

    setDefaultAppointmentTypeAction(data: { id: string }): Full auth sequence. Step 1: serviceClient.from('appointment_types').update({ is_default: false }).eq('tenant_id', tenantId). Step 2: serviceClient.from('appointment_types').update({ is_default: true, updated_at: new Date().toISOString() }).eq('id', parsed.data.id).eq('tenant_id', tenantId). Return { success: true } or error. Log both step errors separately.

    Note: the action is named updateAppointmentTypeFlagsAction (plural "Flags") because it receives and updates all 4 flags in one call. The component must pass all 4 current flag values on every toggle, updating the changed flag and preserving the others.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "export async function" src/app/actions/appointment-types.ts outputs 3
    - grep "refine.*is_voice_bookable.*is_internal_only\|refine.*is_internal_only.*is_voice_bookable" src/app/actions/appointment-types.ts — mutual exclusivity .refine() is present
    - grep "\.eq\('tenant_id', tenantId\)" src/app/actions/appointment-types.ts — every UPDATE and DELETE has tenant_id filter
    - grep "appointment_type_synonyms" src/app/actions/appointment-types.ts — delete and insert both reference this table
    - grep "is_default.*false" src/app/actions/appointment-types.ts — setDefaultAppointmentTypeAction clears all defaults first
    - grep "createServiceRoleClient()" src/app/actions/appointment-types.ts has no 'await' keyword on the same line
    - grep "is_voice_bookable.*is_internal_only\|is_internal_only.*is_voice_bookable" src/app/actions/appointment-types.ts — both flags updated in the same UPDATE statement
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>3 Server Actions: flag update (ALL 4 flags, Zod .refine mutual exclusivity, both eq filters), synonym replace (delete-then-insert), default setter (clear-then-set). TypeScript strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: TerminartenTab Client Component (APPT-01..05)</name>
  <files>src/components/config/TerminartenTab.tsx</files>
  <read_first>
    - src/components/inbox/InboxTable.tsx (full file — Table + sticky header + empty state + skeleton pattern)
    - src/components/status/StatusBar.tsx (lines 22-33, 130-141 — useOptimistic + useTransition + Switch + Loader2 pattern)
    - .planning/phases/05-configuration/05-UI-SPEC.md Module 2 section (column widths, synonym editor spec, visual state rules, APPT-04 default select spec, APPT-05 tooltip spec)
    - .planning/phases/05-configuration/05-PATTERNS.md section TerminartenTab.tsx (lines 523-610)
    - src/app/actions/appointment-types.ts (just written — import updateAppointmentTypeFlagsAction, saveAppointmentSynonymsAction, setDefaultAppointmentTypeAction)
    - src/lib/types/index.ts (AppointmentTypeRow, AppointmentTypeSynonymRow)
  </read_first>
  <action>
    Create src/components/config/TerminartenTab.tsx as a Client Component ('use client').

    Props interface: TerminartenTabProps { tenantId: string; hasEditRight: boolean; initialAppointmentTypes: AppointmentTypeRow[]; initialSynonyms: AppointmentTypeSynonymRow[] }

    Local state:
    - appointmentTypes: AppointmentTypeRow[] (initialized from initialAppointmentTypes, updated optimistically)
    - synonymsByCode: Record&lt;string, string[]&gt; — map of appointment_type_code → synonyms array (initialized from initialSynonyms grouped by code)
    - openSynonymEditor: string | null — the appointment_type_code currently being edited (only one at a time)
    - synonymDraftByCode: Record&lt;string, string&gt; — textarea draft text per code
    - defaultTypeId: string — the id of the row with is_default=true (derived from initialAppointmentTypes)
    - [isPending, startTransition] = useTransition()

    Main table (shadcn Table with sticky header):
    Columns (from UI-SPEC Module 2): Terminart (flexible), Sichtbar (80px), KI-buchbar (100px), Nur intern (100px), Synonyme (flexible), Standard (80px), PID=0 (80px)

    For each row, render:
    - Terminart: display_name text-sm
    - Sichtbar Switch: optimistic toggle; when toggled off, KI-buchbar and Nur intern switches are disabled
    - KI-buchbar Switch: disabled if is_visible=false OR is_internal_only=true (mutual exclusivity); aria-label="KI-buchbar: {display_name}"; on change call updateAppointmentTypeFlagsAction with all 4 current flags, overriding is_voice_bookable with the new value
    - Nur intern Switch: disabled if is_visible=false OR is_voice_bookable=true; aria-label="Nur intern: {display_name}"; on change call updateAppointmentTypeFlagsAction with all 4 current flags, overriding is_internal_only with the new value
    - Synonyme cell: show comma-joined synonym list (text-sm text-muted-foreground) + Pencil icon button (variant="ghost" size="icon") to open synonym editor
    - Standard: Badge variant="default" "Standard" if is_default, otherwise empty
    - PID=0 Switch: Tooltip wrapping the switch with text "Für Führerscheinuntersuchungen kann PID=0 genutzt werden. Der Patient muss nicht im MEDSTAR-System erfasst sein."; on change call updateAppointmentTypeFlagsAction with all 4 current flags, overriding pid_zero_allowed with the new value

    Toggle handler pattern (same for all 4 boolean columns): When any flag is toggled, build the full flags object from the current row state and call updateAppointmentTypeFlagsAction({ id: row.id, visible: newIsVisible, is_voice_bookable: newIsVoiceBookable, is_internal_only: newIsInternalOnly, pid_zero_allowed: newPidZeroAllowed }) inside startTransition. On success, update local appointmentTypes array via setAppointmentTypes with map. The Server Action's Zod .refine() will catch any invalid combination (e.g. both KI-buchbar and Nur intern true) and return an error — surface this as a toast or inline error message.

    Visual state rules:
    - Row with is_visible=false: className includes "opacity-60"
    - Row with open synonym editor: className includes "bg-accent/10 border-l-2 border-primary"
    - KI-buchbar and Nur intern mutual exclusivity: disabled prop set based on sibling flag state (UI prevents the combination; Server Action enforces it as a fallback)

    Synonym editor (inline sub-row — not a Sheet):
    - When openSynonymEditor === row.appointment_type_code: render a tr below the row (use colSpan={7}) with a Textarea showing the draft (comma-separated) + "Synonyme speichern" Button
    - On save: call saveAppointmentSynonymsAction({ appointmentTypeCode: code, synonyms: draft.split(',').map(s => s.trim()).filter(Boolean) }) in startTransition; update synonymsByCode on success; close editor
    - Help text below textarea: "Beispiel: Bauchweh, Magenschmerzen, Leibweh"
    - Opening a synonym editor for one row automatically closes any previously open editor

    Default type section (APPT-04) — rendered BELOW the main table:
    - Section header: "Standard für unklare Beschwerden"
    - Help text: "Diese Terminart wird verwendet, wenn der Anrufer sein Anliegen nicht klar benennen kann."
    - Select listing all appointment types by display_name; value is the id of the current default
    - On change: call setDefaultAppointmentTypeAction({ id: selectedId }) in startTransition; update local appointmentTypes to reflect new default

    Empty state: "Keine Terminarten verfügbar. Prüfen Sie die MEDSTAR-Verbindung." (full colSpan row)

    Refresh button (APPT-01 data source): Button variant="ghost" size="sm" with RefreshCw icon, label "Neu laden" — for MVP this is a placeholder button (a future enhancement will call an n8n webhook; for now show a toast/alert that refresh via n8n is configured separately). Do NOT make a client-side MEDSTAR API call.

    Import list:
    - Switch, Table, TableBody, TableCell, TableHead, TableHeader, TableRow, Button, Textarea, Select, SelectContent, SelectItem, SelectTrigger, SelectValue, Badge, Tooltip, TooltipContent, TooltipProvider, TooltipTrigger from @/components/ui/*
    - Loader2, Pencil, RefreshCw from lucide-react
    - useTransition, useState, useCallback from react
    - Actions: updateAppointmentTypeFlagsAction, saveAppointmentSynonymsAction, setDefaultAppointmentTypeAction from @/app/actions/appointment-types
    - Types: AppointmentTypeRow, AppointmentTypeSynonymRow from @/lib/types
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "'use client'" src/components/config/TerminartenTab.tsx outputs 1
    - grep "updateAppointmentTypeFlagsAction\|saveAppointmentSynonymsAction\|setDefaultAppointmentTypeAction" src/components/config/TerminartenTab.tsx — all 3 imported and used
    - grep "is_internal_only.*is_voice_bookable\|is_voice_bookable.*is_internal_only" src/components/config/TerminartenTab.tsx — mutual exclusivity reflected in disabled prop logic
    - grep "aria-label" src/components/config/TerminartenTab.tsx — all Switch elements have aria-label
    - grep "Tooltip\|TooltipContent" src/components/config/TerminartenTab.tsx — PID=0 tooltip is present
    - grep "Keine Terminarten verfügbar" src/components/config/TerminartenTab.tsx — empty state text matches UI-SPEC
    - grep "Synonyme speichern\|Neu laden" src/components/config/TerminartenTab.tsx — CTA labels present
    - grep "useEffect\|fetch.*api\|fetch.*medstar" src/components/config/TerminartenTab.tsx outputs 0 — no client-side MEDSTAR fetch
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>TerminartenTab renders appointment types table with optimistic switches (all 4 flags sent together on each toggle), inline synonym editor, default type selector, PID=0 tooltip. No client-side DB fetch. TypeScript strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TerminartenTab (browser) → Server Actions | Flag values, synonym lists, and default type selection are untrusted inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-A-01 | Tampering | updateAppointmentTypeFlagsAction — arbitrary column update | mitigate | Schema accepts only the 4 boolean flag fields (visible, is_voice_bookable, is_internal_only, pid_zero_allowed) — no arbitrary column name in the payload |
| T-05-A-02 | Tampering | is_voice_bookable + is_internal_only both true | mitigate | Zod .refine(d => !(d.is_voice_bookable && d.is_internal_only)) on the full flag object in updateFlagsSchema — Server Action rejects before any DB write |
| T-05-A-03 | Tampering | Cross-tenant synonym delete (forged appointmentTypeCode) | mitigate | saveAppointmentSynonymsAction delete step includes .eq('tenant_id', tenantId) — prevents deleting another tenant's synonyms |
| T-05-A-04 | Tampering | Cross-tenant flag update via forged UUID | mitigate | updateAppointmentTypeFlagsAction: .eq('id', id).eq('tenant_id', tenantId) — both filters on UPDATE |
| T-05-A-05 | Information Disclosure | MEDSTAR appointment types client-fetch | mitigate | Types fetched server-side only (page Server Component); TerminartenTab receives as props — no browser fetch path |
</threat_model>

<verification>
After this plan executes:
- npx tsc --noEmit exits 0
- grep -c "export async function" src/app/actions/appointment-types.ts = 3
- grep "refine.*is_voice_bookable.*is_internal_only" src/app/actions/appointment-types.ts — mutual exclusivity .refine() present
- ls src/components/config/TerminartenTab.tsx — file exists with >150 lines
- grep "useEffect" src/components/config/TerminartenTab.tsx outputs 0 (no client-side data fetch)
</verification>

<success_criteria>
- 3 Server Actions: updateAppointmentTypeFlagsAction (all 4 flags in one call, Zod .refine mutual exclusivity, both eq filters), saveAppointmentSynonymsAction (delete-then-insert per code), setDefaultAppointmentTypeAction (clear-all then set-one)
- TerminartenTab: table with all 7 columns, optimistic toggles (Loader2 during pending), mutual exclusivity enforced in UI disabled props AND Server Action .refine, inline synonym editor (one at a time), default type Select, PID=0 Tooltip, no client-side MEDSTAR fetch
- German labels: all copy matches UI-SPEC copywriting contract
- TypeScript strict: no errors
</success_criteria>

<output>
Create .planning/phases/05-configuration/05-03-SUMMARY.md when done.
</output>
