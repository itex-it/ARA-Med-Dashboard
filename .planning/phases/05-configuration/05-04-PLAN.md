---
phase: 05-configuration
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/actions/greeting-texts.ts
  - src/components/config/BegruesungsTexteTab.tsx
autonomous: true
requirements:
  - TEXT-01
  - TEXT-02
  - TEXT-03
  - TEXT-04

must_haves:
  truths:
    - "A user can set a greeting text per mode (Normal, Urlaub, Vertretung, Eigener Vertretungsdienst) and save it"
    - "The EU AI Act Art. 50 disclosure 'Sie sprechen mit einem KI-gestützten Telefonsystem.' is shown as a locked block and cannot be removed — enforced in the Server Action, not just the UI"
    - "The Server Action unconditionally overwrites eu_ai_act_disclosure with the locked constant and eu_ai_act_disclosure_present=true on every save — even if a direct API call omits these fields"
    - "FAQ entries can be added (question + answer) and deleted per mode, with confirmation dialog on delete"
    - "The language selector shows only Deutsch (Standard) for MVP — data model stores language_code='de'"
  artifacts:
    - path: "src/app/actions/greeting-texts.ts"
      provides: "saveGreetingTextAction, addFaqEntryAction, deleteFaqEntryAction"
      exports:
        - "saveGreetingTextAction"
        - "addFaqEntryAction"
        - "deleteFaqEntryAction"
    - path: "src/components/config/BegruesungsTexteTab.tsx"
      provides: "Client Component with 4 sub-tabs, EU AI Act lock block, greeting textarea, FAQ section per sub-tab"
      min_lines: 180
  key_links:
    - from: "src/components/config/BegruesungsTexteTab.tsx"
      to: "src/app/actions/greeting-texts.ts"
      via: "useActionState / useTransition direct calls"
      pattern: "saveGreetingTextAction|addFaqEntryAction|deleteFaqEntryAction"
    - from: "src/app/actions/greeting-texts.ts"
      to: "greeting_texts + faq_entries"
      via: "serviceClient.from(table).upsert/insert/delete"
      pattern: "\\.from\\('(greeting_texts|faq_entries)'\\)"
    - from: "src/app/actions/greeting-texts.ts"
      to: "EU AI Act constant"
      via: "unconditional column overwrite"
      pattern: "EU_AI_ACT_DISCLOSURE.*Sie sprechen"
---

<objective>
Build the Begrüßungstexte configuration module end-to-end: Server Actions with EU AI Act Art. 50 enforcement (server-side, not UI-only) and FAQ CRUD — plus the Client Component tab with 4 sub-tabs, locked disclosure block, greeting textarea, and FAQ section per mode.

Purpose: TEXT-01..04 give the practice control over what ARA-Med Voice says in each operational mode, while ensuring legal compliance with EU AI Act Art. 50 that cannot be bypassed.
Output: greeting-texts.ts (3 Server Actions with server-enforced disclosure), BegruesungsTexteTab.tsx (Client Component with 4 sub-tabs).
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-configuration/05-RESEARCH.md
@.planning/phases/05-configuration/05-PATTERNS.md
@.planning/phases/05-configuration/05-UI-SPEC.md
</context>

<interfaces>
From .planning/phases/05-configuration/05-RESEARCH.md — EU AI Act Pattern 3 (critical — must follow exactly):
- const EU_AI_ACT_DISCLOSURE = 'Sie sprechen mit einem KI-gestützten Telefonsystem.'
- Server Action always sets: eu_ai_act_disclosure: EU_AI_ACT_DISCLOSURE (overwrites any client input)
- Server Action always sets: eu_ai_act_disclosure_present: true
- upsert uses onConflict: 'tenant_id,mode,language_code'
- user_text is the ONLY field the user controls; the disclosure is stored separately

From src/app/actions/update-inbox-status.ts — canonical auth sequence (copy verbatim)

From src/components/inbox/InboxTable.tsx — Tabs component usage (lines 170-196 in PATTERNS.md):
- &lt;Tabs defaultValue="normal"&gt;&lt;TabsList&gt;&lt;TabsTrigger value="normal"&gt;Normal&lt;/TabsTrigger&gt;...&lt;/TabsList&gt;&lt;TabsContent value="normal"&gt;...&lt;/TabsContent&gt;&lt;/Tabs&gt;

From src/app/(dashboard)/settings/SettingsForm.tsx — useActionState + textarea + section header pattern

From src/lib/types/index.ts (Phase 05 section added by Plan 01):
- GreetingMode = 'normal' | 'vacation' | 'deputy' | 'own_service'
- GreetingTextRow: { id, tenant_id, mode (GreetingMode), language_code, eu_ai_act_disclosure, eu_ai_act_disclosure_present, user_text, created_at, updated_at }
- FaqEntryRow: { id, tenant_id, mode, question, answer, active, sort_order, created_at, updated_at }

From .planning/phases/05-configuration/05-PATTERNS.md section BegruesungsTexteTab.tsx (lines 614-669):
- EU AI Act lock block HTML/CSS spec
- Sub-tab structure with Tabs from shadcn
- Textarea field pattern

STATE.md locked: eu_ai_act_disclosure enforcement is server-side (DSGVO checklist item)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server Actions for greeting texts and FAQ (EU AI Act enforcement)</name>
  <files>src/app/actions/greeting-texts.ts</files>
  <read_first>
    - src/app/actions/update-inbox-status.ts (full file — canonical auth + role gate + service-role)
    - src/app/(dashboard)/settings/actions.ts (prevState + formData signature, upsert pattern)
    - .planning/phases/05-configuration/05-RESEARCH.md Pattern 3 (EU AI Act enforcement — lines 289-325)
    - .planning/phases/05-configuration/05-PATTERNS.md section greeting-texts.ts (lines 350-388)
    - src/lib/types/index.ts (GreetingMode, GreetingTextRow, FaqEntryRow)
  </read_first>
  <behavior>
    - saveGreetingTextAction: receives { mode, user_text } — note: no eu_ai_act_disclosure in input; Server Action ignores any such field from client; always upserts with EU_AI_ACT_DISCLOSURE constant and eu_ai_act_disclosure_present=true; Zod validates mode ∈ GreetingMode and user_text as non-null string; returns { success, error }
    - addFaqEntryAction: receives { mode, question, answer }; Zod validates mode ∈ ('all'|GreetingMode), question min(1), answer min(1); inserts row with sort_order=0 and active=true; returns state
    - deleteFaqEntryAction: receives { id }; deletes with .eq('id', id).eq('tenant_id', tenantId); returns state
  </behavior>
  <action>
    Create 'src/app/actions/greeting-texts.ts' with 'use server' directive.

    FILE-LEVEL CONSTANT (critical for TEXT-02 compliance):
    const EU_AI_ACT_DISCLOSURE = 'Sie sprechen mit einem KI-gestützten Telefonsystem.'

    This constant is NEVER derived from client input, NEVER read from the DB before saving, NEVER overridable. It is the server-enforced truth.

    const ALLOWED_ROLES = ['operator', 'ordination_admin']
    const GREETING_MODES = ['normal', 'vacation', 'deputy', 'own_service'] as const

    Action state interfaces:
    - export interface GreetingTextActionState { success?: boolean; error?: string }
    - export interface FaqActionState { success?: boolean; error?: string }

    Zod schemas:
    - saveGreetingTextSchema: z.object({ mode: z.enum(['normal','vacation','deputy','own_service']), user_text: z.string() })
      Note: NO eu_ai_act_disclosure in schema — the Server Action never accepts it from client. It is always the constant.
    - addFaqSchema: z.object({ mode: z.enum(['all','normal','vacation','deputy','own_service']), question: z.string().min(1, 'Dieses Feld ist erforderlich.'), answer: z.string().min(1, 'Dieses Feld ist erforderlich.') })
    - deleteFaqSchema: z.object({ id: z.string().uuid() })

    saveGreetingTextAction(prevState: GreetingTextActionState, formData: FormData): Full 5-step auth sequence. Parse mode and user_text from formData. Zod validate. serviceClient.from('greeting_texts').upsert({ tenant_id: tenantId, mode: parsed.data.mode, language_code: 'de', eu_ai_act_disclosure: EU_AI_ACT_DISCLOSURE, eu_ai_act_disclosure_present: true, user_text: parsed.data.user_text, updated_at: new Date().toISOString() }, { onConflict: 'tenant_id,mode,language_code' }). Return { success: true } or error.

    addFaqEntryAction(data: { mode: string; question: string; answer: string }): Full auth sequence. Zod validate. serviceClient.from('faq_entries').insert({ tenant_id: tenantId, mode: parsed.data.mode, question: parsed.data.question, answer: parsed.data.answer, active: true, sort_order: 0 }). Return state.

    deleteFaqEntryAction(data: { id: string }): Full auth sequence. Zod validate. serviceClient.from('faq_entries').delete().eq('id', parsed.data.id).eq('tenant_id', tenantId). Both filters mandatory.

    All error paths: console.error('[actionName] DB error:', dbError).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "EU_AI_ACT_DISCLOSURE" src/app/actions/greeting-texts.ts — constant defined at file level and used in upsert payload
    - grep "eu_ai_act_disclosure_present.*true" src/app/actions/greeting-texts.ts — always set to true in upsert
    - grep "eu_ai_act_disclosure.*EU_AI_ACT_DISCLOSURE" src/app/actions/greeting-texts.ts — the column is set from the constant, not from any client input
    - grep "eu_ai_act_disclosure.*formData\|eu_ai_act_disclosure.*parsed\.data" src/app/actions/greeting-texts.ts outputs 0 — disclosure never comes from client
    - grep -c "export async function" src/app/actions/greeting-texts.ts outputs 3
    - grep "\.eq\('tenant_id', tenantId\)" src/app/actions/greeting-texts.ts — deleteFaqEntryAction has tenant_id filter
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>3 Server Actions written. EU AI Act disclosure is unconditionally server-enforced — constant never derived from client. FAQ CRUD with defense-in-depth deletes. TypeScript strict clean.</done>
</task>

<task type="auto">
  <name>Task 2: BegruesungsTexteTab Client Component (TEXT-01..04)</name>
  <files>src/components/config/BegruesungsTexteTab.tsx</files>
  <read_first>
    - src/app/(dashboard)/settings/SettingsForm.tsx (full file — useActionState, textarea, section header, pending state, success/error feedback pattern)
    - src/components/inbox/InboxTable.tsx (lines 170-196 in PATTERNS.md — Tabs/TabsList/TabsTrigger/TabsContent pattern for sub-tabs)
    - .planning/phases/05-configuration/05-UI-SPEC.md Module 3 section (EU AI Act lock block visual spec, textarea spec, FAQ section spec, copywriting)
    - .planning/phases/05-configuration/05-PATTERNS.md section BegruesungsTexteTab.tsx (lines 614-669)
    - src/app/actions/greeting-texts.ts (just written — import these 3 functions)
    - src/lib/types/index.ts (GreetingMode, GreetingTextRow, FaqEntryRow)
  </read_first>
  <action>
    Create src/components/config/BegruesungsTexteTab.tsx as a Client Component ('use client').

    Props interface: BegruesungsTexteTabProps { tenantId: string; hasEditRight: boolean; initialGreetingTexts: GreetingTextRow[]; initialFaqEntries: FaqEntryRow[] }

    Helper: derive greetingTextsByMode from initialGreetingTexts as Record&lt;GreetingMode, GreetingTextRow | undefined&gt;.
    Helper: derive faqByMode from initialFaqEntries where filter includes both mode='all' and mode=activeSubTab combined.

    The component renders a shadcn Tabs component with 4 sub-tabs:
    Tab values: 'normal', 'vacation', 'deputy', 'own_service'
    Tab labels: 'Normal', 'Urlaub', 'Vertretung', 'Eigener Vertretungsdienst'

    Sub-tab state: manage with useState (not URL param — inner sub-tab is client state per RESEARCH.md discretion).

    Each sub-tab (TabsContent) renders identically:
    A) EU AI Act Disclosure Lock Block (TEXT-02):
      - Container: div className="bg-muted border border-muted-foreground/30 rounded-md p-4 mb-4"
      - role="region" aria-label="EU-KI-Gesetz Art. 50 Pflichthinweis"
      - Header: p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground flex items-center gap-2" with Lock icon (14px / h-3.5 w-3.5) + "EU-KI-Gesetz Art. 50 — Pflichthinweis"
      - Disclosure text: p className="text-sm text-muted-foreground italic mt-2" containing: "Sie sprechen mit einem KI-gestützten Telefonsystem."
      - Footer note: p className="text-xs text-muted-foreground mt-2" — "Dieser Hinweis ist gesetzlich vorgeschrieben und kann nicht entfernt werden."
      - NO focusable children (no buttons, no inputs) — read-only region
      - The disclosure text is rendered from a CLIENT-SIDE CONSTANT (not from greetingTextsByMode), making it visually clear the value is fixed

    B) Greeting text form (TEXT-01 + TEXT-04):
      - useActionState(saveGreetingTextAction, {}) — per sub-tab or shared state with mode hidden field
      - Form: hidden input name="mode" value={activeSubTab}
      - Language selector (TEXT-04): Select with single option "Deutsch (Standard)", value='de', disabled — visual only for MVP
      - Section label: "Begrüßungstext" (text-xs font-semibold uppercase tracking-wide text-muted-foreground)
      - Help text: "Dieser Text wird von ARA-MED Voice nach dem Pflichthinweis gesprochen."
      - Textarea: name="user_text", defaultValue from greetingTextsByMode[activeSubTab]?.user_text ?? '', min-h-32, full width, placeholder "Guten Tag, Sie haben die Ordination [Name] erreicht. Wie kann ich Ihnen helfen?", disabled if !hasEditRight
      - Character count: p className="text-xs text-muted-foreground text-right mt-1" showing length of textarea content — managed via local state tracking character count as user types
      - Save button: variant="default" size="sm" "Text speichern" with Loader2 during isPending, disabled during isPending or !hasEditRight
      - Error: p role="alert" className="text-xs text-destructive mt-1" when state.error
      - Success: p className="text-xs text-green-600 mt-1" "Text gespeichert." — shown for 3 seconds then hidden (use setTimeout + useState)

    C) FAQ section (TEXT-03) — below the greeting form:
      - Section label: "Häufige Fragen & Standardantworten"
      - Local state per sub-tab: faqEntries (FaqEntryRow[] filtered for current mode), showAddForm (boolean), deleteTarget (FaqEntryRow|null), [isPendingFaq, startFaqTransition] = useTransition()
      - FAQ table (simple Table): columns Frage, Antwort, Aktionen
      - Empty state: "Noch keine FAQ-Antworten für diesen Modus konfiguriert." (full colSpan row)
      - Add button: Button variant="outline" size="sm" icon Plus "Frage hinzufügen"
      - Inline add form: Input for question, Textarea min-h-16 for answer, Button "Hinzufügen" — on submit call addFaqEntryAction({ mode: activeSubTab, question, answer }) in startFaqTransition, update local faqEntries on success, hide form
      - Delete: Trash2 icon → sets deleteTarget; Dialog: title "Frage entfernen", body "Möchten Sie diese FAQ-Antwort löschen?", confirm Button variant="destructive" "Löschen", cancel "Abbrechen"
      - On delete confirm: call deleteFaqEntryAction({ id: deleteTarget.id }) in startFaqTransition, remove from local faqEntries

    Implementation note for FAQ state: since all 4 sub-tabs render simultaneously but only one is visible, manage FAQ state as a map keyed by mode. Initialize from initialFaqEntries: { normal: [...], vacation: [...], ... } where mode='all' entries are merged into all modes.

    Import list:
    - Tabs, TabsList, TabsTrigger, TabsContent, Table, TableBody, TableCell, TableHead, TableHeader, TableRow, Button, Input, Textarea, Select, SelectContent, SelectItem, SelectTrigger, SelectValue, Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter from @/components/ui/*
    - Lock, Plus, Trash2, Loader2 from lucide-react
    - useActionState, useTransition, useState, useEffect, useRef from react
    - Actions: saveGreetingTextAction, addFaqEntryAction, deleteFaqEntryAction from @/app/actions/greeting-texts
    - Types: GreetingMode, GreetingTextRow, FaqEntryRow from @/lib/types
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "'use client'" src/components/config/BegruesungsTexteTab.tsx outputs 1
    - grep "EU-KI-Gesetz Art. 50\|Pflichthinweis" src/components/config/BegruesungsTexteTab.tsx — lock block label present
    - grep "Sie sprechen mit einem KI-gestützten Telefonsystem" src/components/config/BegruesungsTexteTab.tsx — verbatim disclosure text present as constant/literal
    - grep "role=\"region\"" src/components/config/BegruesungsTexteTab.tsx — lock block has role=region
    - grep "aria-label=\"EU-KI-Gesetz Art. 50 Pflichthinweis\"" src/components/config/BegruesungsTexteTab.tsx — accessible label on lock block
    - grep "saveGreetingTextAction\|addFaqEntryAction\|deleteFaqEntryAction" src/components/config/BegruesungsTexteTab.tsx — all 3 imported and used
    - grep "Urlaub\|Vertretung\|Eigener Vertretungsdienst" src/components/config/BegruesungsTexteTab.tsx — all 4 mode tab labels present
    - grep "Dialog\|DialogContent" src/components/config/BegruesungsTexteTab.tsx — FAQ delete confirmation dialog
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>BegruesungsTexteTab renders 4 sub-tabs each with locked EU AI Act block, editable greeting textarea, and FAQ section. Lock block is visually non-editable. TypeScript strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BegruesungsTexteTab (browser) → saveGreetingTextAction | user_text and mode are untrusted; eu_ai_act_disclosure must never come from client |
| Server Action → greeting_texts table | eu_ai_act_disclosure column is overwritten unconditionally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-T-01 | Tampering | EU AI Act disclosure removal | mitigate | saveGreetingTextAction sets eu_ai_act_disclosure = EU_AI_ACT_DISCLOSURE constant unconditionally — ignores any client-sent value; eu_ai_act_disclosure_present always true; verified by acceptance criteria grep |
| T-05-T-02 | Tampering | Cross-tenant FAQ delete via forged UUID | mitigate | deleteFaqEntryAction: .eq('id', id).eq('tenant_id', tenantId) — service-role bypasses RLS; explicit tenant filter mandatory |
| T-05-T-03 | Spoofing | Mode injection (invalid greeting mode) | mitigate | Zod z.enum(['normal','vacation','deputy','own_service']) on mode field in both greeting text and FAQ schemas |
| T-05-T-04 | Elevation | Greeting text save without role check | mitigate | saveGreetingTextAction, addFaqEntryAction, deleteFaqEntryAction all gate on ALLOWED_ROLES before DB write |
  | T-05-T-SC | Tampering | EU AI Act disclosure UI-only lock bypass via direct curl to Server Action | mitigate | Server Action never reads eu_ai_act_disclosure from formData; Zod schema intentionally omits this field; constant is hardcoded in server module |
</threat_model>

<verification>
After this plan executes:
- npx tsc --noEmit exits 0
- grep "EU_AI_ACT_DISCLOSURE" src/app/actions/greeting-texts.ts — constant defined and used in upsert
- grep "eu_ai_act_disclosure.*formData\|parsed\.data.*eu_ai_act" src/app/actions/greeting-texts.ts outputs 0 — disclosure never from client
- ls src/components/config/BegruesungsTexteTab.tsx — file exists with >180 lines
- grep "role=\"region\"" src/components/config/BegruesungsTexteTab.tsx — EU AI Act lock block accessible
</verification>

<success_criteria>
- saveGreetingTextAction: unconditionally sets eu_ai_act_disclosure = 'Sie sprechen mit einem KI-gestützten Telefonsystem.' and eu_ai_act_disclosure_present=true regardless of client input — verified by grep
- BegruesungsTexteTab: 4 sub-tabs (Normal/Urlaub/Vertretung/Eigener Vertretungsdienst) each with locked EU AI Act block (role=region, aria-label, no focusable children), greeting textarea, FAQ section with add/delete
- German labels throughout: all copy from UI-SPEC copywriting contract
- TypeScript strict: no errors
</success_criteria>

<output>
Create .planning/phases/05-configuration/05-04-SUMMARY.md when done.
</output>
