---
phase: 04-inbox-task-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/index.ts
  - src/app/actions/update-inbox-status.ts
  - src/app/actions/save-inbox-note.ts
  - src/lib/hooks/useInboxItems.ts
  - src/components/ui/tabs.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/alert.tsx
autonomous: true
requirements:
  - INBOX-01
  - INBOX-04
  - INBOX-05

must_haves:
  truths:
    - "InboxItemRow TypeScript type includes internal_note: string | null — npx tsc --noEmit passes"
    - "updateInboxStatusAction rejects calls from assistant/viewer with 'Unzureichende Berechtigung.' error"
    - "updateInboxStatusAction validates enum and UUID inputs via Zod before any DB call"
    - "saveInboxNoteAction validates note max 4000 chars via Zod; updates both internal_note and updated_at"
    - "useInboxItems returns items array + loading bool; Realtime INSERT deduplicates by id"
    - "shadcn tabs, tooltip, alert components exist in src/components/ui/"
  artifacts:
    - path: "src/lib/types/index.ts"
      provides: "InboxItemRow with internal_note field"
      contains: "internal_note: string | null"
    - path: "src/app/actions/update-inbox-status.ts"
      provides: "updateInboxStatusAction Server Action"
      exports: ["UpdateInboxStatusState", "updateInboxStatusAction"]
    - path: "src/app/actions/save-inbox-note.ts"
      provides: "saveInboxNoteAction Server Action"
      exports: ["SaveInboxNoteState", "saveInboxNoteAction"]
    - path: "src/lib/hooks/useInboxItems.ts"
      provides: "useInboxItems hook with Realtime subscription"
      exports: ["useInboxItems"]
    - path: "src/components/ui/tabs.tsx"
      provides: "shadcn Tabs component"
    - path: "src/components/ui/tooltip.tsx"
      provides: "shadcn Tooltip component"
    - path: "src/components/ui/alert.tsx"
      provides: "shadcn Alert component"
  key_links:
    - from: "useInboxItems"
      to: "useRealtimeChannel"
      via: "import @/lib/hooks/useRealtimeChannel"
      pattern: "useRealtimeChannel<InboxItemRow"
    - from: "updateInboxStatusAction"
      to: "createServiceRoleClient"
      via: "import @/lib/supabase/server"
      pattern: "createServiceRoleClient\\(\\)"
    - from: "saveInboxNoteAction"
      to: "inbox_items"
      via: "serviceClient.from('inbox_items').update"
      pattern: "from\\('inbox_items'\\)"
---

<objective>
Foundation layer for Phase 4: fix the missing TypeScript type field, install 3 shadcn components, and implement the two Server Actions + Realtime hook that all Wave 2 UI components depend on.

Purpose: Wave 2 UI (InboxTable, CaseDetailSheet, /inbox page) cannot be built until InboxItemRow has internal_note, Server Actions exist with correct signatures, and the useInboxItems hook is ready.

Output: 7 artifacts — type fix, 3 shadcn installs, 2 Server Actions, 1 hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-inbox-task-management/04-RESEARCH.md
@.planning/phases/04-inbox-task-management/04-UI-SPEC.md
</context>

<interfaces>
<!-- Existing contracts the executor must know. Extracted from codebase. No codebase exploration needed. -->

From src/lib/types/index.ts — InboxItemRow (CURRENT, missing field):
```typescript
export interface InboxItemRow {
  id: string
  tenant_id: string
  call_log_id: string | null
  case_type: InboxCaseType
  status: InboxStatus
  created_at: string
  updated_at: string
  // internal_note is MISSING — must be added
}
export type InboxCaseType =
  | 'unidentified_patient' | 'invalid_pid' | 'multiple_pid'
  | 'callback_needed' | 'prescription_blocked' | 'unclear_intent'
  | 'emergency' | 'technical_error'
export type InboxStatus = 'open' | 'in_progress' | 'resolved' | 'archived'
```

From src/lib/hooks/useCallLog.ts — hook pattern to mirror exactly:
```typescript
'use client'
import { useEffect, useState } from 'react'
import { createClient } from '@/lib/supabase/client'
import { useRealtimeChannel } from '@/lib/hooks/useRealtimeChannel'
import type { CallLogRow } from '@/lib/types'
import type { RealtimePostgresChangesPayload } from '@supabase/supabase-js'
// Cast: T & { [key: string]: unknown } required for useRealtimeChannel generic constraint
// (STATE.md locked: "useRealtimeChannel<T> uses T extends { [key: string]: unknown }")
```

From src/app/actions/toggle-ara-med.ts — role gate pattern:
```typescript
const ALLOWED_ROLES = ['operator', 'ordination_admin']
const araRole = user.app_metadata?.ara_role as string | undefined
if (!araRole || !ALLOWED_ROLES.includes(araRole)) return { error: 'Unzureichende Berechtigung.' }
```

From src/app/actions/call-feedback.ts — service-role write pattern:
```typescript
const serviceClient = createServiceRoleClient()
const { error: dbError } = await serviceClient
  .from('call_log')
  .update(updateData)
  .eq('id', callId)
  .eq('tenant_id', tenantId) // Defense-in-depth: forged id from another tenant → zero rows
```

From src/lib/supabase/server.ts — imports needed:
```typescript
import { createServerClient, createServiceRoleClient } from '@/lib/supabase/server'
// createServerClient is async — must be awaited
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix InboxItemRow type + install shadcn tabs/tooltip/alert</name>
  <files>src/lib/types/index.ts, src/components/ui/tabs.tsx, src/components/ui/tooltip.tsx, src/components/ui/alert.tsx</files>
  <read_first>
    - src/lib/types/index.ts — read the full file; InboxItemRow is at line 92-100; add internal_note field after status and before created_at
    - src/components/ui/ directory listing — confirm tabs.tsx, tooltip.tsx, alert.tsx do NOT exist before installing
  </read_first>
  <action>
    Step 1 — Edit src/lib/types/index.ts: In the InboxItemRow interface (currently lines 92-100), add the field `internal_note: string | null` between `status: InboxStatus` and `created_at: string`. Add a comment `// Phase 04: INBOX-05 — note field (DB column existed in Phase 2 migration, type was missing)` above it.

    Step 2 — Install shadcn components. Run in the project root (PowerShell):
    `npx shadcn add tabs tooltip alert --yes`

    If the --yes flag is not supported, run each install separately and confirm prompts. After install, verify that src/components/ui/tabs.tsx, src/components/ui/tooltip.tsx, and src/components/ui/alert.tsx all exist with exported components.

    Do NOT modify the generated shadcn files — use them as-is. shadcn generates correct components against the installed radix-ui versions.
  </action>
  <verify>
    <automated>cd "C:\##CLAUDE-CODE\ARA-Med-Dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types/index.ts: InboxItemRow interface contains `internal_note: string | null`
    - src/components/ui/tabs.tsx exists and exports Tabs, TabsList, TabsTrigger, TabsContent
    - src/components/ui/tooltip.tsx exists and exports Tooltip, TooltipProvider, TooltipTrigger, TooltipContent
    - src/components/ui/alert.tsx exists and exports Alert, AlertTitle, AlertDescription
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>InboxItemRow has internal_note field; 3 shadcn components installed; TypeScript check passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: updateInboxStatusAction Server Action (role gate + Zod + service-role write)</name>
  <files>src/app/actions/update-inbox-status.ts</files>
  <read_first>
    - src/app/actions/toggle-ara-med.ts — read fully; mirrors role gate pattern (ALLOWED_ROLES check on ara_role from JWT)
    - src/app/actions/call-feedback.ts — read fully; mirrors Zod + service-role + eq filter pattern
    - src/lib/types/index.ts — InboxStatus type (4 values: open/in_progress/resolved/archived)
  </read_first>
  <behavior>
    - Input (itemId='not-a-uuid', newStatus='open') → returns { error: 'Ungültige Eingabe.' } (Zod UUID fail)
    - Input (itemId=valid-uuid, newStatus='invalid') → returns { error: 'Ungültige Eingabe.' } (Zod enum fail)
    - Unauthenticated call → returns { error: 'Nicht authentifiziert.' }
    - No tenant_id in JWT → returns { error: 'Kein Tenant zugewiesen.' }
    - ara_role='assistant' → returns { error: 'Unzureichende Berechtigung.' }
    - ara_role='viewer' → returns { error: 'Unzureichende Berechtigung.' }
    - ara_role='operator', valid inputs, DB succeeds → returns { success: true, newStatus: 'in_progress' }
    - DB error → returns { error: 'Statusänderung fehlgeschlagen. Bitte erneut versuchen.' }
  </behavior>
  <action>
    Create src/app/actions/update-inbox-status.ts as a new file with `'use server'` directive.

    Exports:
    - `UpdateInboxStatusState` interface: `{ success?: boolean; error?: string; newStatus?: InboxStatus }`
    - `updateInboxStatusAction(itemId: string, newStatus: InboxStatus): Promise<UpdateInboxStatusState>`

    Implementation sequence (match toggle-ara-med.ts pattern exactly):
    1. Zod schema: `z.object({ itemId: z.string().uuid(), newStatus: z.enum(['open', 'in_progress', 'resolved', 'archived']) })` — safeParse input; return `{ error: 'Ungültige Eingabe.' }` if fails
    2. `const supabase = await createServerClient()` — note: must await (STATE.md locked)
    3. `supabase.auth.getUser()` — return `{ error: 'Nicht authentifiziert.' }` if no user
    4. Extract `tenantId` from `user.app_metadata?.tenant_id` — return `{ error: 'Kein Tenant zugewiesen.' }` if missing
    5. Extract `araRole` from `user.app_metadata?.ara_role` — check against `['operator', 'ordination_admin']` — return `{ error: 'Unzureichende Berechtigung.' }` if not allowed. This is MANDATORY: service-role bypasses RLS UPDATE policy which restricts to these roles.
    6. `const serviceClient = createServiceRoleClient()` (no await — sync)
    7. `serviceClient.from('inbox_items').update({ status: newStatus, updated_at: new Date().toISOString() }).eq('id', itemId).eq('tenant_id', tenantId)` — note: both .eq() filters required for defense-in-depth (forged itemId from another tenant → zero rows). Include `updated_at` because no auto-update trigger exists on inbox_items.
    8. Return `{ error: 'Statusänderung fehlgeschlagen. Bitte erneut versuchen.' }` on dbError; else `{ success: true, newStatus: parsed.data.newStatus }`

    Import: `import type { InboxStatus } from '@/lib/types'`
    No `any`. TypeScript strict.
  </action>
  <verify>
    <automated>cd "C:\##CLAUDE-CODE\ARA-Med-Dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/actions/update-inbox-status.ts
    - Exports UpdateInboxStatusState and updateInboxStatusAction
    - Contains `ALLOWED_ROLES = ['operator', 'ordination_admin']` role gate before any DB call
    - Contains `.eq('tenant_id', tenantId)` on the update query (defense-in-depth filter)
    - Contains `updated_at: new Date().toISOString()` in update payload
    - `npx tsc --noEmit` passes with no errors referencing this file
    - grep: `grep -c "Unzureichende Berechtigung" src/app/actions/update-inbox-status.ts` returns 1
  </acceptance_criteria>
  <done>updateInboxStatusAction enforces role gate, validates input via Zod, updates inbox_items with both status and updated_at, filtered by both id and tenant_id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: saveInboxNoteAction Server Action + useInboxItems hook</name>
  <files>src/app/actions/save-inbox-note.ts, src/lib/hooks/useInboxItems.ts</files>
  <read_first>
    - src/app/actions/call-feedback.ts — saveInboxNoteAction mirrors this exactly (Zod max(4000), tenant-scoped update); NO role gate (assistants can add notes)
    - src/lib/hooks/useCallLog.ts — useInboxItems mirrors this exactly (initial fetch + Realtime INSERT/UPDATE/DELETE with dedup)
    - src/lib/hooks/useRealtimeChannel.ts — verify generic constraint signature (T extends { [key: string]: unknown }) before writing the hook cast
    - src/lib/types/index.ts — InboxItemRow interface (now with internal_note after Task 1)
  </read_first>
  <behavior>
    saveInboxNoteAction:
    - Input (itemId='bad', note='x') → returns { error: 'Ungültige Eingabe.' } (UUID fail)
    - Input (itemId=valid, note=string-of-4001-chars) → returns { error: 'Ungültige Eingabe.' } (max fail)
    - Unauthenticated → returns { error: 'Nicht authentifiziert.' }
    - No tenant_id → returns { error: 'Kein Tenant zugewiesen.' }
    - Valid inputs, DB succeeds → returns { success: true }
    - DB error → returns { error: 'Notiz konnte nicht gespeichert werden.' }

    useInboxItems:
    - Returns { items: InboxItemRow[], loading: boolean }
    - Initial fetch ordered by created_at DESC, limit 200, eq tenant_id
    - Realtime INSERT: deduplicates by id (same dedup logic as useCallLog)
    - Realtime UPDATE: replaces row in-place by id
    - Realtime DELETE: removes row by id
  </behavior>
  <action>
    PART A — Create src/app/actions/save-inbox-note.ts:

    `'use server'` directive. Exports:
    - `SaveInboxNoteState` interface: `{ success?: boolean; error?: string }`
    - `saveInboxNoteAction(itemId: string, note: string): Promise<SaveInboxNoteState>`

    Zod schema: `z.object({ itemId: z.string().uuid(), note: z.string().max(4000) })`

    Auth sequence (no role gate — any authenticated tenant user can save notes per INBOX-05):
    1. Zod safeParse → { error: 'Ungültige Eingabe.' } on fail
    2. `await createServerClient()` → getUser() → { error: 'Nicht authentifiziert.' } if no user
    3. Extract tenantId from JWT → { error: 'Kein Tenant zugewiesen.' } if missing
    4. `createServiceRoleClient().from('inbox_items').update({ internal_note: note, updated_at: new Date().toISOString() }).eq('id', itemId).eq('tenant_id', tenantId)`
    5. Return { error: 'Notiz konnte nicht gespeichert werden.' } on dbError; else { success: true }

    PART B — Create src/lib/hooks/useInboxItems.ts:

    `'use client'` directive. Mirror useCallLog.ts exactly, substituting:
    - Table: 'call_log' → 'inbox_items'
    - Type: CallLogRow → InboxItemRow
    - State: calls/setCalls → items/setItems
    - Channel name: 'call_log:' + tenantId → 'inbox_items:' + tenantId (useRealtimeChannel handles this internally via the `table` prop)
    - Limit: 100 → 200
    - Return: `{ calls, loading }` → `{ items, loading }`

    Critical cast (STATE.md locked decision): use `InboxItemRow & { [key: string]: unknown }` as the generic argument to useRealtimeChannel — this is required because named interfaces lack index signatures.

    Dedup logic for INSERT (copy exactly from useCallLog):
    ```
    const exists = prev.some(r => r.id === newRow.id)
    if (exists) return prev.map(r => r.id === newRow.id ? newRow : r)
    return [newRow, ...prev]
    ```
  </action>
  <verify>
    <automated>cd "C:\##CLAUDE-CODE\ARA-Med-Dashboard" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/actions/save-inbox-note.ts exists; exports SaveInboxNoteState and saveInboxNoteAction
    - No ALLOWED_ROLES check in save-inbox-note.ts (intentional — all users can save notes)
    - Contains `z.string().max(4000)` for note validation
    - Contains `updated_at: new Date().toISOString()` in update payload
    - src/lib/hooks/useInboxItems.ts exists; exports useInboxItems
    - Hook query: `.from('inbox_items').eq('tenant_id', tenantId).order('created_at', { ascending: false }).limit(200)`
    - Hook uses `InboxItemRow & { [key: string]: unknown }` cast for useRealtimeChannel generic
    - `npx tsc --noEmit` passes with no errors referencing either file
    - grep: `grep -c "inbox_items" src/lib/hooks/useInboxItems.ts` returns at least 2 (initial fetch + Realtime table)
  </acceptance_criteria>
  <done>saveInboxNoteAction saves notes with Zod validation and tenant-scoped update; useInboxItems provides tenant-scoped item list with Realtime INSERT/UPDATE/DELETE dedup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | updateInboxStatusAction and saveInboxNoteAction receive untrusted itemId and payload from browser |
| Server Action → Supabase | service-role client bypasses RLS — application must enforce role checks that RLS UPDATE policy would enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | updateInboxStatusAction: itemId parameter | mitigate | `.eq('tenant_id', tenantId)` on every update — forged itemId from another tenant matches zero rows |
| T-04-02 | Elevation of Privilege | updateInboxStatusAction: role bypass | mitigate | Explicit `ara_role` check against `['operator', 'ordination_admin']` before any DB write — mirrors toggleAraMedAction pattern; service-role bypasses RLS so app-level check is mandatory |
| T-04-03 | Tampering | saveInboxNoteAction: note content | mitigate | `z.string().max(4000)` Zod validation — same limit as call-feedback note |
| T-04-04 | Information Disclosure | useInboxItems Realtime channel | mitigate | useRealtimeChannel uses `filter: 'tenant_id=eq.' + tenantId` — C3 locked pattern; cross-tenant data cannot flow through channel |
| T-04-05 | Spoofing | saveInboxNoteAction: itemId parameter | mitigate | `.eq('tenant_id', tenantId)` filter on update — forged itemId from another tenant → zero rows |
| T-04-SC | Tampering | npm/shadcn add installs | mitigate | shadcn CLI generates code from already-installed @radix-ui deps — no new npm package published; no ASSUMED/SUS packages |
</threat_model>

<verification>
After all 3 tasks complete:
- `npx tsc --noEmit` passes with exit code 0
- InboxItemRow in src/lib/types/index.ts contains `internal_note: string | null`
- src/components/ui/ contains tabs.tsx, tooltip.tsx, alert.tsx
- src/app/actions/update-inbox-status.ts exports updateInboxStatusAction with role gate
- src/app/actions/save-inbox-note.ts exports saveInboxNoteAction without role gate
- src/lib/hooks/useInboxItems.ts exports useInboxItems with Realtime subscription
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0 after all 3 tasks
- All 7 artifacts exist at the listed paths
- updateInboxStatusAction returns 'Unzureichende Berechtigung.' for assistant/viewer roles — verifiable by code inspection (ALLOWED_ROLES const + guard clause present)
- saveInboxNoteAction has no role gate — verifiable by code inspection (no ALLOWED_ROLES const)
- Both Server Actions include `updated_at: new Date().toISOString()` in update payload — verifiable by code inspection
- useInboxItems mirrors useCallLog dedup pattern — verifiable by code inspection
</success_criteria>

<output>
Create `.planning/phases/04-inbox-task-management/04-01-SUMMARY.md` when done.
</output>
