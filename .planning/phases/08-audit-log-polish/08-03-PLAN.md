---
phase: 08-audit-log-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01-PLAN.md"
files_modified:
  - src/app/(dashboard)/audit-log/page.tsx
  - src/components/audit/AuditLogTable.tsx
  - src/components/audit/AuditLogFilters.tsx
autonomous: true
requirements:
  - AUDIT-03

must_haves:
  truths:
    - "src/app/(dashboard)/audit-log/page.tsx is a Server Component that fetches audit_log records for the tenant"
    - "Page supports URL-based filtering: ?tage=7/30/90 for date range, ?action= for action type"
    - "Only operator and ordination_admin can access the page (server-side redirect for others)"
    - "AuditLogTable component renders rows with formatted timestamp, user, action badge, object, and details"
    - "AuditLogFilters component renders date range and action type filter controls"
    - "TypeScript compiles clean — no errors"
  artifacts:
    - path: "src/app/(dashboard)/audit-log/page.tsx"
      provides: "Full Server Component: role gate, date filter, action filter, audit_log query, renders AuditLogTable"
      contains: "AuditLogTable"
    - path: "src/components/audit/AuditLogTable.tsx"
      provides: "Table component with columns: timestamp, user, action, object type, object id, details"
      contains: "export"
    - path: "src/components/audit/AuditLogFilters.tsx"
      provides: "Client component: date range selector + action type filter (URL-based — no client state)"
      contains: "use client"
  key_links:
    - from: "src/app/(dashboard)/audit-log/page.tsx"
      to: "src/components/audit/AuditLogTable.tsx"
      via: "import and render AuditLogTable"
      pattern: "AuditLogTable"
    - from: "src/app/(dashboard)/audit-log/page.tsx"
      to: "src/components/audit/AuditLogFilters.tsx"
      via: "import and render AuditLogFilters above table"
      pattern: "AuditLogFilters"
---

<objective>
Build the /audit-log page and its components so operators and ordination_admin users can browse, filter, and review the full audit trail for their tenant (AUDIT-03).

This plan runs in parallel with 08-02 (instrumenting Server Actions). Both depend on 08-01 (foundation — audit_log table + logAuditEvent helper).

Design: Server Component page with URL-based filters (no client state on the page itself). AuditLogFilters is the only 'use client' component — for the filter UI interaction that pushes new URL params. Table is a pure Server Component.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key analogs:
- src/app/(dashboard)/statistiken/page.tsx (URL-based filter pattern with searchParams typed as Promise<{...}>)
- src/components/statistics/KpiCard.tsx (Server Component component pattern — no 'use client')
- src/app/(dashboard)/benutzer/page.tsx (role gate redirect pattern — replicate for audit-log)

Filter design:
- Date range: ?tage=7 (default), ?tage=30, ?tage=90 — same pattern as Statistiken page
- Action filter: ?action=<ACTION_CONSTANT> or empty for all
- Filters are applied in the page Server Component query — no client-side filtering

UI style: Match the dashboard aesthetic — Tailwind + shadcn/ui Card, consistent with Statistiken and Benutzer pages.
</context>

<interfaces>
Page query (supabase service role, tenant-scoped):
```typescript
const { data: auditRows } = await serviceClient
  .from('audit_log')
  .select('id, action, object_type, object_id, user_id, old_value, new_value, ip_address, created_at')
  .eq('tenant_id', tenantId)
  .gte('created_at', dateFrom.toISOString())
  .order('created_at', { ascending: false })
  .limit(500)
```

For action filter (when ?action is set):
```typescript
.eq('action', actionFilter)
```

Email resolution: The page must join user_id → email. Use the same pattern as benutzer/page.tsx:
- Call serviceClient.auth.admin.listUsers({ perPage: 1000 })
- Build a Map<string, string> of userId → email
- Pass the map to AuditLogTable for display

AuditLogTable props:
```typescript
interface AuditLogTableProps {
  rows: AuditLogRow[]
  userEmailMap: Map<string, string>
}
```

Action badge colors (suggestion — adjust for visual clarity):
- USER_* → blue
- SETTINGS_* → purple
- ARA_MED_* → green
- OPENING_HOURS_* / DEPUTY_* → amber
- ROUTING_* / COMM_* / TEMPLATE_* → indigo
- APPOINTMENT_TYPE_* / MEDICATION_* / GREETING_* → teal
- default → gray

AuditLogFilters props:
```typescript
interface AuditLogFiltersProps {
  currentDays: number          // 7, 30, or 90
  currentAction: string        // '' or action constant
  availableActions: string[]   // distinct action values for dropdown
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create src/components/audit/AuditLogTable.tsx</name>
  <files>src/components/audit/AuditLogTable.tsx</files>
  <read_first>
    - src/components/statistics/KpiCard.tsx (Server Component pattern)
    - src/lib/types/index.ts (AuditLogRow interface — look in Phase 08 section)
  </read_first>
  <action>
    Create src/components/audit/AuditLogTable.tsx as a Server Component (no 'use client'):

    Props: { rows: AuditLogRow[], userEmailMap: Map<string, string> }

    Render a table with columns:
    1. Zeitpunkt — formatted: new Date(row.created_at).toLocaleString('de-AT', { timeZone: 'Europe/Vienna', day: '2-digit', month: '2-digit', year: 'numeric', hour: '2-digit', minute: '2-digit' })
    2. Benutzer — userEmailMap.get(row.user_id) ?? row.user_id (truncate to 30 chars with ellipsis if needed)
    3. Aktion — colored badge based on action prefix (USER_ → blue, etc. per interface above)
    4. Objekt — row.object_type: row.object_id (truncate object_id to 20 chars)
    5. Details — a collapsed JSON preview of new_value or old_value (whichever is non-null), shown as a small monospace text truncated to 80 chars

    If rows.length === 0: render an empty state message "Keine Audit-Einträge für diesen Zeitraum."

    Styling:
    - Use a Card wrapper with overflow-hidden
    - Table: w-full text-sm
    - Thead: bg-gray-50 text-xs uppercase tracking-wide text-gray-500
    - Tbody: divide-y divide-gray-100
    - Zebra: even rows bg-gray-50/50
    - Badge: inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium
    - Limit: show max 500 rows (query is already limited — add a note if row count = 500)

    Import AuditLogRow from '@/lib/types'
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/audit/AuditLogTable.tsx
    - No 'use client' directive
    - grep "AuditLogRow" src/components/audit/AuditLogTable.tsx confirms type import
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AuditLogTable.tsx created as Server Component. Renders timestamped, color-coded audit rows.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/components/audit/AuditLogFilters.tsx</name>
  <files>src/components/audit/AuditLogFilters.tsx</files>
  <read_first>
    - src/app/(dashboard)/statistiken/page.tsx (URL filter link pattern — use same approach)
  </read_first>
  <action>
    Create src/components/audit/AuditLogFilters.tsx as a Client Component ('use client'):

    Props: { currentDays: number, currentAction: string, availableActions: string[] }

    Render two filter controls:

    1. Date range buttons (same pattern as Statistiken):
       [7 Tage] [30 Tage] [90 Tage]
       Each is an anchor tag (<a href="?tage=N">) or uses router.push — use anchor tags for simplicity.
       Active button: bg-blue-600 text-white; inactive: bg-white text-gray-700 border

    2. Action filter: a <select> element with onChange using useRouter().push:
       - First option: value="" label="Alle Aktionen"
       - Then one option per availableActions entry
       - Current value = currentAction
       Use useRouter from 'next/navigation' and router.push to update URL.

    Layout: flex items-center gap-4 flex-wrap mb-4

    Import useRouter from 'next/navigation'.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/audit/AuditLogFilters.tsx
    - grep "'use client'" src/components/audit/AuditLogFilters.tsx confirms client directive
    - grep "useRouter" src/components/audit/AuditLogFilters.tsx confirms router usage
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AuditLogFilters.tsx created as Client Component. Date range and action filter controls.</done>
</task>

<task type="auto">
  <name>Task 3: Build full /audit-log page</name>
  <files>src/app/(dashboard)/audit-log/page.tsx</files>
  <read_first>
    - src/app/(dashboard)/audit-log/page.tsx (stub from Plan 08-01 — overwrite with full implementation)
    - src/app/(dashboard)/statistiken/page.tsx (searchParams typing, parallel queries, URL filter pattern)
    - src/app/(dashboard)/benutzer/page.tsx (auth.admin.listUsers pattern for email resolution)
    - src/lib/types/index.ts (AuditLogRow type)
  </read_first>
  <action>
    Replace the stub with the full implementation of src/app/(dashboard)/audit-log/page.tsx:

    1. Server Component (no 'use client')
    2. Props: { searchParams: Promise<{ tage?: string; action?: string }> }
    3. Auth: createServerClient → getUser() → redirect('/auth/login') if no user
    4. Role gate: araRole from user.app_metadata.ara_role — if not operator/ordination_admin → redirect('/dashboard')
    5. tenantId from user.app_metadata.tenant_id (cast to string)
    6. Parse filters:
       - const days = parseInt((await searchParams).tage ?? '7', 10) — must be 7, 30, or 90 (default 7 if invalid)
       - const actionFilter = (await searchParams).action ?? ''
    7. Compute dateFrom: new Date(Date.now() - days * 24 * 60 * 60 * 1000)
    8. Service role client (createServiceRoleClient — synchronous, no await)
    9. Parallel fetch:
       - audit_log rows: .from('audit_log').select(...).eq('tenant_id', tenantId).gte('created_at', dateFrom.toISOString()).order('created_at', { ascending: false }).limit(500)
         + .eq('action', actionFilter) when actionFilter is non-empty
       - auth users: serviceClient.auth.admin.listUsers({ perPage: 1000 })
       - distinct actions for filter dropdown: .from('audit_log').select('action').eq('tenant_id', tenantId).order('action')
    10. Build userEmailMap: Map<string, string> from auth users list
    11. Build availableActions: deduplicated sorted string[] from distinct actions query
    12. Render:
        - Page heading: "Audit-Log" (h1 text-2xl font-bold)
        - Subtitle: "Alle sicherheitsrelevanten Änderungen für Ihren Mandanten." (text-gray-500 text-sm)
        - <AuditLogFilters currentDays={days} currentAction={actionFilter} availableActions={availableActions} />
        - <AuditLogTable rows={auditRows ?? []} userEmailMap={userEmailMap} />
        - If rows.length === 500: show a note "Maximale Anzahl von 500 Einträgen angezeigt. Filtern Sie den Zeitraum für ältere Einträge."

    Import createServerClient from '@/lib/supabase/server'
    Import createServiceRoleClient from '@/lib/supabase/server'
    Import AuditLogTable from '@/components/audit/AuditLogTable'
    Import AuditLogFilters from '@/components/audit/AuditLogFilters'
    Import AuditLogRow from '@/lib/types'
    Import redirect from 'next/navigation'
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "AuditLogTable\|AuditLogFilters" src/app/(dashboard)/audit-log/page.tsx — both components imported
    - grep "ordination_admin" src/app/(dashboard)/audit-log/page.tsx — role gate present
    - grep "searchParams" src/app/(dashboard)/audit-log/page.tsx — filter param handling
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Full /audit-log page built. Role gate, date filter, action filter, table rendering complete.</done>
</task>

<task type="auto">
  <name>Task 4: Git commit audit-log UI</name>
  <files>all modified and created files</files>
  <action>
    Stage and commit all changes:
    - src/app/(dashboard)/audit-log/page.tsx (full implementation)
    - src/components/audit/AuditLogTable.tsx (new)
    - src/components/audit/AuditLogFilters.tsx (new)

    Commit message: "feat(phase-08): audit-log page — role-gated table with date and action filters (AUDIT-03)"
  </action>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - git status shows clean working tree after commit
    - git log --oneline -1 confirms the commit
  </acceptance_criteria>
  <done>Audit-log page and components committed. /audit-log live for operator/ordination_admin users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /audit-log | Server-side role check — araRole from JWT, cannot be spoofed |
| Page → audit_log query | tenant_id from JWT app_metadata — RLS + explicit .eq() double-isolation |
| Page → auth.admin.listUsers | Service role client — only available server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Elevation | assistant/viewer reads /audit-log | mitigate | Server-side redirect on araRole — not operator/ordination_admin → /dashboard |
| T-08-08 | Info Disclosure | Cross-tenant audit log leak | mitigate | .eq('tenant_id', tenantId) + RLS SELECT policy — double isolation |
| T-08-09 | Info Disclosure | User emails exposed in audit table | accept | Emails are within-tenant data; operator already has access to user list |
| T-08-SC | Tampering | npm installs | accept | No new packages in this plan |
</threat_model>

<verification>
After all tasks:
- Test-Path src/components/audit/AuditLogTable.tsx
- Test-Path src/components/audit/AuditLogFilters.tsx
- grep "AuditLogTable\|AuditLogFilters" src/app/(dashboard)/audit-log/page.tsx — both present
- grep "'use client'" src/components/audit/AuditLogFilters.tsx — client directive present
- grep -v "'use client'" src/components/audit/AuditLogTable.tsx — NOT a client component
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
- /audit-log page accessible only to operator and ordination_admin
- Page shows audit_log rows filtered by date range (?tage=7/30/90) and action type (?action=)
- AuditLogTable renders: formatted timestamp, user email, color-coded action badge, object type/id, value preview
- AuditLogFilters provides date range buttons and action type dropdown — URL-driven, no client state
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/08-audit-log-polish/08-03-SUMMARY.md when done.
</output>
