---
phase: 08-audit-log-polish
plan: 04
type: execute
wave: 3
depends_on:
  - "08-02-PLAN.md"
  - "08-03-PLAN.md"
files_modified:
  - .planning/ROADMAP.md
  - .planning/HANDOFF.json
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03

must_haves:
  truths:
    - "npx tsc --noEmit exits 0 — no TypeScript errors across the entire project"
    - "Docker build succeeds via Portainer PUT redeploy (or npm run build exits 0 locally)"
    - "DSGVO compliance checklist completed with no blocking findings"
    - "ROADMAP.md Phase 8 marked [x] complete with all 4 plans checked"
    - "HANDOFF.json updated to reflect project complete status"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete with completion date"
      contains: "[x] **Phase 8"
    - path: ".planning/HANDOFF.json"
      provides: "Final status update"
      contains: "complete"
---

<objective>
Final wave for Phase 8: verify the full build compiles and deploys, perform a DSGVO compliance review, then mark Phase 8 (and the MVP) complete.

This plan runs after 08-02 (Server Action instrumentation) and 08-03 (audit-log page) are both done.
</objective>

<execution_context>
@C:/Users/AWinter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/AWinter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.json

DSGVO compliance review checklist (from ROADMAP Phase 8 Success Criterion 4):
1. Phone numbers stored only as hashed values — no plaintext phone in dashboard DB
2. Transcripts accessible only to users with call-detail rights (audio/transkripte permission)
3. Audio URLs generated on-demand with 15-minute expiry (not stored permanently)
4. SVNR (Sozialversicherungsnummer) never stored in the dashboard DB
5. Tenant data isolation verified across all tables (RLS + tenant_id enforcement)

Portainer redeploy instructions (from HANDOFF.json blockers):
- Manual PUT to https://194.242.35.77:9443/api/stacks/97?endpointId=3 with PullImage:true AND Env array
- MUST include all 3 env vars (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY) or they get wiped
- MUST include stackFileContent in PUT body — GET /api/stacks/97/file?endpointId=3 first
- GitHub Actions auto-deploy is blocked by firewall — manual Portainer PUT is the deployment path
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full TypeScript type-check</name>
  <files>all project files</files>
  <action>
    Run full TypeScript type-check:
    npx tsc --noEmit

    If errors: fix them before continuing. Common issues after Phase 8 instrumentation:
    - Missing import of logAuditEvent in an action file
    - Wrong parameter name in logAuditEvent call (e.g., 'tenantId' not 'tenant_id')
    - Type mismatch in oldValue/newValue (must be Record<string, unknown> | null — not object)

    Only proceed to Task 2 when npx tsc --noEmit exits 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit; Write-Output "Exit: $LASTEXITCODE"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - Zero TypeScript errors
  </acceptance_criteria>
  <done>TypeScript type-check passes with 0 errors across the entire project.</done>
</task>

<task type="auto">
  <name>Task 2: Production build verification</name>
  <files>N/A (build check only)</files>
  <action>
    Run production build to catch any runtime errors not caught by TypeScript:
    npm run build 2>&1

    Watch for:
    - "Type error" lines — fix before continuing
    - "Module not found" — import path error in a new component
    - "useRouter only works in Client Components" — missing 'use client' in AuditLogFilters
    - "Page ..." errors — check page.tsx files

    If build fails: fix the specific error, re-run. Do not proceed to Task 3 until build exits 0.

    Note: If npm run build hangs (Next.js 16 Turbopack sometimes does), try:
    npx next build --no-turbopack
    or verify with just: npx tsc --noEmit (already done in Task 1).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Build output ends with "Route (app)" table and "✓ Compiled successfully" or equivalent
    - No "Type error" or "Module not found" in build output
    - Exit code 0
  </acceptance_criteria>
  <done>Production build succeeds. All pages compile to static/server components correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Portainer redeploy</name>
  <files>N/A (deployment only)</files>
  <read_first>
    - .planning/HANDOFF.json (blocker section — Portainer PUT pattern with env vars and stackFileContent)
    - .env.local (for PORTAINER_API_KEY if stored there — check carefully)
  </read_first>
  <action>
    Trigger production redeploy via Portainer API.

    Step 1: Get current stack file content:
    curl --insecure -s -X GET \
      "https://194.242.35.77:9443/api/stacks/97/file?endpointId=3" \
      -H "X-API-Key: {PORTAINER_API_KEY}"
    → Save the stackFileContent value from the response.

    Step 2: Read current env vars from Portainer to avoid wiping them:
    curl --insecure -s -X GET \
      "https://194.242.35.77:9443/api/stacks/97?endpointId=3" \
      -H "X-API-Key: {PORTAINER_API_KEY}"
    → Note the Env array (3 vars: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY).

    Step 3: PUT to redeploy with PullImage:true:
    curl --insecure -s -X PUT \
      "https://194.242.35.77:9443/api/stacks/97?endpointId=3" \
      -H "X-API-Key: {PORTAINER_API_KEY}" \
      -H "Content-Type: application/json" \
      -d '{
        "stackFileContent": "<from Step 1>",
        "env": [<all 3 env vars from Step 2>],
        "pullImage": true
      }'

    Expected: HTTP 200 with updated stack object.
    If HTTP 400 "Invalid stack file content": stackFileContent is malformed — re-read from Step 1.
    If HTTP 500: check Portainer logs.

    Step 4: Wait ~60 seconds, then verify container is running:
    curl --insecure -s "https://194.242.35.77:9443/api/stacks/97?endpointId=3" \
      -H "X-API-Key: {PORTAINER_API_KEY}" | ConvertFrom-Json | Select-Object -ExpandProperty Status

    Status 1 = Active.

    Step 5: Smoke test — verify app is responding at https://app.aramed.at
    curl -s -o /dev/null -w "%{http_code}" https://app.aramed.at
    Expected: 200 or 302 (redirect to login).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://app.aramed.at</automated>
  </verify>
  <acceptance_criteria>
    - Portainer PUT returns HTTP 200
    - Container status = 1 (Active) after redeploy
    - https://app.aramed.at returns 200 or 302
  </acceptance_criteria>
  <done>Production container redeployed. app.aramed.at responding. Phase 8 live.</done>
</task>

<task type="auto">
  <name>Task 4: DSGVO compliance review</name>
  <files>N/A (code review only)</files>
  <read_first>
    - supabase/migrations/ (scan for any column that stores phone numbers, SVNR, audio URLs as plaintext)
    - src/app/(dashboard)/ (scan for transcript/audio display to verify permission gate)
    - src/lib/types/index.ts (look for any field that could store SVNR or plaintext PII)
  </read_first>
  <action>
    Perform DSGVO compliance review against the 5-point checklist:

    1. Phone numbers — grep for "phone_number\|telefonnummer\|phone" in supabase/migrations/
       Expected: call_log has caller_number_hash (hashed) — not plaintext. Flag if plaintext found.

    2. Transcripts — grep for "transkript\|transcript" in src/app/(dashboard)/
       Expected: transcript display is gated by audio/transkripte permission. Flag if ungated.

    3. Audio URLs — grep for "audio_url\|recording_url" in supabase/migrations/ and src/
       Expected: audio URLs are generated on-demand, not stored permanently (or if stored, expiry policy exists).
       Flag if permanent storage found with no expiry logic.

    4. SVNR — grep for "svnr\|sozialversicherung\|ssn\|social_security" in entire codebase
       Expected: zero matches. Flag any match immediately.

    5. Tenant isolation — verify user_tenant_roles, audit_log, call_log, inbox_items, and config tables all have:
       grep "tenant_id" supabase/migrations/ — each table must have tenant_id column
       grep "ROW LEVEL SECURITY" supabase/migrations/ — each table must have RLS enabled
       Expected: all tables isolated. Flag any table without RLS.

    Document findings as PASS/FLAG for each of the 5 points.
    If any FAIL is found: do not mark Phase 8 complete — report to user and stop.
  </action>
  <verify>
    <automated>grep -r "svnr\|sozialversicherung\|social_security" src/ supabase/ 2>/dev/null | head -5</automated>
  </verify>
  <acceptance_criteria>
    - All 5 DSGVO checklist items PASS
    - Zero SVNR-related columns or fields found
    - Phone numbers confirmed as hashed storage only
    - No blocking DSGVO findings
  </acceptance_criteria>
  <done>DSGVO compliance review complete. All 5 checklist items PASS. No blocking findings.</done>
</task>

<task type="auto">
  <name>Task 5: Update ROADMAP.md — Phase 8 complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (find Phase 8 section — currently [ ] not started)
  </read_first>
  <action>
    In .planning/ROADMAP.md, update Phase 8:

    1. Change phase header from:
       - [ ] **Phase 8: Audit Log & System Polish**
       to:
       - [x] **Phase 8: Audit Log & System Polish** - Compliance-ready, launch-hardened (completed 2026-05-24)

    2. In the Phase 8 Plans section, add plan entries and mark all complete:
       Plans:
       - [x] 08-01-PLAN.md — audit_log migration, logAuditEvent helper, nav link, stub page [Wave 1]
       - [x] 08-02-PLAN.md — Instrument all Server Actions with audit logging (AUDIT-01) [Wave 2a]
       - [x] 08-03-PLAN.md — Audit-log page with date range and action filters (AUDIT-03) [Wave 2b]
       - [x] 08-04-PLAN.md — Build gate, DSGVO compliance review, deployment [Wave 3]

    Do not modify any other sections of ROADMAP.md.
  </action>
  <verify>
    <automated>grep "\[x\].*Phase 8" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "[x].*Phase 8" .planning/ROADMAP.md confirms Phase 8 marked complete
    - All 4 plan entries are checked [x]
    - No other sections modified
  </acceptance_criteria>
  <done>ROADMAP.md updated. Phase 8 marked complete. All 4 plans checked.</done>
</task>

<task type="auto">
  <name>Task 6: Update HANDOFF.json — project complete</name>
  <files>.planning/HANDOFF.json</files>
  <read_first>
    - .planning/HANDOFF.json (current state)
  </read_first>
  <action>
    Update .planning/HANDOFF.json to reflect Phase 8 completion:

    - "phase": "08"
    - "phase_name": "Audit Log & System Polish"
    - "status": "complete"
    - "completed_tasks": [1, 2, 3, 4] (all 4 plans)
    - "remaining_tasks": []
    - "next_action": "MVP complete. All 8 phases delivered. Ready for live tenant onboarding."
    - "context_notes": "Phase 8 complete. audit_log table live. All Server Actions instrumented. /audit-log page live. DSGVO compliance review passed. MVP delivery complete — 73/73 requirements delivered across 8 phases."

    Keep blockers and decisions arrays intact — add one new decision:
    {
      "decision": "All Server Actions instrumented with logAuditEvent() — try/catch prevents audit failures from blocking primary operations",
      "rationale": "Audit reliability must never come at the cost of UX — logAuditEvent swallows its own errors silently",
      "phase": "08"
    }
  </action>
  <verify>
    <automated>(Get-Content .planning/HANDOFF.json | ConvertFrom-Json).status</automated>
  </verify>
  <acceptance_criteria>
    - .planning/HANDOFF.json status = "complete"
    - remaining_tasks = []
    - next_action updated to indicate MVP complete
  </acceptance_criteria>
  <done>HANDOFF.json updated to reflect Phase 8 and MVP completion.</done>
</task>

<task type="auto">
  <name>Task 7: Final git commit</name>
  <files>.planning/ROADMAP.md, .planning/HANDOFF.json</files>
  <action>
    Stage and commit the planning file updates:
    - .planning/ROADMAP.md (Phase 8 marked complete)
    - .planning/HANDOFF.json (status: complete)

    Commit message: "chore(phase-08): mark Phase 8 complete — MVP delivery done (73/73 requirements)"
  </action>
  <acceptance_criteria>
    - git status shows clean working tree
    - git log --oneline -1 confirms the commit
  </acceptance_criteria>
  <done>Phase 8 completion committed. MVP delivery complete — all 73 requirements across 8 phases delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSGVO review → codebase | Read-only review — no code changes unless blocking finding |
| Portainer redeploy → production | Uses API key — confirm key is from .env.local or secure store |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Info Disclosure | SVNR stored in DB | mitigate | DSGVO review Task 4 explicitly checks for SVNR — blocks completion if found |
| T-08-11 | Info Disclosure | Plaintext phone in call_log | mitigate | DSGVO review confirms caller_number_hash — hashed storage only |
| T-08-12 | Availability | Portainer redeploy wipes env vars | mitigate | Task 3 explicitly reads current env before PUT and includes all 3 vars |
</threat_model>

<verification>
After all tasks:
- npx tsc --noEmit exits 0
- npm run build exits 0 (or build verified via tsc)
- curl https://app.aramed.at returns 200/302
- grep "[x].*Phase 8" .planning/ROADMAP.md — Phase 8 marked complete
- (Get-Content .planning/HANDOFF.json | ConvertFrom-Json).status equals "complete"
- git log --oneline -3 shows all Phase 8 commits
</verification>

<success_criteria>
- TypeScript compiles with 0 errors
- Production build succeeds
- app.aramed.at responds after redeploy
- DSGVO compliance review: all 5 checklist items PASS, no blocking findings
- ROADMAP.md Phase 8 marked [x] complete with all 4 plans checked
- HANDOFF.json status = "complete", next_action updated to indicate MVP delivery done
</success_criteria>

<output>
Create .planning/phases/08-audit-log-polish/08-04-SUMMARY.md when done.
</output>
